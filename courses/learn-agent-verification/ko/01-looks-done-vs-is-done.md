# 레슨 1: '완료된 것처럼 보인다'는 '완료됐다'가 아니다

> 학습 목표:
> - Claude가 작업이 *완료된 것처럼 보이는* 지점에서 멈추는 이유와, 그럴 때 검증을 결국 누가 떠맡게 되는지 설명하기
> - 결정론적 시스템과 비결정론적 시스템의 대비를 사용해, 전통적인 테스트의 "입력 X, 경로 Y, 출력 Z" 전제가 에이전트에서 무너지는 이유 설명하기
> - 완료 보고를 받았을 때 어느 문장이 주장이고 어느 문장이 증거인지 구별하고, 검수에 무엇이 아직 빠졌는지 짚어내기
>
> 전제: 이 시리즈의 앞선 아홉 개 코스를 마쳤고, `stop_reason` 이 구동하는 하네스 루프를 손으로 쓸 수 있으며, 체크포인트와 복구를 이해하고 있어야 합니다 | 다음: [레슨 2 >>](./02-what-to-verify.md)

## 화요일에 배포하고, 금요일에 깨지다

화요일 오후, 여러분은 사내 관리자 페이지에 '사용자 일괄 가져오기' 기능을 추가해 달라고 에이전트에 요청합니다. CSV를 업로드하고, 파싱하고, 필드를 검증하고, 데이터베이스에 씁니다. 요구사항을 분명히 적어 준 뒤 회의에 들어갑니다.

돌아와 보니 세션은 멈춰 있습니다. 마지막 메시지는 이렇습니다.

```text
완료했습니다.

- 파싱과 필드 매핑을 위해 src/importer/parseCsv.js 추가
- 이메일 형식과 필수 필드를 확인하는 src/importer/validate.js 추가
- POST /api/users/import 에 배치 삽입 연결

구현은 기존 모듈의 코드 스타일을 따릅니다. 실행 중 에러는 없었습니다.
```

여러분은 diff를 훑어봅니다. 함수는 깔끔하게 분리되어 있고, 이름은 이웃 모듈과 맞춰져 있고, 엣지 케이스도 고려된 것처럼 보입니다 — 빈 파일은 명시적인 에러를 돌려주고, 이메일 정규식도 딱 봐서 망가져 보이지는 않습니다. 머지합니다. 배포합니다.

금요일 오후, 운영팀이 채널에 글을 올립니다. '방금 빈 사용자가 400건이나 들어왔는데 왜죠?'

원인은 간단합니다. 운영팀은 그 CSV를 Excel에서 '다른 이름으로 저장'해서 만들었고, Excel은 UTF-8 파일 앞에 BOM — 눈에 보이지 않는 3바이트 `﻿` — 을 붙이기를 좋아합니다. 그래서 첫 번째 열 이름이 `email` 이 아니라 `﻿email` 로 파싱되었고, 필드 매핑 전체가 헛돌았으며, 모든 행이 '전 필드가 undefined'가 되었습니다. 그 검증 계층은 어땠을까요? 그것은 '이메일 형식이 유효한가'를 확인했지만, undefined 는 다른 분기로 빠져 '이 열은 채워지지 않았다'로 취급되었고, 그래서 통과했습니다.

여기서 손을 뺀 사람은 없습니다. 에이전트는 돌아가는 코드를 썼습니다. 자기가 만든 CSV로 스스로 테스트도 했습니다 — 물론 자기가 만든 CSV에는 BOM이 없습니다. 여러분이 diff를 리뷰할 때 보고 있던 것은 '이 코드가 올바르게 쓰였는가'였지, '이 코드가 현실의 입력과 만나면 무슨 일이 벌어지는가'가 아니었습니다. 양쪽 다 최선을 다했습니다. 그래도 간극은 생겼습니다.

문제는 **멈춘 그 순간**에 있습니다. 에이전트가 멈췄을 때 손에 있던 것은 '썼고, 한 번 다시 읽었고, 괜찮아 보인다'였습니다. '완료됐음을 확인했다'에서 멈춘 것이 아닙니다. '완료된 것처럼 보인다'에서 멈춘 것입니다. 그리고 대화 히스토리만으로는 그 차이를 구별할 수 없습니다.

## 완료된 것처럼 보이는 곳에서 멈춘다

Claude Code 문서는 이것을 담담하게 적어 둡니다. Claude는 작업이 완료된 것처럼 보이면 멈춥니다. 스스로 실행할 수 있는 검사가 없으면 '완료된 것처럼 보인다'가 손에 넣을 수 있는 유일한 신호이고, 여러분 자신이 검증 루프가 됩니다. 모든 실수가 여러분이 알아채 주기를 기다리게 됩니다[^S4].

이 문장은 단어 하나하나 두 번 읽을 값어치가 있습니다. 'Claude가 가끔 대충 한다'도 아니고 '모델의 능력이 아직 부족하다'도 아닙니다. 이것은 구조적인 사실을 서술합니다. **파이프라인 전체에서 객관적인 결과를 낼 수 있는 것이 하나도 없다면, '완료된 것처럼 보인다'가 이 시스템에 존재하는 유일한 신호가 됩니다.** 모델은 그 신호만으로 판단할 수밖에 없습니다. 다른 것은 아무것도 가지고 있지 않습니다.

같은 문서는 이 현상에 이름을 붙입니다. the trust-then-verify gap(먼저 믿고 그다음에 검증하는 간극) — Claude는 그럴듯해 보이지만 엣지 케이스를 처리하지 않는 구현을 내놓습니다[^S4]. 쉽게 말하면 이렇습니다. 먼저 믿고(코드가 좋아 보인다), 검증은 일어나지 않거나 너무 늦게 일어납니다(금요일 오후, 운영 채널에서). 위의 BOM 예시는 이 간극의 표준형입니다. 코드가 틀린 것이 아니라, 'Excel에서 내보낸 파일이면 어떻게 되지?'를 아무도 묻지 않은 것입니다.

여기에는 놓치기 쉬운 두 번째 층이 있습니다. 문서가 제시하는 대책은 이렇게 끝납니다. **검증할 수 없다면, 배포하지 마라**[^S4]. 강조점은 '검증한다'가 아니라 '배포하지 않는다' 쪽에 있습니다. 이것은 아무리 해도 검증할 수 없는 것이 존재함을 인정합니다. 검증할 수 없을 때 올바른 한 수는 '이번만 감을 믿는다'가 아닙니다. 범위를 좁히거나, 요구사항을 바꾸거나, 배포를 미루는 것입니다.

## 주장과 증거: 무엇이 다른가

그 완료 메시지로 돌아갑시다. 한 문장씩 쪼개고, 각각에 같은 질문을 던집니다. **코드를 읽지 않고, 보여 준 것만으로 이 문장을 확인할 수 있는가?**

- '`src/importer/parseCsv.js` 추가' — 확인할 수 있습니다. 파일이 존재하는지는 한눈에 확인됩니다. 이것은 증거입니다(가장 약한 종류이긴 합니다).
- '구현은 기존 모듈의 코드 스타일을 따릅니다' — 확인할 수 없습니다. 이것은 모델의 미적 판단입니다. 주장입니다.
- '실행 중 에러는 없었습니다' — 증거처럼 들리지만 사실은 주장입니다. 호출한 도구들이 예외를 던지지 않았다는 말이지, 출력이 올바르다는 말이 아닙니다. 모든 도구가 성공을 돌려주면서 결과는 완전히 틀린 상황 — 얼마든지 가능합니다.
- '이메일 형식과 필수 필드를 확인' — 확인할 수 없습니다. 이것은 의도를 서술하지 동작을 서술하지 않습니다. 그 정규식이 실제로 무엇을 허용하고 무엇을 거부하는지? 이 문장은 그것에 대해 아무 말도 하지 않습니다.

경계선은 어디일까요? **증거는 다른 사람이 똑같은 방식으로 다시 돌려 볼 수 있는 것입니다.** 명령과 그 원본 출력, 종료 코드, 실패한 테스트 이름 목록, 스크린샷, 변경 전후의 수치 비교. **주장은 믿을지 말지 고를 수밖에 없는 것입니다.** '로직은 올바릅니다', '문제없을 겁니다', '이미 최적화했습니다', '다시는 일어나지 않을 겁니다'.

공식 문서는 정확히 이 선을 긋습니다. Claude가 성공을 주장하게 하지 말고 증거를 보여 주게 하라 — 테스트 출력, 실행한 명령과 그것이 돌려준 것, 또는 결과의 스크린샷. 증거를 검토하는 편이 검증을 직접 다시 돌리는 것보다 빠르고, 여러분이 지켜보지 않았던 세션에도 통합니다[^S4].

마지막 반 문장이 핵심입니다. 처음부터 끝까지 지켜보고 있었다면 '주장 대 증거'의 구별은 별 값어치가 없습니다 — 직접 봤으니까요. 하지만 눈을 뗀 순간, 대화 히스토리에 남는 것은 텍스트뿐이고, 텍스트 안에서 주장은 증거만큼이나 자신 있어 보입니다.

## 왜 에이전트에서 이 문제가 특히 두드러지는가

'맞아 보이는데 틀렸다'는 전통적인 소프트웨어에서도 봅니다. 왜 에이전트에서는 레슨 하나를 통째로 쓸 만한 문제가 될까요?

전통적인 테스트가 에이전트는 만족시키지 못하는 전제 위에 서 있기 때문입니다.

정의부터 봅시다. 컴퓨팅에서 결정론적 시스템은 동일한 입력이 주어지면 매번 동일한 출력을 내놓지만, 비결정론적 시스템 — 에이전트 같은 — 은 같은 시작 조건에서도 서로 다른 응답을 생성할 수 있습니다[^S3]. 이것은 '버그가 있어서 불안정하다'가 아닙니다. 원래 그렇게 동작하는 것입니다. 프롬프트에서 아무것도 바꾸지 않아도 두 실행의 결정이 일치한다는 보장은 없습니다[^S2].

그래서 전통적인 평가의 전제가 무너집니다. 전통적인 평가는 흔히 AI가 매번 같은 단계를 따른다고 가정합니다. 입력 X가 주어지면 시스템은 경로 Y를 따라 출력 Z를 내놓아야 한다는 것입니다[^S2]. 멀티 에이전트 시스템은 이렇게 동작하지 않습니다. **시작점이 동일해도 에이전트는 목표에 도달하기 위해 완전히 다른, 그러나 유효한 경로를 택할 수 있습니다** — 한 에이전트는 출처 세 개를 검색하고 다른 에이전트는 열 개를 검색할 수 있고, 같은 답을 찾는 데 서로 다른 도구를 쓸 수도 있습니다[^S2].

구체적으로는 이렇게 보입니다.

```text
같은 작업, 같은 프롬프트, 두 번의 실행

실행 1: read_file(schema.sql) → grep("user_id") → edit(models/user.js)
        → run_tests → 완료

실행 2: list_dir(src/) → read_file(models/user.js) → read_file(models/order.js)
        → edit(models/user.js) → edit(models/order.js) → run_tests
        → run_tests → 완료
```

어느 쪽 궤적도 '틀렸다'고 부를 수 없습니다. 두 번째 실행은 파일을 하나 더 읽고, 한 군데를 더 고치고, 테스트를 두 번 돌렸습니다 — 돌아간 것일 수도 있고, 첫 번째 실행이 놓친 결합을 잡아낸 것일 수도 있습니다. '반드시 schema.sql 을 먼저 읽어야 한다'는 단언을 쓰면 두 번째 실행은 실패합니다 — 그런데 두 번째 실행이 더 나은 일을 했을 수도 있습니다.

**미리 정해 둔 각본에 궤적을 대조하는 방식은 여기서 통하지 않습니다.** 올바른 단계가 무엇인지 늘 아는 것이 아니기 때문에, 미리 규정해 둔 '올바른' 단계를 에이전트가 따랐는지 확인하는 것만으로는 대개 충분하지 않습니다[^S2].

층을 하나 더 얹습니다. 에이전트 시스템에서 에러는 **복리로 불어납니다**. 전통적인 소프트웨어에서는 사소한 버그가, 에이전트에 닿으면 작업 전체를 탈선시킬 수 있습니다 — 한 단계의 실패가 에이전트를 완전히 다른 궤적으로 탐색하게 만들고, 예측 불가능한 결과로 이어집니다[^S2]. 이것은 '어떤 함수가 잘못된 값을 돌려주고 그것이 위로 전파된다'는 전통적인 프로그램과는 다릅니다. 에이전트는 잘못된 결과를 받아 **그 잘못된 결과를 근거로 새로운 결정을 내립니다.** 파일을 잘못 읽으면 '이 모듈은 존재하지 않는다'고 결론 내리고 새 모듈을 만들 수 있고, 그다음에는 그 새 모듈을 중심으로 계속 작업합니다. 최종 출력을 볼 때쯤이면 에러는 처음 있던 자리에 더 이상 없습니다. 다른 무언가로 자라 있습니다.

Anthropic 자신의 결론도 여기에 닿습니다. 에이전트의 자율적인 성격은 더 높은 비용과 에러가 복리로 불어날 가능성을 뜻하므로, 샌드박스 환경에서의 광범위한 테스트를 적절한 가드레일과 함께 권장합니다[^S1]. 하나 더, 더 직접적인 문장이 있습니다. LLM은 잠재적으로 여러 턴에 걸쳐 동작하므로, 여러분은 그 의사 결정에 어느 정도의 신뢰를 두어야 합니다[^S1].

'어느 정도의 신뢰'라는 표현에 주목하세요. '신뢰해야만 한다'고 말하는 것이 아닙니다. 이 신뢰가 어딘가에서 와야 한다고 말하는 것입니다. 그리고 신뢰의 출처는 둘뿐입니다. 여러분이 직접 지켜봤거나(그렇다면 에이전트는 여러분의 시간을 하나도 아껴 주지 않았습니다), 무언가가 여러분 대신 지켜봤거나. 이 코스 전체는 두 번째에 대한 이야기입니다.

```agentmentor-check
{
  "id": "vq-zh-01-plausible-not-correct",
  "label": "이 완료 보고를 믿을 것인가",
  "prompt": "빌링 모듈에 '통화별 금액 분할' 함수를 추가해 달라고 에이전트에 요청했습니다. 스무 번 남짓의 도구 호출을 거쳐 마침내 이렇게 보고합니다. '완료했습니다. 구현은 요구사항을 충족합니다. 실행 중 에러는 없었습니다.' diff를 훑어보니 구조는 합리적이고, 이름은 관례를 따르며, 눈에 띄는 문제는 없습니다. 프로젝트에 테스트 스위트가 있지만 에이전트는 그것을 돌리지 않았고 여러분도 돌리지 않았습니다. 다음에 무엇을 해야 할까요?",
  "whyHere": "레슨 전반부는 에이전트가 왜 '완료된 것처럼 보인다'에서 멈추는지 막 설명했고, 후반부는 실행할 수 있는 검사를 어떻게 쥐여 줄지 보여 줍니다. 이 문항은 그 중간에 놓여, 원리로 넘어가기 전에 구체적인 판단을 한 번 내리게 합니다. 판단을 먼저 내려야 원리가 붙습니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "과정에 에러가 없었고 diff도 괜찮아 보이니 머지한다. 진짜 문제가 있으면 하류에서 드러날 것이다.",
      "correct": false,
      "feedback": "아닙니다. '에러 없음'은 호출한 도구들이 예외를 던지지 않았다는 뜻이지 출력이 올바르다는 뜻이 아닙니다 — 모든 도구가 성공을 돌려주면서 결과는 완전히 틀린 상황은 얼마든지 함께 성립합니다. '하류에서 드러날 것이다'에 대해서는, 이것이 바로 여러분 자신이 검증 루프가 되는 모습이며, 다만 검증의 순간이 장애가 난 뒤로 미뤄졌을 뿐입니다. 빌링 금액이라면 하류에서 드러날 때쯤에는 보통 회계팀이 이미 묻고 있습니다."
    },
    {
      "id": "b",
      "text": "먼저 테스트 스위트를 돌려 이 변경에 통과/실패 결과를 붙이고, 그다음에 믿을지 정한다.",
      "correct": true,
      "feedback": "정답입니다. 이 한 걸음의 값어치는 '테스트를 돌렸다'가 아닙니다. 감으로만 판단할 수 있던 상황을 객관적인 결과가 있는 상황으로 바꿨다는 데 있습니다. 그리고 이 걸음은 앞당길 수 있습니다. 검사가 이미 있고 명령도 알고 있으니, 다음번에는 처음부터 에이전트에 이렇게 말해 두면 됩니다. '변경한 뒤에 npm test 를 실행해 줘. 전부 초록이 되면 보고해 줘.' 그러면 결과를 읽는 사람은 여러분이 아닙니다."
    },
    {
      "id": "c",
      "text": "에이전트에 한 번 더 스스로 점검해 보라고 시키고, 문제없다고 하면 머지한다.",
      "correct": false,
      "feedback": "아닙니다. 흔한 직관이지만 방향이 반대입니다. 작업을 한 모델이 심판까지 맡아서는 안 됩니다 — 공식 문서의 조언은 깨끗한 컨텍스트를 가진 새 모델이 결과를 반박해 보게 하라는 것입니다. 스스로 점검할 때 모델이 보는 것은 방금 자기가 쓴 추론이고, 그 추론은 이미 자기 안에서 앞뒤가 맞았습니다 — 아니었다면 애초에 그렇게 쓰지 않았을 것입니다. '다시 봐 달라'고 시키면 대개 같은 판단을 더 자신 있는 어조로 반복할 뿐입니다."
    }
  ]
}
```

## 출구: 스스로 실행할 수 있는 검사를 쥐여 주기

지금까지의 밑작업은 한 문장으로 모입니다. **Claude에게 스스로 실행할 수 있는 검사를 쥐여 주세요** — 테스트, 빌드, 비교할 스크린샷. 그것이 지켜봐야 하는 세션과 자리를 떠도 되는 세션의 차이입니다[^S4].

그 차이는 어디서 생길까요? 통과냐 실패냐를 내놓는 것을 Claude에게 주면 루프가 스스로 닫힙니다. Claude가 작업을 하고, 검사를 돌리고, 결과를 읽고, 검사가 통과할 때까지 반복합니다[^S4].

이 문장은 이 시리즈 7번째 코스에서 다룬 하네스 루프에 그대로 대응시킬 수 있습니다. 먼저 지금의 루프가 어디서 멈추는지 보세요.

```javascript
let response = await client.messages.create({ tools, messages });
while (response.stop_reason === "tool_use") {
  messages.push({ role: "assistant", content: response.content });
  const toolResults = await runToolUses(response.content);
  messages.push({ role: "user", content: toolResults });
  response = await client.messages.create({ tools, messages });
}
// 루프는 여기서 멈춘다: stop_reason 이 "tool_use" 에서 "end_turn" 으로 바뀌었다
```

`end_turn` 은 무슨 뜻일까요? 모델이 이번 턴에 할 말을 다 했다고 여긴다는 뜻입니다. **그게 전부입니다.** 작업이 올바르다는 뜻이 아니고, 응답이 완결됐다는 보장조차 아닙니다 — 이 루프는 `tool_use` 만 알아보므로 `stop_reason` 이 그 밖의 무엇이 되든 빠져나갑니다. 출력이 `max_tokens` 때문에 문장 중간에서 잘린 경우까지 포함해서요. 종료 조건 안에 '출력의 품질'과 관련된 것은 아무것도 없습니다.

그러면 검사를 연결한다는 것은 어떤 모습일까요? 유효한 자리는 두 곳입니다.

자리 1, 검사를 에이전트가 호출할 수 있는 도구로 만들어 루프 안쪽에서 돌리기.

```javascript
const tools = [
  ...editTools,
  {
    name: "run_checks",
    description:
      "importer 모듈의 테스트 스위트를 실행한다. 종료 코드와 실패한 테스트 이름을 돌려준다. " +
      "src/importer/ 아래의 파일을 수정했다면 반드시 한 번 호출해야 한다.",
    input_schema: { type: "object", properties: {}, required: [] },
  },
];
```

자리 2, 루프가 빠져나온 뒤에 게이트를 두기. 자기 보고를 믿지 말고 직접 돌리기.

```javascript
// while 루프는 끝났지만, 아직 완료로 취급하지 않는다
messages.push({ role: "assistant", content: response.content }); // 모델의 마무리 답변도 히스토리에 넣는다
let verdict = await runChecks(); // { exitCode, failed: ["BOM 헤더 파싱", ...] }

while (verdict.exitCode !== 0) {
  messages.push({
    role: "user",
    content: `검사가 통과하지 못했어. 실패한 테스트: ${verdict.failed.join(", ")}. 고치고 다시 보고해 줘.`,
  });
  response = await client.messages.create({ tools, messages });
  // …위의 tool_use 루프로 돌아가, 다시 마무리할 때까지…
  messages.push({ role: "assistant", content: response.content }); // 마무리 답변은 매번 히스토리에 넣는다
  verdict = await runChecks();
}
```

코드 자체에 묘수는 없습니다. 핵심은 **종료 조건의 주인이 바뀌었다**는 것입니다. '모델이 더는 도구를 부르고 싶지 않다고 말했다'에서 '결정론적인 코드가 0을 돌려줬다'로. 앞의 것은 모델의 자기 평가입니다. 뒤의 것은 아닙니다.

그러면 '검사'는 무엇일 수 있을까요? 공식 문서가 제시하는 범위는 생각보다 넓습니다. 검사란 Claude가 대화 안에서 읽을 수 있는 신호를 돌려주는 것 전부입니다 — 테스트 스위트, 빌드의 종료 코드, 린터, 출력을 픽스처와 diff하는 스크립트, 또는 디자인과 비교하는 브라우저 스크린샷[^S4].

'픽스처'를 설명해 두겠습니다. 미리 저장해 둔 '모범 답안 파일'로, 실행 후 출력을 그것과 비교하며 한 글자도 달라서는 안 되는 방식입니다. 무디게 들리지만 '출력 형식이 안정적이어야 한다'는 종류의 작업에서는 가장 간단하고 가장 믿을 만한 검사의 형태입니다.

이 사고방식은 에이전트 실행에 대한 Anthropic의 권고와 일치합니다. 실행 중에 에이전트가 각 단계에서 환경으로부터 "ground truth" — 도구 호출 결과나 코드 실행 같은 — 를 얻어 자신의 진척을 평가하는 것이 결정적으로 중요합니다[^S1]. '환경으로부터'에 주목하세요 — 자기 자신의 추론으로부터가 아닙니다. 모델의 추론은 스스로 생성한 것입니다. 환경의 반환값은 그렇지 않습니다.

## 남은 다섯 레슨이 푸는 것

'실행할 수 있는 검사를 쥐여 준다'를 본줄기로 삼으면 남은 질문들이 구체적으로 바뀝니다.

**레슨 2: 무엇을 검증할 것인가.** 미리 정한 각본에 궤적을 대조하는 방식이 통하지 않는다면 무엇을 확인해야 할까요? 답은 종료 상태 우선입니다. 특정 프로세스를 따랐는지가 아니라 올바른 최종 상태에 도달했는지를 평가하고, 복잡한 워크플로라면 특정 상태 변화가 일어났어야 하는 개별 체크포인트로 평가를 쪼갭니다[^S2]. 이 레슨에서는 흐릿한 요구사항을 측정 가능한 성공 기준으로 바꾸는 방법도 다룹니다.

**레슨 3: 결정론적 검증기.** 통과/실패를 낼 수 있는 검사를 어떻게 고르고 어떻게 쓸지. 정확 일치, 스크립트 비교, 테스트 스위트 — 무엇이 어디에 맞는지, 그리고 직관에 반하는 함정 하나: 지나치게 엄격한 검증기는 올바른 답을 거부합니다. 구체적인 검증기 목록과 우선순위는 그 레슨에 있으니 여기서 펼치지는 않습니다.

**레슨 4: LLM 판정자.** 자유 형식 텍스트는 문자열 비교를 쓸 수 없으니 모델에 점수를 매기게 해야 합니다. 루브릭을 어떻게 쓰는지, 출력 형식을 어떻게 제약하는지, 추론을 먼저 할지 점수를 먼저 낼지, 그리고 작업을 한 모델이 자기 답을 채점하면 안 되는 이유 — 마지막 것은 앞의 퀴즈에서 이미 건드렸습니다. 구체적인 루브릭 설계는 레슨 4에 있습니다.

**레슨 5: 평가 세트.** 검사 하나는 작업 하나를 담당하고, 작업의 묶음이 평가 세트가 됩니다. 실제 사용에서 사례를 어떻게 모으는지, 엣지 케이스를 어떻게 채우는지, 홀드아웃 세트가 무엇을 하는지, 그리고 '몇 개면 충분한가' — 모두 레슨 5에서 답합니다. 답은 생각보다 작을지도 모릅니다.

**레슨 6: 직접 만들기.** 앞의 다섯 레슨을 하나로 잇습니다. 평가 작업 하나에 하네스 루프 하나를 붙여 돌리고 리포트를 냅니다. 프롬프트의 버전을 하나 바꿔 점수가 움직였는지 봅니다.

## 비례성: 사소한 일까지 검수 절차로 감싸지 않기

여기까지 오면 반대쪽 극단으로 기울기 쉽습니다. 모든 작업에 테스트와 판정자와 평가 세트가 필요하다고 여기는 것입니다. 그렇지 않습니다.

Anthropic의 원문은 이렇습니다. **다른 모든 LLM 기능과 마찬가지로 성공의 열쇠는 성능을 측정하고 구현을 반복 개선하는 것이며, 다시 말하지만 복잡성은 그것이 결과를 눈에 보이게 개선할 때에만 더할 것을 고려해야 합니다**[^S1]. 같은 글에는 더 구체적인 경로 권고도 있습니다. 단순한 프롬프트로 시작해 종합적인 평가로 그것을 최적화하고, 더 단순한 해법으로 부족할 때에만 여러 단계의 에이전트 시스템을 더하라는 것입니다[^S1].

검증에 적용하면 판단 기준은 몇 줄로 줄어듭니다.

- **이 작업은 반복해서 돌아가는가?** 한 번 쓰고 버리는 스크립트, 즉석 데이터 처리, 3분이면 끝나고 지켜볼 작정인 일 — 검수 장치를 세우는 것은 손해입니다. 반복해서 돌아가거나, 다른 사람이 고치거나, 여러분이 자리에 없을 때 돌아가는 것 — 값어치가 있습니다.
- **에러의 비용은 누가 지는가?** 오타를 잘못 고쳤다면 스스로 되돌리면 그만입니다. 빌링 로직을 깨뜨리면 회계팀이 비용을 집니다. 비용이 하류로 갈수록, 되돌리기 어려울수록, 앞단에서 막아야 합니다.
- **지금 그것을 검증하는 데 시간을 얼마나 쓰는가?** 매번 손으로 페이지 세 개를 열어 비교해야 한다면, 그 세 페이지 비교를 스크립트로 만드는 것이 자동화할 값어치가 가장 큰 일입니다 — 이미 그 비용을 치르고 있는데, 알아채지 못했을 뿐입니다.

따로 짚어 둘 값어치가 있는 경우가 하나 더 있습니다. **이미 가지고 있는 검사를 에이전트에 연결하지 않았을 뿐인 경우입니다.** 프로젝트에 있는 그 테스트 스위트, 그 lint 명령, 그 빌드 스크립트 — 대개 전부터 존재했을 것입니다. 그것들을 작업 설명에 적어 넣거나 도구로 만드는 비용은 거의 0인데, 세션의 성격은 달라집니다. 이것이 ROI가 가장 높은 한 걸음이고, 이 코스의 다음 몇 레슨의 출발점이기도 합니다.

## 💻 연습

<!-- exercises -->

### 레벨 1: 주장과 증거 구별하기

아래는 서로 다른 세 세션에서 나온 에이전트 완료 보고 세 건입니다. 각각에 대해 어느 부분이 주장이고 어느 부분이 증거인지 판단하세요. 그다음, 각 보고를 '검수 통과'로 취급하기 전에 **구체적으로 어떤 증거가 아직 빠졌는지** 쓰세요.

글로 답하면 되고 코드는 필요 없습니다.

**보고 A**

```text
완료했습니다. src/importer.js 를 리팩터링해 필드 매핑을 mapFields() 라는
독립 함수로 추출했고, 로직이 전보다 훨씬 명확해졌습니다. 변경은 기존 동작에
영향을 주지 않았습니다.
```

**보고 B**

```text
완료했습니다. npm test -- importer 를 실행한 출력은 이렇습니다.

  PASS  test/importer.test.js
  Tests: 14 passed, 14 total
  Time:  1.842 s

새로 추가한 테스트 케이스 3개는 "handles BOM in header", "rejects duplicate
email", "errors on missing column" 입니다. 전체 테스트 스위트는 돌리지 않았고
importer 그룹만 돌렸습니다.
```

**보고 C**

```text
완료했습니다. BOM 헤더 때문에 필드 매핑이 실패하던 문제를 고쳤습니다. 코드를
다시 확인했고 로직은 올바르며, 비슷한 문제가 다시 나오지는 않을 것입니다.
하는 김에 파싱 성능도 조금 최적화했습니다.
```

<!-- rubric -->

- 세 건 모두 '주로 주장 / 주로 증거' 판정이 있고, 결과는 B가 주로 증거(셋 중 검수 통과에 가장 가까운 유일한 건), A와 C는 주로 주장
- 보고마다 빠진 증거를 최소 하나 **구체적으로** 짚어낼 것 — 명령과 그 원본 출력, 실패에서 통과로 바뀐 비교, 변경 전후 수치 같은 형태로. 막연한 '테스트가 필요하다'는 해당하지 않음
- C의 '코드를 다시 확인했다'가 작업을 한 모델이 자기를 채점하는 것이라 독립적인 검증으로 치지 않음을 지적하고, '성능도 조금 최적화했다'가 전후 기준 데이터가 하나도 없는 범위 밖 변경임을 알아챌 것
- 보고 A에서 가장 위험한 문장 하나를 짚어낼 것: '변경은 기존 동작에 영향을 주지 않았습니다' — 완전히 검증 가능한 주장인데(리팩터링의 정의가 동작 불변) 보고는 검증을 하나도 내놓지 않으며, 검증 가능한 서술을 주장으로 말하면 순전한 빈말보다 오히려 통과시키기 쉬움. 결론처럼 들리기 때문

<!-- answer -->

**보고 A: 거의 전부 주장입니다.**

- '필드 매핑을 `mapFields()` 라는 독립 함수로 추출했다' — 약한 증거입니다. 함수가 존재하는지, 이름이 무엇인지는 diff에서 확인할 수 있지만, 그것이 증명하는 것은 변경이 일어났다는 사실뿐이지 그 변경이 올바르다는 것은 아닙니다.
- '로직이 전보다 훨씬 명확해졌다' — 주장입니다. 미적 판단이고, 검증할 수 없으며, 검수가 관심 가져야 할 것도 아닙니다.
- '변경은 기존 동작에 영향을 주지 않았다' — **이것이 가장 위험한 문장입니다.** 완전히 검증 가능한 주장인데(리팩터링의 정의가 동작 불변입니다) 보고는 검증을 하나도 내놓지 않습니다. 검증 가능한 서술을 주장으로 말하면 순전한 빈말보다 사람을 더 쉽게 지나칩니다. 결론처럼 들리기 때문입니다.

검수까지 아직 빠진 것:

1. 리팩터링 전과 후의 테스트 출력. 두 벌 다 필요하고, 테스트 개수와 통과 개수가 그대로임을 보여야 합니다(이것이 '동작 불변'의 최소 증거입니다)
2. `importer.js` 에 애초에 테스트 커버리지가 없었다면 그것을 먼저 밝혀야 합니다 — '테스트가 없어서 동작 불변을 증명할 수 없다'는 정직한 상태이고, '변경은 기존 동작에 영향을 주지 않았습니다'보다 훨씬 쓸모 있습니다
3. 변경 범위: `git diff --stat` 으로 `src/importer.js` 만 건드렸고 다른 것을 곁다리로 고치지 않았음을 확인

**보고 B: 주로 증거입니다.**

- `npm test -- importer` 명령과 원본 출력 — 이것은 그대로 다시 돌려 볼 수 있습니다. 표준적인 증거의 형태입니다.
- 새 테스트 이름 세 개가 나열되어 있어 어떤 방향을 다루는지 볼 수 있습니다(BOM, 중복 이메일, 누락된 열).
- '전체 테스트 스위트는 돌리지 않았고 importer 그룹만 돌렸습니다' — 이 문장은 값어치가 있습니다. 하지 않은 것을 먼저 밝히고 있습니다. 보고에 'X는 하지 않았다'가 들어 있다면 대개 자기 경계에 대한 개념이 있다는 뜻입니다.

검수까지 아직 빠진 것:

1. 전체 테스트 스위트 결과. 로컬 통과는 이 변경이 다른 모듈을 깨뜨렸을 가능성을 배제하지 못합니다 — 복리로 불어나는 에러가 숨기 딱 좋은 자리입니다
2. 테스트 이름은 이름일 뿐입니다. `"handles BOM in header"` 가 안에서 실제로 무엇을 단언하는지는 소스나 더 자세한 출력을 봐야 합니다. 이름만 맞고 아무것도 단언하지 않는 테스트도 초록으로 지나갑니다
3. 엄밀히는 '먼저 빨간 것을 봤다'는 기록도 있어야 합니다. 수정을 되돌려 그 세 테스트가 실패하는지 확인하는 것입니다. 한 번도 빨간 적 없는 테스트는 자기가 무엇을 테스트하는지 증명하지 못합니다

**보고 C: 주장, 그리고 검증되지 않은 범위 밖 변경 하나.**

- 'BOM 헤더 때문에 필드 매핑이 실패하던 문제를 고쳤다' — 주장입니다. 무엇을 했는지 말하지만 결과가 어떻게 됐는지는 말하지 않습니다.
- '코드를 다시 확인했고 로직은 올바르다' — 작업을 한 모델이 자기를 채점하고 있습니다. 다시 확인할 때 모델이 읽는 것은 방금 자기가 생성한 추론이고, 그 추론은 이미 자기 안에서 앞뒤가 맞았습니다. 이 문장은 독립적인 검증으로 치지 않습니다.
- '비슷한 문제가 다시 나오지는 않을 것이다' — 주장이고, 정보량이 가장 적은 종류입니다. '않을 것이다', '다시는' 같은 표현에는 검증 가능한 내용이 하나도 없습니다.
- '하는 김에 파싱 성능도 조금 최적화했다' — 범위 밖 변경입니다. 전후 수치도 없고, 어디를 어떻게 바꿨는지조차 말하지 않습니다. 여기의 위험은 고친 버그보다 큽니다. 버그 수정은 적어도 대상이 분명하지만 '곁다리 최적화'는 그렇지 않습니다.

검수까지 아직 빠진 것:

1. BOM 문제를 재현하는 테스트 케이스와, 그것이 실패에서 통과로 바뀐 비교 출력('고쳤다'를 증명할 수 있는 유일한 것)
2. 성능 주장의 기준 데이터. 같은 입력으로 돌린 전후 시간과 측정 방법. 그것을 낼 수 없다면 올바른 한 수는 이 변경을 되돌리고 따로 제출하는 것이지, 버그 수정에 묶어 머지하는 것이 아닙니다
3. 전체 테스트 스위트 결과 — BOM 수정은 보통 파싱 진입점을 건드리므로 다른 분기에 실수로 영향을 주기 가장 쉽습니다

**세 건을 한 문장으로 정리하면**: 판단 기준은 언제나 같습니다. **코드를 읽지 않고, 보여 준 것만으로 이 문장을 확인할 수 있는가?** 확인할 수 있으면 증거, 믿을지 말지 고를 수밖에 없으면 주장입니다. B가 가장 나은 이유는 길어서가 아니라, 붙여 넣은 것을 그대로 다시 돌려 볼 수 있기 때문입니다.

<!-- hint -->

문장마다 같은 질문을 던지세요. **코드를 열지 않고, 여기 붙여 넣은 것만으로 이것을 확인할 수 있는가?** 그렇다면 증거이고, 믿을지 말지만 고를 수 있다면 주장입니다. 문장의 어조에 휘둘리지 마세요 — '변경은 기존 동작에 영향을 주지 않았습니다'는 아주 확신에 차 들리지만, 여러분이 검증할 수 있는 것은 하나도 주지 않았습니다.

<!-- hint -->

'에러 없음', '로직은 올바르다', '일어나지 않을 것이다', '이미 최적화했다' — 이 표현들이 공유하는 것이 하나 있습니다. 주어가 언제나 자기 작업에 대한 모델 자신의 평가라는 점입니다. 뒤집어 보세요. 실제로 검증 가능한 것은 어떤 모습일까요? 대개 명령줄, 종료 코드, 테스트 이름, 소요 시간, 줄 수 같은 구체적인 것을 달고 있고, 다른 사람이 똑같이 다시 돌려 볼 수 있습니다. 보고 A에서 '검증할 수 있는데 검증하지 않은' 문장에 특히 주의하세요 — 순전한 빈말보다 방어선을 더 쉽게 지나갑니다.

### 레벨 2: 작은 작업에 증거 체크리스트 설계하기

작업은 이렇습니다.

> `data/contacts.csv` 에서 `email` 열을 기준으로 중복 행을 제거하되, 각 이메일의 **첫 번째 등장**만 남기고, 결과를 같은 파일에 다시 써 주는 스크립트를 작성해 줘.

이 작업을 에이전트에 넘겼고, 실행됐고, '완료했습니다, 중복을 제거했습니다'라고 보고했다고 합시다.

**증거 체크리스트**를 설계하세요. 이 작업을 검수하려면 무엇을 봐야 할까요? 항목마다 그것이 어떤 형태를 취하는지 분명히 쓰세요(명령과 그 출력인가? 변경 전후 비교인가? 파일인가?). 그다음 두 번째 질문에 답하세요. **이 중 어느 것이 루프를 스스로 닫게 할 수 있는가** — 즉 여러분이 자리에 없어도 에이전트가 실행하고, 결과를 읽고, 통과할 때까지 반복할 수 있는 것은 어느 것인가.

의사 코드나 명령 예시로 답해도 되고, 완전한 코드는 필요 없습니다.

<!-- rubric -->

- 체크리스트에 '명령과 그 원본 출력' 형태의 항목이 최소 하나 있고, 변경 **전과 후에** 각각 무엇을 볼지 분명히 쓸 것. 변경 후 결과만 있는 것은 해당하지 않음
- 어느 검사가 통과/실패를 내어 루프를 닫게 하는지 명시하고, 근거가 '이것이 가장 중요해서'가 아니라 '사람이 해석할 필요가 없고 기대값이 분명해서'에 닿을 것. 아울러 지표 하나만으로는 속일 수 있음을 잡아낼 것 — '중복 개수 0' 같은 지표는 파일을 비워 버려도 달성되므로, 루프를 진짜로 닫는 검사는 '중복이 없다, 첫 번째 등장을 남겼다, 행 수가 기대와 맞는다'를 하나의 종료 코드로 묶어야 하며 어느 하나에 기대서는 안 됨
- '중복이 더 없다' ≠ '제대로 했다'임을 알아차릴 것. 체크리스트에 실수로 지운 것을 막는 항목이 최소 하나 있을 것(남은 행이 첫 번째 등장인지 확인, 중복이 아닌 행은 손대지 않았는지 확인, 또는 되돌릴 백업이나 `git diff` 가 있는지 등)

<!-- answer -->

**증거 체크리스트**

**1. 변경 전후의 행 수**

```text
변경 전: wc -l data/contacts.csv   →  1204
변경 후: wc -l data/contacts.csv   →  1187
```

두 숫자가 다 필요합니다. 변경 후 숫자만으로는 의미가 없습니다 — 무엇에서 바뀐 것인지 알 수 없으니까요. 헤더 행이 세어졌는지도 함께 보세요. 그 한 줄의 어긋남이 검수에서 가장 놓치기 쉽습니다.

**2. 중복 이메일 개수(기대값: 0)**

```text
cut -d, -f2 data/contacts.csv | tail -n +2 | sort | uniq -d | wc -l
변경 전 → 17
변경 후 → 0
```

핵심은 이 명령이 **에이전트가 쓴 스크립트와 독립적이라는 것**입니다. 자기 스크립트를 다시 돌려 자기 정확성을 증명한다면 아무것도 증명하지 못합니다 — 중복 제거 스크립트의 버그는 검사에도 똑같이 나타납니다. 다른 도구 묶음으로 계산해 나온 0, 그것이 증거입니다.

**3. 남은 행이 정말 첫 번째 등장인가**

원본 파일에서 중복이 있던 이메일 하나를 골라 그 모든 등장을 줄 번호와 함께 붙이고, 그다음 결과 파일에 남은 한 행을 붙이세요.

```text
변경 전:
  grep -n "minsu.kim@example.com" data/contacts.csv
  42:1042,minsu.kim@example.com,김민수,2024-03-11
  877:1877,minsu.kim@example.com,김민수(중복 임포트),2025-01-20

변경 후:
  grep -n "minsu.kim@example.com" data/contacts.csv
  42:1042,minsu.kim@example.com,김민수,2024-03-11
```

남은 것은 877번 줄이 아니라 42번 줄입니다. 이 검사 하나는 '중복 개수 0'으로는 잡을 수 없습니다 — 두 행 중 어느 쪽을 지워도 중복 개수는 0이 되니까요.

**4. 중복이 아닌 행은 손대지 않았는가**

```text
git diff --stat data/contacts.csv
 data/contacts.csv | 17 -----------------
 1 file changed, 17 deletions(-)
```

삭제만 있고 추가는 없으며, '수정'도 없습니다(수정은 diff에서 삭제 한 줄과 추가 한 줄로 나타납니다). 이것이 '하는 김에 날짜 형식도 통일했다'거나 '공백을 다듬었다' 같은 범위 밖 변경을 막아 줍니다.

**5. 되돌릴 수단**

원본 파일에 다시 쓰는 것은 파괴적인 조작입니다. 백업 파일 경로(`data/contacts.csv.bak`)가 있거나, 이 파일이 이미 git에 있어서 `git checkout` 한 번으로 되돌릴 수 있어야 합니다. 이것은 '올바름을 증명한다'가 아니라 '틀렸을 때 되돌릴 수 있다'이지만, 검수에 속하는 것은 마찬가지입니다.

**어느 것이 루프를 스스로 닫게 하는가**

2번 항목이 가장 가깝지만, **그것만으로는 충분하지 않습니다.**

먼저 왜 가까운가. 목록에서 사람의 해석이 전혀 필요 없는 유일한 검사입니다. 명령 하나, 숫자 하나, 0이라는 분명한 기대값. 반환값이 그대로 통과/실패로 옮겨지니 에이전트는 돌리고 나면 통과했는지 압니다. 1번은 '17행이 줄어든 것이 타당한가'를 사람이 판단해야 하고, 3번은 텍스트 두 덩이를 사람이 눈으로 봐야 하고, 4번은 diff의 모양을 사람이 이해해야 하고, 5번은 검사조차 아닙니다. 스스로 판정할 수 있는 것은 2번뿐입니다.

이제 왜 충분하지 않은가. 그것은 '깨끗이 중복 제거되지 않았다'만 막을 뿐 '너무 많이 지웠다'는 막지 않습니다. `truncate -s 0 data/contacts.csv` 로 파일을 비우면 중복 개수는 여전히 0이고, 이 검사는 여전히 초록으로 통과합니다. **지표는 달성하고 작업은 망가뜨리기** — 바로 그 장면입니다.

그래서 올바른 접근은 2, 3, 4번을 하나의 검증 스크립트로 합쳐, 세 단언이 모두 통과해야 0으로 종료하게 만드는 것입니다.

```javascript
// scripts/check-dedupe.js
// 사용법: node scripts/check-dedupe.js <원본-파일-백업> <결과-파일>
// 종료 코드 0 = 통과, 1 = 실패

const before = readRows(process.argv[2]);
const after = readRows(process.argv[3]);
const failures = [];

// 단언 1: 결과에 중복 이메일이 없다
const emails = after.map((r) => r.email);
if (new Set(emails).size !== emails.length) {
  failures.push("결과 파일에 아직 중복 이메일이 있다");
}

// 단언 2: 남은 행이 각 이메일의 첫 번째 등장이다(엉뚱한 행을 지우는 것을 막는다)
const expected = [];
const seen = new Set();
for (const row of before) {
  if (seen.has(row.email)) continue;
  seen.add(row.email);
  expected.push(row);
}
if (JSON.stringify(expected) !== JSON.stringify(after)) {
  failures.push("남은 행이 '첫 번째 등장' 기대와 맞지 않는다");
}

// 단언 3: 행 수가 중복 제거 후 기대값과 정확히 같다(과다·과소 삭제를 막는다)
if (after.length !== expected.length) {
  failures.push(`행 수 불일치: 기대 ${expected.length}, 실제 ${after.length}`);
}

if (failures.length > 0) {
  console.error(failures.join("\n"));
  process.exit(1);
}
console.log("check-dedupe: 통과");
```

단언 2가 사실 단언 1과 3을 함의한다는 점을 눈여겨보세요 — 그래도 셋으로 나눠 보고하면 에이전트가 받는 실패 정보가 더 구체적이고 고칠 방향도 분명해집니다. 검사에 '어디가 틀렸는지 분명히 말하는' 능력을 쥐여 주는 것은 검사 그 자체만큼 중요합니다.

이 스크립트가 있으면 작업 설명은 이렇게 쓸 수 있습니다. '중복 제거 전에 원본 파일을 `data/contacts.csv.bak` 로 백업해 줘. 제거한 뒤에는 `node scripts/check-dedupe.js data/contacts.csv.bak data/contacts.csv` 를 실행해 줘. 종료 코드가 0이 아니면 통과할 때까지 계속 고쳐 줘.' 그러면 에이전트가 작업을 하고, 검사를 돌리고, 결과를 읽고, 통과할 때까지 반복합니다 — 그 전 과정에 여러분이 자리에 있을 필요가 없습니다[^S4].

남은 5번 항목(되돌릴 수단)은 스크립트 바깥에 두는 편이 낫습니다. 여러분의 하네스 코드가 작업 실행 전에 무조건 백업을 뜨는 것입니다 — 백업이 모델의 기억력에 기대게 해서는 안 됩니다.

<!-- hint -->

원본 파일에 다시 쓰는 파괴적인 조작이 이 문제의 숨은 암초입니다. '중복이 사라졌다'는 결과는 `truncate -s 0` 로 파일을 비워도 달성됩니다. 여러분의 체크리스트는 이 '지표는 달성하고 작업은 망가뜨리는' 경우를 막나요? 먼저 그 방향으로 생각해 보세요 — 무엇이 아직 빠졌나요?

<!-- hint -->

'어느 것이 루프를 스스로 닫게 하는가'를 고를 때 기준은 하나뿐입니다. **에이전트가 이 검사를 돌린 뒤, 사람에게 묻지 않고 통과 여부를 알 수 있는가?** 텍스트 두 덩이를 눈으로 봐야 하거나 '이 숫자가 타당한가'를 판단해야 하는 것은 해당하지 않습니다. 그리고 어느 항목도 혼자서는 자격이 안 된다면, 몇 개를 하나의 스크립트로 합쳐 함께 하나의 종료 코드를 내게 할 수 있는지도 생각해 보세요.

<!-- /exercises -->

## 정리

- Claude는 작업이 **완료된 것처럼 보이면** 멈춘다. 스스로 실행할 수 있는 검사가 없으면 '완료된 것처럼 보인다'가 손에 넣을 수 있는 유일한 신호이고, 여러분 자신이 검증 루프가 된다 — 모든 실수가 알아채 주기를 기다린다[^S4]
- 공식 문서는 이 간극에 이름을 붙인다. the trust-then-verify gap — Claude는 그럴듯해 보이지만 엣지 케이스를 처리하지 않는 구현을 내놓는다. 짝지어진 대책의 후반부도 똑같이 중요하다: **검증할 수 없다면, 배포하지 마라**[^S4]
- 주장과 증거를 가르는 선은 '다른 사람이 똑같은 방식으로 다시 돌려 볼 수 있는가'다. Claude가 성공을 주장하게 하지 말고 증거를 보여 주게 하라 — 테스트 출력, 실행한 명령과 그것이 돌려준 것, 결과의 스크린샷. 증거를 검토하는 편이 검증을 직접 다시 돌리는 것보다 빠르고, 지켜보지 않았던 세션에도 통한다[^S4]
- 에이전트는 비결정론적 시스템이다. 같은 시작 조건에서도 서로 다른 응답을 생성할 수 있고[^S3], 프롬프트가 같아도 실행 사이의 결정이 일치한다는 보장이 없다[^S2]. 그래서 '입력 X가 주어지면 경로 Y를 따라 출력 Z를 낸다'는 전통적인 평가의 전제가 무너진다[^S2] — 동일한 시작점에서 완전히 다르지만 유효한 경로가 나올 수 있다[^S2]
- 에이전트 시스템에서 에러는 복리로 불어난다. 한 단계의 실패가 완전히 다른 궤적을 탐색하게 만들고 예측 불가능한 결과로 이어진다[^S2]. 자율성은 더 높은 비용과 에러가 복리로 불어날 가능성을 데려오므로, 샌드박스 환경에서의 광범위한 테스트와 가드레일이 권장된다[^S1]
- 출구는 실행할 수 있는 검사를 쥐여 주는 것이다. 통과냐 실패냐를 내놓는 것이 있으면 루프가 스스로 닫힌다 — 작업을 하고, 검사를 돌리고, 결과를 읽고, 통과할 때까지 반복한다[^S4]. 검사는 테스트 스위트, 빌드의 종료 코드, 린터, 출력을 픽스처와 diff하는 스크립트, 또는 디자인과 비교하는 브라우저 스크린샷일 수 있다[^S4]
- 실행 중에는 에이전트가 각 단계에서 환경으로부터 "ground truth"(도구 결과, 코드 실행 결과)를 얻어 진척을 평가하게 하라. 자기 자신의 추론으로부터가 아니다[^S1]
- LLM은 잠재적으로 여러 턴에 걸쳐 동작하므로 그 의사 결정에 어느 정도의 신뢰를 두어야 한다[^S1] — 다만 그 신뢰는 어딘가에서 와야 한다
- 사소한 일까지 온전한 검수 장치로 감싸지 마라. 복잡성은 그것이 결과를 눈에 보이게 개선할 때에만 더할 것을 고려해야 한다[^S1]. 먼저 이 작업이 반복해서 돌아가는지, 에러의 비용을 누가 지는지, 지금 손으로 검증하는 데 시간을 얼마나 쓰는지를 보라
- ROI가 가장 높은 한 걸음은 대개 이것이다. 프로젝트에 있는 그 테스트 스위트, 그 lint 명령, 그 빌드 스크립트는 이미 존재한다 — 아직 에이전트에 연결하지 않았을 뿐이다

[>> 레슨 2: 무엇을 검증할 것인가: 종료 상태를 먼저, 프로세스는 보조로](./02-what-to-verify.md)
