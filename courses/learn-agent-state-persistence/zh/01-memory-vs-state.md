# 第 1 课：记忆之外还有状态

> 学习目标：
> - 用一句话区分「记忆」（喂给模型的上下文）与「执行状态」（harness 攥着的运行现场），并各举一个例子
> - 说出至少三条支撑「崩溃对长任务格外致命」的具体依据，并说清它们分别指向哪个环节
> - 给定一份运行中的信息清单，能判断每一项属于记忆、执行状态还是磁盘产出，以及进程崩溃后它是否还在
> 前置要求：完成本系列前 8 门课，理解 `stop_reason` 驱动的 harness 循环（第 7 门课）与上下文管理（第 8 门课） | 下一课 [第 2 课 >>](./02-checkpoint-anatomy.md)

## 第 23 轮，进程被杀

设想你在第 7 门课写的那个 harness，正跑一个要转 40 轮的长任务：读一批文件、逐个分析、边跑边把结论追加进报告。跑到第 23 轮，进程被杀了——可能是一次部署更新，可能是服务器掉电，也可能只是你手滑按了 Ctrl+C。

你去看磁盘：前 22 轮生成的报告文件都还在；`NOTES.md`（第 8 门课装的那份任务进展摘要）里也记着「已经分析到第几个文件、结论是什么」。看起来损失不大，重启进程接着跑就是了。

但你重启后才发现，`runAgent` 里那个 `messages` 数组是空的——它得从 `[{ role: "user", content: userInput }]` 重新开始拼。`turns` 计数器归零了。`tokensUsed` 也归零了。如果崩溃恰好发生在第 23 轮「模型点名要调工具」和「工具结果塞回 `messages`」之间，那次工具调用——不管它当时是刚开始跑还是已经跑完——现在也没了痕迹。

任务不是从第 23 轮接着跑，是从第 1 轮重新开始。磁盘上的文件都在，但 harness 自己那份「跑到哪儿了」的记录，一点没留下。

## 记忆是什么，状态是什么

这门课要讲的东西，得先跟一个容易混的概念划清界限。

**记忆**，是喂给模型的上下文内容——本系列第 5 门课讲跨会话怎么把它存下来、第 8 门课讲每一轮具体喂给模型看什么。它回答的问题是「模型看见了什么」。`NOTES.md` 就是记忆的一种载体：它已经写在磁盘上，下一轮、下一次会话，都可以被读回来塞进 prompt。

**执行状态**，是 harness 进程自己攥着的那份运行现场——`messages` 数组、`turns` 和 `tokensUsed` 这类计数器、还没落账的工具调用。它回答的问题是「harness 记得跑到哪儿了」。

这两者的关键差别不在「内容是什么」，而在「此刻它活在哪儿」。记忆可以已经落盘（`NOTES.md` 就摆在文件系统里，进程死不死都不影响它还在）；执行状态默认只活在进程的内存里——`let messages = [...]` 这行代码创建出来的东西，没有人专门把它写到磁盘之前，进程一退出就随内存一起被回收。上一节里 `messages`、`turns`、`tokensUsed`、悬空的工具调用，没有一样是自动落盘的。

```javascript
// 跑到第 23 轮那一刻，这几样东西只活在 node 进程的内存里
let state = {
  messages,        // 完整对话历史——恢复上下文的唯一来源
  turns,           // 当前第几轮
  tokensUsed,      // 累计用量
  pendingToolUse,  // 模型点了名、但结果还没塞回 messages 的工具调用
};
```

这份 `state`，本课就是要教你怎么把它变成能落盘、能恢复的东西。

## 为什么这对长任务是命门

先把账算清楚：Agent 可以长时间运行，跨很多次工具调用维持状态[^S1]——这正是第 23 轮那份 `messages` 越滚越大的原因。可这也意味着 Agent 是有状态的，而错误会复利[^S1]：状态越攒越多，一旦哪个环节出岔子，代价不是线性的，是叠加的。

再加一条：没有有效缓解手段的话，一次很小的系统故障，对 Agent 来说都可能是灾难性的[^S1]。「小」和「灾难性」这两个词摆在一起，说的正是第 23 轮这种情况——进程被杀本身只是个寻常的运维事件，可它把 22 轮的执行状态一次性清零，代价就被放大了。

出了错之后，不能干脆从头重启：重启是昂贵的，也会让用户沮丧[^S1]。所以要构建的是能从出错的地方接着跑的系统[^S1]——这也是为什么「记忆之外还有状态」不是一个学院派的概念区分，而是决定长任务能不能扛住中断的地基。

任务越长，这笔账越重：模型可能要连续跑很多轮，而你对它每一步决策的信任是有限度的[^S3]；自主运行的本质，天然带着更高的成本和误差累积的风险[^S3]。转的轮数越多，攒下的执行状态越厚，一次中断能抹掉的东西也越多。

## 崩溃不是稀罕事

第 23 轮那次「进程被杀」，听起来像个小概率的意外，但对一个要跑几十轮的长任务来说，中途被打断其实算不上稀奇。就连最正常不过的一次部署更新，都可能撞上运行中的 Agent——所以才需要专门用彩虹部署，把流量从旧版本逐步切到新版本，来避免打断正在运行的 Agent[^S1]，原因是：只要一部署，Agent 可能正处在它整个流程的任意一步[^S1]。

换句话说，连负责运维的人自己都默认「Agent 随时可能被打断」是会发生的事，要专门设计机制去规避它。你的 harness 没有理由假设自己会更幸运。

## 解药预告：本课的路线图

第 23 轮丢掉的那份执行状态，解法分五步，也是接下来五课的顺序：

1. **检查点**（第 2 课）——把 `messages`、`turns`、`tokensUsed`、悬空的工具调用这些执行状态，定期序列化写到磁盘上的一个检查点文件里。
2. **断点恢复**（第 3 课）——进程重启后，从检查点文件里把这份状态读回来，重建 `messages`，让循环从断的地方接着转，而不是从第 1 轮重来。
3. **副作用与幂等**（第 4 课）——恢复时最麻烦的不是「状态没了」，是「有些工具调用可能已经真的执行过了」：哪些工具重跑一次没关系，哪些必须防止重复执行。
4. **回退与分叉**（第 5 课）——检查点不只是用来救灾的，它还能让你回退到早先的现场重试，或者从某个节点分叉出另一条尝试路线。
5. **实战**（第 6 课）——把前面几课的机制，装进第 7 门课那个 harness 里，亲手跑一次「中途被杀，重启后接着跑到完成」的长任务。

有一份以「harness 工程」为主线的社区开源路线图，把这条路线概括成一句话：给每一步都建检查点，这样才能恢复、回退、分叉[^S5]——这只是社区文档对持久化这个组件职责的一句框架性概括，不是本课要逐字照搬的规范，但它点出的顺序，和上面五步基本对得上。

## 分寸：不是所有任务都需要这套

不是每个 Agent 都要背上检查点这套机制。加复杂度这件事，值得只在它确实能明显改善结果的时候才考虑[^S3]——一个几轮工具调用就能收尾的任务，`messages` 数组统共没几条，进程死了直接重跑，代价也就是重新问一次，犯不上专门设计一套落盘与恢复的机制。

真正用得上本课这套东西的，是像开头第 23 轮那样的场景：任务要跑几十轮、耗时可能是几分钟到几小时、中途累积了大量还没落盘的执行状态。判断要不要引入检查点，先问自己一句：如果现在被杀，重跑一遍的代价你能不能接受？接受不了，才轮到接下来几课登场。

```agentmentor-check
{
  "id": "sp-zh-01-restart-illusion",
  "label": "判断进程崩溃后能否直接重启重跑",
  "prompt": "你的 harness 正在跑一个 40 轮的长任务，第 23 轮进程被杀（比如撞上一次部署更新）。你检查磁盘：前 22 轮生成的报告文件都还在，`NOTES.md` 里也记着已完成的进展。你判断：这次崩溃没造成实质损失，重启进程、把任务从头跑一遍就行。这个判断站得住吗？",
  "whyHere": "刚讲完记忆与执行状态的物理位置差异——记忆可能已经落盘，执行状态默认只在进程内存里，「产出文件都在磁盘上=没丢东西」是这里最容易掉进去的直觉误区，必须当场纠正",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "站得住，报告文件和 NOTES.md 都在磁盘上，等于什么都没丢，重启重跑一遍就行",
      "correct": false,
      "feedback": "磁盘上有文件，不等于执行状态还在。`messages` 数组、`turns`、`tokensUsed` 这些东西是进程内存里的变量，没有人专门把它们序列化写到磁盘之前，进程一死就随内存一起被回收——它们和已经落盘的报告文件、`NOTES.md`，物理上活在两个地方，一个没了不代表另一个也在。"
    },
    {
      "id": "b",
      "text": "站不住，但真正丢的只有 tokensUsed 这个计数器，messages 数组和悬空的工具调用其实可以从已生成的报告文件反推出来",
      "correct": false,
      "feedback": "报告文件是任务运行到某一步的产出，不是运行过程本身的记录——它没留下模型每一轮具体说了什么、工具调用的完整参数和结果、崩溃那一刻是否正卡在某次工具调用中途。从产出反推不出这些细节，所以 messages 数组和悬空调用没法靠它补回来，只能靠专门的快照。"
    },
    {
      "id": "c",
      "text": "站不住，harness 攥着的运行现场（messages 数组、轮次与用量计数器、还没落账的工具调用）默认只活在进程内存里，进程一死就没了；重启代价高，而且从头重跑还可能让已经发生过的副作用重复一遍",
      "correct": true,
      "feedback": "对。出错不能从头重启，因为重启昂贵、也让人沮丧，这正是要构建能从出错处恢复的系统的原因。更麻烦的是「重来一遍」不只是浪费时间——如果前 22 轮里有工具已经真的执行过（比如发过一封邮件、改过一次数据），重新从第 1 轮跑，等于让这些动作再发生一次。这类副作用怎么处理，第 4 课细讲。"
    }
  ]
}
```

<!-- exercises -->
## 💻 练习

### Level 1：给六件运行中的「东西」分类

下面是一个正在运行的 harness 涉及的六样「东西」。请把每一样归到「记忆」「执行状态」「磁盘产出」三类之一，并说明：如果进程在这一刻被杀，它还在不在。

1. `NOTES.md` 里已经写好的任务进展摘要（第 8 门课装的那份）
2. 内存里的 `messages` 数组
3. 已经写到磁盘上的报告文件 `report.md`
4. `turns` 计数器（当前第几轮）
5. 模型刚生成的这一轮响应文字里，对项目结构的一段分析——还没来得及被写进 `NOTES.md`
6. 一个模型已经点名要调用、但工具还没跑完、结果也还没塞回 `messages` 的 `tool_use` 块

<!-- rubric -->
- 六项都给出分类，且大方向正确：`messages`、`turns`、悬空的 `tool_use` 归「执行状态」；已经落盘的 `NOTES.md` 摘要归「记忆」（并注明它已落盘），`report.md` 归「磁盘产出」——这两项都在磁盘上，但一个是要喂回给模型看的上下文、一个是任务产出的成果文件，分属不同类别；第 5 项归「记忆」但特别标注它还没落盘
- 能说清「记忆」不等于「已经落盘」——第 5 项证明了记忆本身也可能还停留在进程内存里、随进程一起丢失，这和执行状态的处境是一样的
- 崩溃后「在/不在」的判断准确：第 1、3 项还在；第 2、4、6 项没了；第 5 项也没了（因为它虽然是记忆内容，但还没来得及落盘）

<!-- answer -->
1. **记忆，且已落盘** —— `NOTES.md` 是喂给模型看的上下文载体，这份摘要已经写进文件，进程崩溃不影响它，重启后还能读回来。
2. **执行状态** —— `messages` 数组是 harness 自己攥着的对话历史，只活在进程内存里，崩溃后清零，要从头拼。
3. **磁盘产出** —— `report.md` 是任务运行出来的成果文件，已经落盘，崩溃后原样还在。
4. **执行状态** —— `turns` 是内存里的一个计数器变量，崩溃后归零，恢复时得知道该从第几轮接着算。
5. **记忆，但没落盘** —— 这段「对项目结构的理解」本质上和 `NOTES.md` 里的内容是一类东西（都是要喂给模型看的上下文），但它这一刻还只存在于模型这一轮的响应文字里，如果 harness 还没来得及把它写进 `NOTES.md`，进程一崩，这段理解也跟着没了。这说明「记忆」和「执行状态」的分类，不完全等于「在磁盘上」和「在内存里」——记忆也可能还没来得及落盘。
6. **执行状态** —— 这个悬空的 `tool_use` 块是「崩溃发生在工具执行与结果落账之间」的典型情况，只存在于内存里的 `messages` 或某个待处理队列中，崩溃后没有任何记录留下，第 3 课要专门处理这类断点。

<!-- hint -->
别只用「在磁盘上」还是「在内存里」当唯一的判断标准——第 5 项就是个反例：内容上属于记忆，但这一刻还没落盘。

<!-- hint -->
第 2、4、6 项有个共同点：它们都是 harness 循环运行时用来记录「跑到哪儿了」的东西，不专门设计一套落盘机制，进程死了它们就跟着没了。

### Level 2：给第 7 门课的 harness 列一份崩溃损失清单

回到开头的场景：第 7 门课那个 `runAgent`（用 `messages`、`turns`、`tokensUsed` 这几个变量驱动 `stop_reason` 循环）正在跑一个 40 轮的任务，第 23 轮进程被杀，而且崩溃恰好发生在「模型点名要调一个工具」和「工具结果塞回 `messages`」之间。不用写代码，只用文字完成两件事：

1. **崩溃损失清单**：这次崩溃具体丢了什么？哪些东西没受影响，还在？
2. **检查点该存什么**：如果这个 harness 装了检查点机制，你觉得检查点文件至少要存哪几个字段，才能把这次损失降到最低？列出字段名，并说明每个字段为什么要存。

<!-- rubric -->
- 损失清单里明确点出 `messages` 数组、`turns`、`tokensUsed`、以及那个正卡在工具执行与结果落账之间的 `tool_use` 调用，这几样内存里的东西全部丢失；同时认识到磁盘上已经生成的产出文件、`NOTES.md` 不受影响，还在
- 字段设想至少覆盖 `messages`、`turns`、`tokensUsed` 这三个能从 `runAgent` 代码里直接认出的现成变量，并各给出一句「为什么要存」的理由（比如 `messages` 是恢复对话上下文的唯一来源，`turns` 用来判断离轮次上限还有多远，`tokensUsed` 对接第 8 门课的预算和压缩阈值）
- 意识到「正在执行、还没落账的工具调用」需要单独一个字段来记（哪怕自己起个名字，不必和后面课程用的名字一样），并说明这是因为崩溃可能恰好卡在工具跑完和结果写回 `messages` 之间，不单独记就没法判断这次调用到底算不算数

<!-- answer -->
**崩溃损失清单**：
- 丢了：内存里的 `messages` 数组（前 22 轮加第 23 轮已经拼进去的部分）、`turns` 计数器（原本该是 22 或 23）、`tokensUsed` 累计用量、以及那个卡在「模型已经点名要调」和「结果还没塞回 `messages`」之间的工具调用——它是跑完了没记下来，还是根本没跑完，重启后完全无从判断。
- 没丢：磁盘上前几轮已经写出的报告文件、`NOTES.md` 里已经落盘的进展摘要，这些不受进程崩溃影响，原样都在。

**检查点该存的字段**：
- `messages` —— 恢复对话上下文的唯一来源，没有它，第 23 轮之前模型看到过什么、说过什么，全部无法重建。
- `turns` —— 恢复后要知道自己已经转了多少轮，才能正确判断离轮次上限还有多远，不会一恢复就立刻撞上限或者反而多给自己批了轮次。
- `tokensUsed` —— 恢复后要接着累计用量，才能对上第 8 门课设定的预算和压缩阈值，不然等于恢复了一个不知道自己花了多少钱的 Agent。
- 一个专门记录「正在执行、还没落账的工具调用」的字段 —— 因为崩溃可能恰好卡在工具跑完和结果写回 `messages` 之间，如果不单独记下这次调用的身份和参数，恢复时既不知道要不要重新执行它，也不知道该往 `messages` 里补一个什么样的结果。

<!-- hint -->
翻回第 7 门课 `runAgent` 的函数体，看看循环转的过程中，它往哪几个变量里塞了东西——那几个变量基本就是损失清单和检查点字段的主体。

<!-- hint -->
崩溃发生的具体时间点很关键：如果崩溃发生在「模型响应到达」和「工具真正开始跑」之间，和发生在「工具跑完」和「结果塞回 messages」之间，两种情况下这次工具调用算不算数、该怎么处理，是不一样的——先想清楚这一点，再决定这个字段该记录什么信息。

<!-- /exercises -->

## 小结

- Agent 可以长时间运行、跨很多次工具调用维持状态，也正因此是有状态的、错误会复利[^S1]——状态攒得越多，一次故障能抹掉的东西也越多。
- 没有有效缓解手段时，一次很小的系统故障对 Agent 可能是灾难性的[^S1]；出错后不能从头重启，因为重启昂贵、也让用户沮丧，所以要构建能从出错处恢复的系统[^S1]。
- 崩溃不是稀罕事——连正常的部署更新都要专门用彩虹部署来避免打断运行中的 Agent，因为部署时 Agent 可能正处在流程的任意一步[^S1]。
- 自主运行天然带着更高成本和误差累积的风险[^S3]，任务越长，这笔账越重，也越需要一套能扛住中断的机制。
- 不是所有任务都要背上这套机制——加复杂度只在它确实能明显改善结果时才考虑[^S3]，几轮就能收尾的任务，直接重跑的代价通常可以接受。
- 记忆（喂给模型的上下文）和执行状态（harness 攥着的运行现场）是两回事：记忆可能已经落盘，执行状态默认只活在进程内存里——接下来几课，就是把这份执行状态也变成能落盘、能恢复的东西。

[>> 第 2 课：检查点：把执行现场落盘](./02-checkpoint-anatomy.md)
