# 레슨 3: 외부 메모리: 파일과 검색

> 학습 목표:
> - 세션을 넘어 살아남아야 하는 메모리가 윈도우 밖, 파일에 기록되어야 하는 이유 설명하기
> - CLAUDE.md 같은 사람이 쓰는 메모리 파일과 Auto memory 같은 모델이 쓰는 메모리 파일 구분하기
> - 「필요할 때 검색」과 「미리 전부 로드」 사이의 트레이드오프 말하기
> - 메모리 파일을 읽고 쓰는 도구에 안전한 경로 경계 검사 추가하기
>
> 전제: 레슨 2를 마치고 컴팩션과 도구 결과 비우기의 차이를 이해 | 이전: [레슨 2 <<](./02-managing-conversation-history.md) | 다음: [레슨 4 >>](./04-structured-task-state.md)

## 세션이 끝나면 윈도우 안의 모든 것이 사라진다

레슨 2는 풀리지 않은 문제로 끝났습니다: 사용자가 지난주 「저는 매운 음식 안 먹어요」라고 말한 일정 보조 에이전트가, 이번 주에 빈 윈도우로 새 대화를 열면 — 그 문장이 말해진 적 있다는 것을 전혀 모릅니다.

잘라내기, 컴팩션, 도구 결과 비우기로는 이것을 고칠 수 없습니다. 이 셋은 모두 「이 하나의 대화 안에서 자리가 부족해졌다」를 다룹니다. 여기 문제는 다릅니다: 이 대화에는 첫 턴부터 지난주의 내용이 전혀 들어 있지 않았습니다. 공식 쿡북은 선을 직설적으로 긋습니다 — 비우기와 컴팩션은 둘 다 현재 컨텍스트를 대상으로 하며, 새 세션이 시작되고 윈도우가 비어 있을 때는 어느 쪽도 도움이 되지 않습니다. 메모리가 그 문제를 풉니다[^S2]. 윈도우는 컨테이너로서 이 하나의 세션만큼만 삽니다. 세션을 닫으면, 윈도우 안에서 다른 곳으로 옮겨지지 않은 것은 진짜로 사라집니다.

정보를 이 세션 너머로 살려 두는 유일한 방법은, 세션이 끝나기 전에 그것을 윈도우 밖 어딘가에 기록하는 것입니다 — **외부 메모리**로: 이 대화의 생애 주기에 묶이지 않은 저장소, 대개는 그냥 디스크의 파일입니다. 다음 세션이 시작되면 그 파일을 다시 읽어 그 내용을 새 컨텍스트 윈도우에 로드합니다.

## CLAUDE.md: 사람이 쓰고, 매번 전부 로드된다

외부 메모리의 가장 직접적인 패턴은 사람이 **메모리 파일**을 관리하며, 프로젝트에 두고 매 세션 시작 때 전부 읽는 것입니다. Claude Code의 CLAUDE.md가 대표적 사례입니다: 공식 문서에 따르면 CLAUDE.md 파일은 매 세션 시작 때 컨텍스트 윈도우에 로드되어 대화 자체와 나란히 토큰을 쓰며, 권장 크기 목표는 각 파일을 200줄 아래로 유지하는 것입니다 — 파일이 길수록 더 많은 컨텍스트를 소비하고 에이전트의 지시 준수도가 낮아집니다.[^S3] 200줄은 완화된 권장치라는 점에 유의하세요. 진짜 딱딱한 한계는 4 MiB입니다: 그보다 큰 CLAUDE.md는 통째로 건너뜁니다.[^S3]

```markdown
# CLAUDE.md

## 코드 스타일
- 탭이 아니라 스페이스 2칸으로 들여쓴다
- const를 선호하고, 정말 필요할 때만 let을 쓴다

<!-- 이 메모는 미래의 나를 위한 것이다. 에이전트의 컨텍스트에 들어갈 필요는 없다. -->
## 내부 메모
- 지난 결제 모듈 리팩터링에서 겪은 함정: xxx
```

이 파일에서 눈여겨볼 만한 한 가지 세부가 있습니다: 공식 문서는 CLAUDE.md 안의 **블록 수준 HTML 주석**이 내용이 에이전트 컨텍스트에 주입되기 전에 제거된다고 설명합니다.[^S3] 다시 말해, `<!-- -->` 안에 무엇을 쓰든 사람이 파일을 열면 보이지만, 에이전트가 읽는 버전에는 그 주석이 포함되지 않습니다 — 이는 사람에게 「에이전트의 토큰 예산을 쓰지 않으면서 나에게 메모를 남기는」 방법을 줍니다.

CLAUDE.md에는 레슨 2의 컴팩션과 직접 이어지는 또 다른 성질이 있습니다: 문서는 프로젝트 루트 CLAUDE.md가 컴팩션을 견뎌 낸다고 언급합니다 — `/compact` 후 Claude는 그것을 디스크에서 다시 읽어 세션에 재주입합니다.[^S3] 달리 말하면, 이런 파일은 컴팩션에 의해 「우연히 보존」되는 것이 아니라, 따로 다시 읽혀 재주입됩니다 — 그 컴팩션이 그 내용을 요약에 남겼는지 여부에 전혀 의존하지 않습니다.

```agentmentor-check
{
  "id": "mem-zh-03-claudemd-size",
  "label": "메모리 파일이 무한정 커져도 되는지 판단하기",
  "prompt": "누군가 CLAUDE.md가 완전할수록 좋다고 여겨, 프로젝트의 전체 요구사항 문서, 전체 의사결정 이력, 모든 모듈에 대한 상세한 서술을 밀어 넣어 — 도합 2000줄이 되었습니다. 이렇게 하는 것의 문제는 무엇일까요?",
  "whyHere": "방금 CLAUDE.md가 매 세션 시작 때 전부 로드되고 200줄이 공식 크기 목표라는 것을 다뤘습니다. 이 문항은 학습자가 메모리 파일이 그 자체로 토큰 예산을 쓰고 지시 준수도를 떨어뜨린다는 것을 깨닫지 못한 채 '완전할수록 좋은' 지식 베이스로 취급하는지 확인합니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "문제없다. 메모리 파일이 완전할수록 에이전트가 가진 배경이 많아지고, 답도 더 정확해진다.",
      "correct": false,
      "feedback": "그렇지 않습니다. CLAUDE.md는 매 세션 시작 때 전부 로드되고 200줄이 공식 크기 목표입니다 — 그것을 넘기면 컨텍스트를 더 쓰고 에이전트의 지시 준수도가 낮아집니다. '더 많이 기억하는 것'은 '잘 쓰는 것'과 같지 않으며, 이것이 바로 레슨 1의 컨텍스트 로트가 메모리 파일이라는 구체적 상황에서 나타난 것입니다."
    },
    {
      "id": "b",
      "text": "그렇다, 문제가 있다: 파일이 길수록 컨텍스트를 더 쓰고, 문서가 긴 파일은 에이전트의 지시 준수도를 낮춘다고 분명히 말한다.",
      "correct": true,
      "feedback": "맞습니다. CLAUDE.md는 매 세션 전부 로드되고 200줄이 공식 크기 목표입니다 — 파일이 길수록 컨텍스트를 더 태우고 에이전트의 준수도가 낮아집니다(진짜 딱딱한 한계는 4 MiB이고 그보다 크면 통째로 건너뜁니다). 자주 바뀌거나 큰 정보는, 매번 전부 로드되는 파일에 쏟아붓지 말고 필요할 때의 검색 뒤에 두어야 합니다."
    },
    {
      "id": "c",
      "text": "문제없다. 내용이 정확하기만 하면 길이는 에이전트의 성능에 영향을 주지 않는다.",
      "correct": false,
      "feedback": "길이는 성능에 직접 영향을 줍니다. CLAUDE.md의 내용은 이 대화와 나란히 컨텍스트에 로드되어 같은 토큰 예산을 씁니다. 지나치게 긴 파일은 단지 '자리를 차지하는' 것이 아니라 — 문서가 에이전트의 지시 준수도를 낮춘다고 분명히 말하며, 이는 대충 넘길 수 있는 세부가 아닙니다."
    }
  ]
}
```

## Auto memory: 모델이 쓰고, 필요할 때 검색된다

CLAUDE.md는 사람이 쓰고 매번 전부 로드됩니다. 이를 보완하는 패턴이 있습니다: 대화가 진행됨에 따라 기억할 가치가 있는 것을 모델이 직접 적어, 자신의 메모리 파일에 저장하게 하는 것 — Claude Code는 이 메커니즘을 **Auto memory**라 부릅니다. CLAUDE.md와의 분업은 상호 보완적이며, 비교표가 그 차이를 분명히 보여 줍니다: CLAUDE.md는 당신이 쓰고, Auto memory는 Claude가 씁니다.[^S3]

모델이 스스로 쓰는 메모리는 대개 두 층으로 갈립니다: 인덱스 파일(이를테면 `MEMORY.md`)과 주제별로 나뉜 구체적 메모리 파일 더미입니다. 인덱스 파일도 무한정 로드되지 않습니다 — 문서가 주는 규칙은 이렇습니다: 매 대화 시작 때 `MEMORY.md`의 첫 200줄, 또는 첫 25KB 중 먼저 도달하는 지점까지만 로드되고, 그 임계값을 넘는 내용은 세션 시작 때 로드되지 않습니다.[^S3]

그것이 **필요할 때 검색(retrieval on demand)**입니다: 세션 시작 때 에이전트는 인덱스 항목의 요약만 보고(「이 주제에 대한 상세 메모는 어떤 파일에 있다」 같은 것), 각 구체적 메모리 파일의 전체 내용은 보지 않습니다. 문서는 이에 대해 직접적입니다: 주제 파일은 시작 때 로드되지 않으며, Claude는 그 정보가 필요할 때 표준 파일 도구로 필요할 때 로드한다[^S3]. 현재 작업이 실제로 특정 주제를 요구할 때에만, 그 구체적 메모리 파일의 내용이 이번 회차의 컨텍스트 윈도우로 끌려 들어옵니다.

나란히 놓고 보면, CLAUDE.md와 Auto memory는 메모리의 서로 다른 두 차원을 다룹니다:

- **CLAUDE.md** — 사람이 선별하고 크기를 절제한, 매번 적용되는 규칙과 관례. 「이 프로젝트는 원래 이렇게 돌아가기로 되어 있다」류의 안정된 정보에 맞고, 미리 전부 로드됩니다.
- **Auto memory** — 수가 많을 수 있고 특정 작업에서만 중요한 구체적 세부. 필요할 때 검색에 맞아, 이번 작업이 필요로 하지 않는 메모리에 윈도우 예산을 낭비하지 않습니다.

둘 다 외부 메모리입니다. 차이는 「누가 쓰는가」와 「언제 로드되는가」뿐이며 — 이는 레슨 2의 사고 모델을 되울립니다: 메모리의 요점은 정보를 윈도우 밖으로 옮겨 세션을 넘어 살아남게 하는 것이고, 그 정보가 미리 전부 로드되는지 필요할 때 검색되는지는 그것이 얼마나 안정적이고 얼마나 자주 쓰이는지에 달려 있습니다.

```agentmentor-check
{
  "id": "mem-zh-03-on-demand-retrieval",
  "label": "필요할 때 검색이 실제로 무엇을 푸는지 판단하기",
  "prompt": "한 에이전트의 Auto memory에 온갖 과거 작업의 세부를 다루는 주제 메모리 파일 50개가 쌓였습니다. 만약 매 세션이 그 50개 전체 내용을 한꺼번에 컨텍스트에 로드하며 시작한다면 무엇이 잘못될까요? 그리고 그것은 '필요할 때 검색'이 정확히 무엇을 피하려는 상황일까요?",
  "whyHere": "방금 인덱스 파일과 구체적 메모리 파일의 두 층 구조를 다뤘습니다. 이 문항은 학습자가 필요할 때 검색의 동기를 이해하는지 확인합니다 — 전부 로드하는 것이 기술적으로 불가능해서가 아니라, 그렇게 하면 레슨 1·2의 윈도우 용량과 컨텍스트 로트 문제를 다시 촉발한다는 것입니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "윈도우가 빠르게 차고, 로드된 것 대부분이 현재 작업과 무관해 컨텍스트 로트를 다시 촉발한다 — 이것이 바로 필요할 때 검색이 피하는 것이다.",
      "correct": true,
      "feedback": "맞습니다. 50개 파일 전체 내용을 로드하면 윈도우가 빠르게 차는데, 이 작업은 아마 그중 한둘하고만 관련이 있습니다. 나머지는 레슨 1의 컨텍스트 로트를 다시 촉발하는 잡음입니다. 필요할 때 검색은 인덱스 요약만 먼저 로드하고, 그다음 작업이 실제로 필요로 하는 단일 주제만 가져오므로, 쓰이지 않을 메모리에 윈도우 예산을 쓰지 않습니다."
    },
    {
      "id": "b",
      "text": "문제없다. 메모리 파일은 에이전트 자신이 중요하다고 판단한 것을 담으므로, 전부 로드하면 답이 더 완전해질 뿐이다.",
      "correct": false,
      "feedback": "'에이전트가 중요하다고 판단한 것'은 쓰는 시점에 내린 판단이며, 이번 작업이 그 전부를 필요로 한다는 뜻은 아닙니다. 50개 파일을 전부 로드하면 윈도우가 차고 성능이 떨어집니다 — 이것이 바로 문서가 MEMORY.md의 첫 200줄 또는 25KB만 로드하고 나머지는 필요할 때 검색하는 이유이며, 부차적 세부가 아닙니다."
    },
    {
      "id": "c",
      "text": "문제없다. 메모리 파일은 디스크에 살아 있고 컨텍스트 윈도우 용량을 차지하지 않기 때문이다.",
      "correct": false,
      "feedback": "'디스크에 살아 있는 것'과 '이 요청에 로드되는 것'은 별개의 두 가지입니다. 일단 로드되면 그 내용은 messages나 시스템 프롬프트에 나타나 이 요청의 컨텍스트 윈도우에 똑같이 포함됩니다[^S1] — 레슨 1의 '모델은 윈도우 안에 있는 것만 안다'의 이면입니다. 로드되었다는 것은 용량을 소비한다는 뜻이며, 공짜가 아닙니다."
    }
  ]
}
```

## 메모리 파일 읽기·쓰기에 안전한 경계 더하기

CLAUDE.md 같은 사람이 쓴 파일이든 Auto memory 같은 모델이 쓴 파일이든, 에이전트가 메모리 파일을 읽고 쓰는 도구를 갖게 되면 마주해야 할 구체적인 엔지니어링 질문이 있습니다: 그 도구가 프로젝트 디렉터리 밖의 파일을 읽거나 쓰도록 구슬려질 수 있는가?

문자열 접두사 매치만 하는 경로 검사는 「메모리 디렉터리를 탈출하라」는 요청을 막는 것처럼 보이지만, 고전적인 구멍이 있습니다. 메모리 루트가 `/project/memory`라면, 순진한 `startsWith("/project/memory")` 검사는 `/project/memory-evil` 같은 경로도 통과시킵니다. 그 문자열로 시작하기는 하니까요 — 비록 그것이 메모리 루트 밖에 있는 완전히 다른 디렉터리이더라도 말입니다. 안전한 방법은 경로가 루트와 정확히 같거나, 「루트 뒤에 경로 구분자가 붙은 것」으로 시작하도록 요구하는 것입니다:

```javascript
const path = require("node:path");
const MEMORY_ROOT = path.resolve("./memory");

async function readMemoryFile({ path: relPath }) {
  const abs = path.resolve(MEMORY_ROOT, relPath);
  const inRoot = abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep);

  if (!inRoot) {
    return "거부됨: 경로가 메모리 디렉터리 밖이며, 이 도구는 그 밖의 파일을 읽을 수 없습니다.";
  }

  const fs = require("node:fs/promises");
  return await fs.readFile(abs, "utf-8");
}
```

`abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep)` 조합이 「루트 자체와 같은」 경로나 「루트 뒤에 구분자가 붙은 것으로 시작하는」 경로만 통과되도록 실제로 보장하는 것입니다 — `/project/memory-evil`은 어느 조건도 만족하지 않으므로 `/project/memory` 안의 경로로 오인되지 않습니다. 이 패턴은 레슨 6에서 지속 메모리 계층의 읽기·쓰기 도구를 만들 때 그대로 다시 쓰이며, 이 경계 검사가 무력할 때 메모리 파일이 어떤 종류의 공격 표적이 되는지는 레슨 5에서 분명히 밝힐 것입니다.

<!-- exercises -->
## 💻 연습

### 레벨 1: 어떤 메모리에 둘지 고르기

아래 네 가지 정보 각각에 대해, CLAUDE.md에 더 맞는지 아니면 Auto memory의 주제 파일 중 하나에 더 맞는지 판단하고 이유를 말하세요.

1. 「이 프로젝트는 탭이 아니라 스페이스 4칸으로 들여쓴다」 — 프로젝트가 만들어진 날부터 바뀌지 않음.
2. 「지난 수요일에 프로덕션 타임아웃을 추적했는데, 근본 원인은 데이터베이스 커넥션 풀이 너무 작게 설정된 것이었고, 그때 50으로 올렸다」 — 나중에 비슷한 문제에 쓸모 있을 수 있는, 지난 사건에 대한 일회성 기록.
3. 「이전 대화에서 사용자가 팀의 코드 리뷰 절차는 린트 먼저, 그다음 사람 리뷰라고 언급했다」 — 이 사용자와 상호작용하는 세션에서만 관련 있음.
4. 「이 대화에서 사용자가 어떤 함수를 동기 구현으로 바꿔 달라고 일회성으로 요청했다」 — 이 하나의 작업에만 관련 있음. 다음 세션에 다시 나올 가능성은 거의 없음.

<!-- rubric -->
- 항목 1은 CLAUDE.md로 판단하고, 「안정적이고 매번 쓸모 있음」을 언급하는 이유
- 항목 2와 3은 Auto memory로 판단하고, 「많아질 수 있고 특정 작업에서만 쓸모 있어, 필요할 때 검색에 맞음」을 언급하는 이유
- 항목 4는 외부 메모리가 전혀 필요 없다고 판단하고, 「이 하나의 세션에만 관련 있어 세션을 넘어 살아남을 필요 없음」을 언급하는 이유

<!-- answer -->
참고 답안: 항목 1은 **CLAUDE.md**에 맞습니다 — 프로젝트 시작부터 안정적이고 매 세션 쓸모 있는 관례로, CLAUDE.md의 「사람이 선별하고 크기를 절제한, 매번 쓸모 있는」 역할에 부합합니다. 항목 2와 3은 **Auto memory** 주제 파일에 맞습니다 — 둘 다 시간이 지나며 쌓일 수 있는 구체적 역사 세부이고, 비슷한 상황이 올 때만 검색이 필요하며, 매번 전부 로드하기에 맞지 않습니다. 정확히 필요할 때 검색하는 경우입니다. 항목 4는 외부 메모리가 **전혀** 필요 없습니다 — 이 하나의 세션에만 중요하고 세션을 넘어 살아남을 필요가 없으므로, 외부 메모리에 쓰는 것은 낭비일 뿐입니다. 이런 정보는 현재 세션의 컨텍스트 윈도우에 머물다 세션이 끝나면 자연스럽게 사라지면 되고, 그것이 기대된 동작입니다.

<!-- hint -->
판별 기준은 둘입니다: 이 정보는 「안정적인가, 아니면 바뀌는가」, 그리고 「매 세션 쓸모 있는가, 아니면 특정 상황에서만 쓸모 있는가」.

<!-- hint -->
항목 4는 「기록해야 한다」고 오판하기 쉽습니다 — 하지만 이 레슨이 연 문제를 되짚어 보세요: 외부 메모리는 「세션을 넘어 살아남기」를 풀려고 존재합니다. 어떤 정보가 이 하나의 세션에만 중요하다면, 그것을 외부 메모리에 쓰는 것은 아무것도 얻지 못하고, 저장 공간과 나중의 검색 비용만 낭비합니다.

### 레벨 2: 버그 있는 경로 검사 진단하기

아래 코드는 메모리 읽기 도구를 `MEMORY_ROOT` 디렉터리 안의 파일로 제한하려 합니다:

```javascript
const MEMORY_ROOT = "/project/memory";

async function readMemoryFile({ path: relPath }) {
  const abs = require("node:path").resolve(MEMORY_ROOT, relPath);

  if (!abs.startsWith(MEMORY_ROOT)) {
    return "거부됨: 경로가 범위 밖입니다.";
  }

  return await require("node:fs/promises").readFile(abs, "utf-8");
}
```

이 코드의 보안 구멍을 찾고, 검사를 우회해 `MEMORY_ROOT` 밖 파일을 읽는 구체적 경로 예시를 제시하며, 수정된 검사 조건을 작성하세요.

<!-- rubric -->
- `startsWith`만 쓰면 같은 접두사 우회 문제가 있음을 지적
- 검사를 우회하는 구체적 경로 예시 제시(예: `/project/memory-evil` 같은 디렉터리로 해석되는 경우)
- 수정된 조건은 「루트 자체와 같음」과 「루트 뒤에 경로 구분자가 붙은 것으로 시작함」을 모두 포함해야 함

<!-- answer -->
참고 답안: 구멍은 `abs.startsWith(MEMORY_ROOT)`가 「같은 접두사이지만 실제로는 다른 디렉터리」 경우를 무시하는 단순 문자열 접두사 매치라는 점입니다. 예시: 디스크에 `/project/memory-evil` 디렉터리가 마침 존재하면, `relPath`가 해석되어 `abs`가 `/project/memory-evil/secrets.txt`가 될 때 `"/project/memory-evil/secrets.txt".startsWith("/project/memory")`는 `true`를 반환합니다 — 그 문자열로 시작하기는 하니까요. `memory-evil`은 `MEMORY_ROOT` 안에 없는 완전히 다른 디렉터리인데도 검사가 우회됩니다. 수정된 조건은 `abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep)`여야 합니다: 루트와 정확히 같거나 「루트 뒤에 경로 구분자가 붙은 것」으로 시작하는 경로만 진짜로 루트 안에 있는 것으로 칩니다. `/project/memory-evil`은 `/project/memory`와 같지도 않고 `/project/memory/`로 시작하지도 않아, 어느 조건도 만족하지 못하고 올바르게 거부됩니다.

<!-- hint -->
구체적 반례를 떠올려 보세요: 리터럴 `MEMORY_ROOT` 문자열로 시작하지만 실제로는 다른 디렉터리인 이름이 있을까요?

<!-- hint -->
이 레슨의 「안전한 경계 더하기」 절의 올바른 버전에서, `||`의 어느 쪽도 뺄 수 없습니다 — 한 조건만으로는 충분하지 않습니다. 둘이 함께여야 구멍이 막히는 이유를 따져 보세요.

<!-- /exercises -->

## 정리

- 세션이 끝나면 윈도우 안에서 밖으로 옮겨지지 않은 것은 영영 사라진다. 정보를 세션을 넘어 지키려면 세션이 끝나기 전에 윈도우 밖 외부 메모리에 기록해야 한다
- CLAUDE.md는 사람이 쓰고 매 세션 컨텍스트에 전부 로드되며, 공식 크기 목표는 200줄이다(딱딱한 한계 4 MiB, 그보다 크면 통째로 건너뛴다). 블록 수준 HTML 주석은 주입 전에 제거되고, 프로젝트 루트 CLAUDE.md는 `/compact` 후 다시 읽혀 재주입된다[^S3]
- Auto memory는 모델이 쓰고, 인덱스 파일과 구체적 주제 파일로 나뉜다. 인덱스는 첫 200줄 또는 25KB만 로드되고, 주제 파일은 시작 때 로드되지 않고 필요할 때 로드한다[^S3]. 그래서 쓰이지 않을 메모리에 윈도우 예산을 낭비하지 않는다
- 둘은 상호 보완적이다: CLAUDE.md는 매번 쓸모 있는 안정된 규칙에, Auto memory는 특정 작업에만 필요한 대량의 세부에 맞는다
- 메모리 파일의 읽기·쓰기 도구는 안전한 경로 경계 검사를 해야 한다. `abs === ROOT || abs.startsWith(ROOT + path.sep)` 조합은 양쪽이 다 필요한데, `startsWith` 하나만으로는 같은 접두사 우회 구멍이 있기 때문이다

[>> 레슨 4: 구조화된 상태: 에이전트가 작업이 어디까지 왔는지 기억하는 법](./04-structured-task-state.md)
