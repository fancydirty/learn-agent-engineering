# 레슨 4: 구조화된 상태: 에이전트는 작업이 어디까지 왔는지 어떻게 기억하는가

> 학습 목표:
> - 작업 진행 상황을 대화 문장 속에 묻어 두는 방식이 왜 신뢰할 수 없는지, 그리고 왜 그것이 구조화된 상태가 되어야 하는지 설명하기
> - 하나의 todo가 생성부터 제거까지 거치는 전체 생명주기를 말하기
> - 처음부터 다시 시작하는 비용과 중단된 지점부터 이어서 하는 비용을 구분하기
> - 어떤 작업 상태를 세션 히스토리에 두어야 하고, 어떤 것을 별도의 체크포인트에 기록해야 하는지 판단하기
>
> 전제: 레슨 3을 마치고 외부 메모리의 두 가지 모드를 이해하고 있을 것 | 이전: [레슨 3 <<](./03-external-memory-files.md) | 다음: [레슨 5 >>](./05-memory-boundaries-and-safety.md)

## 프로세스 재시작 이후, 에이전트는 자신이 어느 단계까지 왔는지 여전히 아는가

한 에이전트가 여러 단계로 이루어진 작업을 진행하고 있습니다. 어떤 모듈을 리팩터링하는 작업으로, 네 개의 단계로 나뉩니다 — '타입 정의 업데이트', '호출부 업데이트', '테스트 실행', '문서 업데이트'. 두 번째 단계를 막 끝낸 순간 예기치 못한 재시작으로 프로세스가 중단됩니다. 다시 올라왔을 때, 에이전트는 똑같은 작업 설명을 마주합니다. 첫 번째 단계부터 다시 해야 할까요, 아니면 이미 앞의 두 단계를 끝냈다는 사실을 알고 곧바로 세 번째 단계부터 이어서 할 수 있을까요?

답은 한 가지로 귀결됩니다. 작업의 진행 상황이 대화 문장 더미 여기저기에 흩어져 있는 대신 **구조화된 상태**로 기록되었는가입니다. '타입 정의는 이미 업데이트됐다'가 그저 모델의 앞선 답변 어딘가에 있는 한 줄의 자연어, 수십 개의 메시지 사이에 묻힌 한 줄일 뿐이라면, 호스트 코드는 거기서 '지금 몇 번째 단계인가'라는 구체적 사실을 신뢰할 수 있게 뽑아낼 방법이 없습니다. 하지만 그 진행 상황이 고정된 필드를 가진 **todo 목록**으로 — 모든 작업이 명시적인 상태 표시를 지니는 형태로 — 표현되어 있다면, 호스트 코드는 그것을 바로 읽어낼 수 있습니다. 1단계와 2단계는 completed, 3단계는 아직 시작되지 않았다고 말입니다.

이것이 이번 레슨이 다루는 핵심 질문입니다. 레슨 1, 2, 3은 모두 *콘텐츠*를 관리하는 방법 — 대화 히스토리, 메모리 파일 — 에 관한 것이었습니다. 이번 레슨은 *진행 상황*을 표현하는 방법에 관한 것으로, 에이전트나 그 호스트 애플리케이션이 중단 이후 작업이 정확히 어디까지 왔는지 알 수 있게 하는 것입니다.

## Todo 생명주기: 생성, 활성화, 완료, 제거

Claude Code의 작업 추적 도구를 예로 들어 봅시다. 공식 문서는 실행 중에 하나의 todo가 거치는 전체 생명주기를 제시합니다 — 네 단계입니다.[^S4]

1. **생성(Created)**: Claude는 작업을 식별하면 그 todo를 pending으로 추가합니다
2. **활성화(Activated)**: Claude는 작업을 시작할 때 todo를 in_progress로 설정합니다
3. **완료(Completed)**: Claude는 작업이 성공적으로 끝나면 completed로 표시합니다
4. **제거(Removed)**: Claude는 더 이상 필요 없는 todo를 TaskUpdate 호출에서 status: "deleted"로 설정해 삭제합니다[^S4]

이 네 단계는 자연어의 모호한 '다 했어요'나 '하고 있어요'가 아닙니다. 네 개의 명시적인 상태 값입니다. pending, in_progress, completed, 그리고 제거를 뜻하는 deleted입니다. 모든 상태 변화는 모델이 답변 텍스트에서 슬쩍 언급하는 것이 아니라, 명시적인 도구 호출을 통해 일어납니다.

문서는 또한 이 메커니즘이 대화에서 실제로 어떻게 드러나는지도 명확히 밝힙니다: "In a session that has the task-tracking tools, Claude keeps a written todo list, updating each item's status as it works. You see each change in the message stream as a structured tool call."[^S4]（작업 추적 도구를 갖춘 세션에서 Claude는 작성된 todo 목록을 유지하며, 작업을 진행하는 동안 각 항목의 상태를 업데이트합니다. 각 변화는 메시지 스트림 안에서 구조화된 도구 호출로 나타납니다.） 이 문장은 핵심적인 구분을 못 박습니다. 진행 상황은 대화 속에 수동적으로 '반영'되는 것이 아니라, 그 자체로 식별하고 파싱할 수 있는 도구 호출로 능동적으로 기록됩니다. 그것이 바로 구조화된 상태와 문장 속에 흩어진 진행 상황 설명의 진짜 차이입니다.

```agentmentor-check
{
  "id": "mem-zh-04-structured-vs-prose",
  "label": "작업 진행 상황을 어떻게 표현해야 하는지 판단하기",
  "prompt": "한 에이전트가 답변에 이렇게 씁니다: '타입 정의 업데이트를 끝냈고, 다음으로 호출부를 처리하겠습니다.' 어떤 todo의 상태를 pending에서 in_progress로, 그리고 completed로 옮기는 구조화된 도구 호출과 비교했을 때, 호스트 코드가 현재 진행 상황을 신뢰성 있게 읽어낼 수 있는지의 관점에서 둘의 차이는 무엇인가요?",
  "whyHere": "이 체크는 todo 생명주기가 구조화된 도구 호출로 표현된다는 점을 보인 직후에 놓입니다. 자연어 설명과 구조화된 상태가 프로그램이 신뢰성 있게 파싱할 수 있는지에서 근본적으로 다르다는 것을 알아채지 못한 채 '모델이 말했다는 것은 곧 상태가 기록된 것과 같다'고 가정하는지를 검증합니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "차이 없음 — 둘 다 타입 정의가 업데이트됐다는 사실을 표현하며, 호스트 코드는 같은 방식으로 그것을 읽는다.",
      "correct": false,
      "feedback": "아닙니다. 자연어 설명은 모델이 만들어 낸 한 덩어리의 텍스트로, 형식과 표현이 매번 달라질 수 있어, 거기서 '몇 번째 단계, 어떤 상태'라는 신뢰할 수 있는 값을 뽑아내는 것은 호스트 코드에게 어렵습니다. 구조화된 도구 호출은 고정된 필드(가령 작업 ID와 상태 값)를 가지고 있어, 호스트 코드가 추측이나 자연어 이해 없이 바로 읽어낼 수 있습니다."
    },

    {
      "id": "b",
      "text": "차이가 있다 — 구조화된 도구 호출은 호스트 코드가 곧바로 상태로 파싱할 수 있는 고정된 필드를 가지는 반면, 자연어 설명은 같은 정보를 추출하려면 추가적인 해석이 필요해 훨씬 덜 신뢰할 수 있다.",
      "correct": true,
      "feedback": "정답입니다. 구조화된 상태의 가치는 프로그램이 그것을 바로 읽을 수 있다는 데 있습니다 — 작업 ID나 상태 값 같은 필드가 고정되어 있어, 호스트 코드가 '이 문장이 작업 완료를 뜻하는가'를 언어 이해에 기대어 추측할 필요가 없습니다. 문서가 작업 추적 도구는 모든 상태 변화를 단순한 텍스트 설명이 아니라 메시지 스트림 안의 구조화된 도구 호출로 나타나게 한다고 강조하는 것도 바로 그 때문입니다."
    },
    {
      "id": "c",
      "text": "차이가 있지만, 그것은 구조화된 도구 호출이 더 격식 있어 보인다는 점뿐이다 — 실제로 전달하는 정보의 양은 같다.",
      "correct": false,
      "feedback": "차이는 단지 '더 격식 있어 보인다'가 아닙니다. 중요한 것은 프로그램이 그것을 신뢰성 있게 파싱할 수 있는가입니다 — 자연어 설명은 고정된 형식이나 표현이 없어 호스트 코드가 '몇 번째 단계, 어떤 상태' 같은 구조화된 사실을 안정적으로 추출할 수 없지만, 구조화된 도구 호출은 고정된 필드를 가집니다. 이것은 스타일의 문제가 아니라 실질적인 기능의 차이입니다."
    }
  ]
}
```

## 체크포인트: 복구를 처음부터 다시 시작하는 것이 아닌 무언가로 만들기

구조화된 todo 목록을 갖추고 나면, 다음 질문은 이것입니다: 그 목록 자체는 어디에 사는가? 그것이 오직 이번 세션의 대화 히스토리에만 존재한다면, 세션이 진짜로 끝나는 순간(짧은 중단이 아니라 레슨 3의 '세션이 끝나면 윈도우 안의 모든 것은 사라진다'는 개념처럼 완전히 닫히는 것) 진행 기록도 함께 사라집니다. 근본적으로 API는 상태를 유지하지 않습니다: "The Messages API is stateless, which means that you always send the full conversational history to the API."[^S6]（Messages API는 상태를 유지하지 않으며, 이는 항상 전체 대화 히스토리를 API에 보낸다는 뜻입니다.）

바로 그것이 **체크포인트**가 해결하는 문제입니다: 어느 시점의 작업 상태 — 어떤 단계가 끝났는지, 어떤 단계가 현재인지, 어떤 단계가 남았는지 — 를 하나의 데이터로 기록하고, 세션의 수명 바깥 어딘가에 영속화하는 것입니다. 체크포인트는 레슨 3의 외부 메모리와 같은 밑바탕 메커니즘(파일에 쓰고 나중에 다시 읽기)을 씁니다. 차이는 체크포인트가 '기억할 가치가 있는 지식'을 저장하는 것이 아니라 '작업이 어디까지 왔는지' — 실행을 재개하는 데 바로 쓸 수 있는 종류의 상태 — 를 저장한다는 점입니다.

체크포인트가 자리를 잡으면 마침내 **복구 가능성**이 성립합니다: 프로세스 재시작 이후 에이전트는 '내가 어디까지 했더라'를 추측할 필요가 없습니다. 가장 최근의 체크포인트를 읽고 '1단계와 2단계는 completed, 3단계는 in_progress'라는 것을 확인한 뒤, 1단계와 2단계를 다시 하지 않고 3단계부터 이어서 합니다.

여기서 비용 비교는 구체적입니다. 리팩터링 작업에서 '타입 정의 업데이트' 단계는, 그것이 멱등적(다시 실행해도 같은 결과가 나오는)이라면 처음부터 다시 해도 낭비되는 시간만 듭니다. 하지만 어떤 단계가 '데이터베이스에 마이그레이션 레코드를 삽입한다'처럼 멱등적이지 않은 연산이라면, 처음부터 다시 하는 것은 중복 레코드를 두 개 삽입하고 심지어 데이터를 손상시킬 수도 있습니다. 체크포인트가 아끼는 것은 시간만이 아닙니다 — 그런 종류의 멱등적이지 않은 연산을 실수로 다시 실행할 위험입니다.

```agentmentor-check
{
  "id": "mem-zh-04-checkpoint-durability",
  "label": "작업 상태가 신뢰할 수 있으려면 어디에 살아야 하는지 판단하기",
  "prompt": "한 에이전트의 todo 목록 상태는 줄곧 현재 세션의 대화 히스토리에만 살았고 별도의 체크포인트 파일에는 한 번도 기록되지 않았습니다. 이 세션이 완전히 닫힌다면(짧은 중단이 아니라 진짜 종료), 다음번에 같은 작업이 시작될 때 에이전트는 '앞의 두 단계는 이미 끝났다'는 진행 상황을 여전히 읽을 수 있을까요?",
  "whyHere": "이 체크는 복구를 실제로 뒷받침하려면 체크포인트가 세션의 수명 바깥에 상태를 기록해야 한다는 점을 보인 직후에 놓입니다. 'todo 목록이 구조화되어 있다'는 것과 'todo 목록이 세션 종료 후에도 반드시 살아남는다'는 것을 혼동하는지 검증합니다 — 구조화되어 있다는 것은 '프로그램이 읽을 수 있는가'만 해결할 뿐, '세션이 끝난 뒤에도 여전히 남아 있는가'는 해결하지 않습니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "그렇다, 읽을 수 있다. todo 목록은 이미 구조화된 상태이고, 구조화된 데이터는 세션이 끝나도 사라지지 않기 때문이다.",
      "correct": false,
      "feedback": "'구조화됨'과 '영속적임'은 서로 다른 두 가지입니다. 구조화는 '호스트 코드가 현재 상태를 신뢰성 있게 파싱할 수 있는가'를 해결할 뿐, '이 데이터가 세션 종료 후에도 여전히 남아 있는가'는 해결하지 않습니다. 레슨 3에서 다뤘듯 — 세션이 끝나면 윈도우 안에서 다른 곳으로 옮겨지지 않은 것은 영영 사라집니다. 이 todo 목록이 오직 대화 히스토리에만 존재하고 체크포인트 파일에는 한 번도 기록되지 않았다면, 그것 역시 세션과 함께 사라집니다."
    },
    {
      "id": "b",
      "text": "아니다, 읽을 수 없다. 이 상태는 오직 이번 세션의 히스토리에만 존재하고 체크포인트에는 한 번도 기록되지 않았으므로, 세션이 끝나면 사라지기 때문이다.",
      "correct": true,
      "feedback": "정답입니다. todo 목록이 구조화되어 있는지는 프로그램이 그것을 신뢰성 있게 파싱할 수 있는지에만 영향을 줄 뿐, 세션 종료 후 살아남는지에는 영향을 주지 않습니다. 세션이 완전히 닫힌 뒤에도 읽을 수 있게 하려면, 레슨 3의 외부 메모리처럼 별도의 체크포인트 파일에 기록해야 합니다 — 대화 히스토리에만 사는 구조화된 상태는 세션이 끝날 때 사라지는 운명을 벗어날 수 없습니다."
    },
    {
      "id": "c",
      "text": "그렇다, 읽을 수 있다. 작업 추적 도구가 모든 상태 변화를 자동으로 디스크에 동기화하기 때문이다.",
      "correct": false,
      "feedback": "그것은 그냥 가정할 수 없는 전제입니다. 작업 추적 도구 자체의 역할은 상태 변화를 메시지 스트림 안의 구조화된 도구 호출로 나타나게 하는 것입니다. 그것을 디스크에 동기화하는 별도의 계층이 있는지, 아니면 개발자가 그 영속화를 직접 구현해야 하는지는 구체적인 설계에 달렸습니다 — '구조화되어 있다는 것은 곧 자동으로 영속화된다'로 기본값을 두어서는 안 됩니다."
    }
  ]
}
```

## 구조화된 상태는 중단을 견뎌내기 위해 존재한다

이번 레슨을 연 시나리오로 돌아가 봅시다: 프로세스 재시작 이후 에이전트는 처음부터 다시 시작해야 할까요, 아니면 중단된 지점부터 이어서 해야 할까요? 이제 우리는 그것을 분명히 답할 수 있습니다 — 두 가지가 이뤄졌는지에 달려 있습니다. 작업의 진행 상황이 (대화 문장 속에 흩어지지 않고) 구조화된 상태로 표현되었는가, 그리고 그 상태가 (이번 한 세션의 히스토리에만 살지 않고) 체크포인트에 기록되었는가입니다. 둘 다 필요합니다. 구조화된 상태만 있고 체크포인트가 없으면 상태는 세션이 끝날 때 여전히 사라지고, 체크포인트는 있지만 구조화된 상태가 없으면 체크포인트에 기록된 것 자체가 모호한 자연어여서, 다시 읽어도 작업이 어느 단계까지 왔는지 신뢰성 있게 알 수 없습니다.

이번 레슨은 진행 상황을 표현하고 보존하는 방법에 관한 것이었습니다. 다음 레슨은 그에 못지않게 중요한 또 다른 질문으로 향합니다: 이렇게 보존된 상태와 메모리가 공격자의 표적이 될 수 있는가 — 공격자가 체크포인트나 메모리 파일에 콘텐츠를 쓸 수 있다면 어떤 일이 벌어지는가입니다.

<!-- exercises -->
## 💻 연습

### 레벨 1: 하나의 작업 실행의 생명주기 상태에 라벨 붙이기

한 에이전트가 '프로젝트에 단위 테스트 추가하기'라는 작업을 받아 세 개의 todo로 나눕니다: '테스트 케이스 작성', '테스트 스위트 실행', '실패한 케이스 수정'. 아래의 시간 순서에 따라, 각 시점에서 각 todo가 어떤 생명주기 상태(pending / in_progress / completed / deleted)여야 하는지 적으세요.

1. 작업을 막 분해했다. 셋 중 아무것도 시작되지 않았다.
2. 에이전트가 테스트 케이스 작성을 시작한다.
3. 테스트 케이스가 작성됐다. 에이전트가 테스트 스위트 실행을 시작한다.
4. 테스트를 실행하니 실패한 케이스 두 개(케이스 A와 케이스 B)가 드러난다. 에이전트는 '실패한 케이스 수정'을 '케이스 A 수정'과 '케이스 B 수정'이라는 두 개의 새 todo로 세분화하고, 케이스 A 수정을 시작한다.
5. 케이스 A를 수정하는 중에, 케이스 B는 사실 테스트 자체가 잘못 작성된 것이어서 전혀 고칠 필요가 없다는 것이 밝혀지고, 그래서 '케이스 B 수정'을 제거한다.

<!-- rubric -->
- 시점 1: 셋 모두 pending
- 시점 2: '테스트 케이스 작성'은 in_progress, 나머지는 pending 유지
- 시점 3: '테스트 케이스 작성'은 completed, '테스트 스위트 실행'은 in_progress
- 시점 4: '테스트 스위트 실행'은 completed, 원래의 '실패한 케이스 수정'은 세분화, '케이스 A 수정'은 in_progress, '케이스 B 수정'은 pending
- 시점 5: '케이스 B 수정'은 deleted로 변경, 목록에서 흔적 없이 그냥 사라지는 것이 아님

<!-- answer -->
참고 답안: 시점 1에서 세 todo는 모두 **pending**(생성됐으나 시작되지 않음)입니다. 시점 2에서 '테스트 케이스 작성'은 **in_progress**가 되고, 나머지 둘은 pending을 유지합니다. 시점 3에서 '테스트 케이스 작성'은 **completed**가 되고 '테스트 스위트 실행'은 in_progress가 됩니다. 시점 4에서 '테스트 스위트 실행'은 **completed**가 되고, 원래의 포괄적인 '실패한 케이스 수정'은 '케이스 A 수정'과 '케이스 B 수정'이라는 두 개의 새 todo로 세분화되어, '케이스 A 수정'은 **in_progress**, '케이스 B 수정'은 **pending**입니다. 시점 5에서 케이스 B가 고칠 필요가 없다는 것을 알게 됐으므로, '케이스 B 수정'은 TaskUpdate 호출을 통해 상태가 **deleted**로 설정되어야 합니다 — 생명주기의 네 번째 단계인 '제거'가 바로 이 '더 이상 필요 없는' 경우를 다루는 것으로, 항목이 아무 기록도 남기지 않고 목록에서 조용히 사라지게 두는 것이 아닙니다.

<!-- hint -->
네 상태는 생명주기의 네 단계에 대응합니다: 생성은 pending에, 활성화는 in_progress에, 완료는 completed에, 더 이상 필요 없음은 deleted에 대응합니다. 이번 레슨의 'Todo 생명주기' 절과 대조하며 각 항목을 하나씩 짚어 보세요.

<!-- hint -->
시점 5는 'deleted가 된다'는 단계를 놓치고 '이 항목은 더 이상 존재하지 않는다'로 다루기 쉬운 지점입니다. 모든 상태 변화는 명시적인 도구 호출을 통해 일어난다는 이번 레슨의 논점을 떠올리세요 — 제거 역시 암묵적인 소멸이 아니라 명시적인 상태 변화가 필요합니다.

### 레벨 2: 복구 불가능한 작업 설계 진단하기

한 팀이 이런 실행 로직을 설계했습니다: 에이전트의 작업 진행 상황은 매 답변의 자연어 요약 안에만 나타납니다. '지금까지 앞의 두 단계는 끝났고, 현재 세 번째를 처리 중' 같은 식으로요. 그 요약은 오직 현재 세션의 메시지 히스토리에만 존재하고, 팀은 체크포인트 파일을 전혀 쓰지 않습니다. 시스템은 자원 한계 때문에 이따금 프로세스를 재시작하고, 재시작 후에는 같은 작업을 다시 이어서 합니다.

이 설계의 두 가지 문제('상태가 구조화되어 있는가'에 관한 것 하나, '상태가 영속화되었는가'에 관한 것 하나)를 지적하고, 각각에 대응하는 수정 방향을 제시하세요.

<!-- rubric -->
- 문제 하나: 자연어 요약은 구조화된 상태가 아니어서, 호스트 코드가 '정확히 어느 단계까지 왔는지'를 신뢰성 있게 파싱할 수 없다
- 문제 둘: 진행 상황이 체크포인트 없이 세션 히스토리에만 존재해, 프로세스 재시작이 세션을 잃는 순간 상태도 함께 사라진다
- 수정은 각각에 대응한다: 고정된 필드를 가진 todo 목록(pending/in_progress/completed/deleted 같은 상태 집합)으로 전환하기, 그리고 추가로 목록의 상태를 체크포인트 파일에 기록하기

<!-- answer -->
참고 답안: 문제 하나 — 진행 상황이 오직 자연어 요약('지금까지 앞의 두 단계는 끝났고')으로만 존재하고 고정된 필드가 없어서, 호스트 코드가 '정확히 어떤 단계가 끝났고 어떤 단계가 현재인지'를 신뢰성 있게 파싱할 방법이 없습니다. 요약의 표현은 턴마다 달라질 수 있으므로 파싱 로직이 틀리기 쉽거나 아예 자동화할 수 없습니다. 수정은 각 todo가 명시적인 상태 필드(pending / in_progress / completed / deleted)를 갖는 구조화된 todo 목록으로 전환하고, 상태 변화를 자연어 재해석이 아니라 명시적인 도구 호출을 통해 하는 것입니다. 문제 둘 — 구조화된 목록으로 전환한 뒤에도, 이 상태는 여전히 현재 세션의 메시지 히스토리에만 살고 팀은 별도의 체크포인트 파일을 쓰지 않습니다. 프로세스 재시작이 세션을 끝내는 순간, 히스토리와 그 안의 todo 목록은 함께 사라지고, 다음번에 다시 이어서 할 때 또다시 진행 상황을 알 방법이 없습니다. 수정은 추가로 todo 목록 상태를 체크포인트 파일에 영속화하여, 프로세스 재시작 후 에이전트가 먼저 가장 최근 체크포인트를 읽고 나서 어느 단계부터 이어갈지 결정하게 하는 것입니다. 세션 히스토리에만 기대는 대신 말입니다.

<!-- hint -->
두 문제를 따로 생각하세요: 하나는 '프로그램이 이 진행 상황 설명을 신뢰성 있게 읽을 수 있는가', 다른 하나는 '설령 읽을 수 있다 해도, 그 설명이 프로세스 재시작 후에도 여전히 남아 있는가'입니다. 이번 레슨은 이 두 가지를 구조화된 상태와 체크포인트라고 부릅니다 — 서로 다른 층위의 문제를 해결합니다.

<!-- hint -->
레슨 3의 '세션이 끝나면 윈도우 안의 모든 것은 사라진다'를 떠올리세요 — todo 목록이 세션 히스토리에만 산다면, 아무리 구조화되어 있어도 프로세스 재시작이 세션을 끝내는 순간 함께 사라집니다. 바로 그것이 체크포인트가 존재하는 이유입니다.

<!-- /exercises -->

## 정리

- 작업 진행 상황은 구조화된 상태가 되어야만 호스트 코드가 신뢰성 있게 읽어낼 수 있다. 자연어 답변에 흩어진 진행 상황 설명은 '지금 몇 번째 단계인가'로 안정적으로 파싱될 수 없다
- 하나의 todo의 전체 생명주기는 네 단계다 — 생성(pending), 활성화(in_progress), 완료(completed), 제거(deleted) — 그리고 각 단계는 메시지 스트림에서 관찰할 수 있는 명시적인 구조화된 도구 호출을 통해 일어난다
- 구조화됨과 영속적임은 서로 다른 두 가지다: 구조화는 '프로그램이 읽을 수 있는가'를 해결하고, 체크포인트는 '프로세스 재시작이나 세션 종료 후에도 상태가 여전히 남아 있는가'를 해결한다 — 둘 다 필요하다
- 체크포인트는 어느 시점의 작업 상태를 세션의 수명 바깥 어딘가에 기록하여, 복구를 '처음부터 다시 시작'이 아니라 '중단된 지점부터 이어서'로 바꾸며, 특히 멱등적이지 않은 연산의 실수 재실행을 피하게 한다
- 구조화된 상태도 외부 메모리와 마찬가지로 일단 영속화되면 그 자체로 공격 표적이 된다 — 다음 레슨의 주제다. 더 많이 보존할수록 지켜야 할 경계도 더 많아진다

[>> 레슨 5: 메모리의 경계와 안전](./05-memory-boundaries-and-safety.md)
