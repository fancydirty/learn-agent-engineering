# レッスン4: 構造化された状態: エージェントがタスクの進捗をどう記憶するか

> 学習目標:
> - タスクの進捗を会話の文章に埋め込むことが信頼性に欠ける理由と、なぜそれが構造化された状態にならなければならないかを説明できる
> - TODOが作成から削除まで経る完全なライフサイクルを述べることができる
> - ゼロから始めるコストと、中断した場所から再開するコストの違いを区別できる
> - タスクの状態がセッション履歴に存在すべきか、別のチェックポイントに書き込むべきかを判断できる
>
> 前提: レッスン3を終えて、外部メモリの2つのモードを理解している | 前: [<< レッスン3](./03-external-memory-files.md) | 次: [レッスン5 >>](./05-memory-boundaries-and-safety.md)

## プロセスが再起動した後、エージェントはどのステップまで到達したかを知っているか

エージェントが複数ステップのタスクを実行しています: モジュールのリファクタリングで、4つのステップに分解されます — "型定義を更新する"、"呼び出し元を更新する"、"テストを実行する"、"ドキュメントを更新する"。2番目のステップを終えたところで、予期しない再起動によってプロセスが中断されます。再起動後、同じタスク記述を受け取ります。エージェントは最初のステップからやり直すべきでしょうか、それとも最初の2つのステップは既に完了していることを知っていて、ステップ3から直接続けられるでしょうか？

答えは一つのことに帰着します: タスクの進捗が**構造化された状態**として記録されているか、会話の文章の山に散らばっているかです。「型定義は既に更新済み」がモデルの以前の返信の1行の自然言語に過ぎず、数十のメッセージに埋もれているなら、ホストコードには「どのステップにいるか」という具体的な事実を信頼性をもって引き出す方法がありません。しかし、その進捗が固定されたフィールドを持つ**TODOリスト**として表現されている場合 — すべてのタスクが明示的なステータスマーカーを持つ形です — ホストコードはそれを直接読み取れます: ステップ1と2は完了済み、ステップ3は未開始。

それがこのレッスンが扱う中核的な問いです。レッスン1、2、3はすべて*コンテンツ*の管理についてでした — 会話履歴、メモリファイル。このレッスンは*進捗*をどう表現するかについてです。中断の後、エージェントまたはそのホストアプリケーションが、タスクがどこまで進んだかを正確に知るために。

## TODOのライフサイクル: 作成、開始、完了、削除

Claude Codeのタスク追跡ツールを例に取ります。公式ドキュメントは、実行中にTODOが経る完全なライフサイクルを示しています — 4つのステップです: [^S4]

1. **作成**: Claudeはタスクを特定したときにTODOをpendingとして追加します
2. **開始**: Claudeは作業を開始するときにTODOをin_progressに設定します
3. **完了**: Claudeはタスクが正常に終了したときにcompletedとマークします
4. **削除**: Claudeは不要になったTODOをTaskUpdateコールでstatus: "deleted"に設定して削除します[^S4]

これらの4つのステップは、自然言語の曖昧な「完了しました」や「取り組んでいます」ではありません。4つの明示的なステータス値です: pending、in_progress、completed、そして削除のためのdeleted。すべてのステータス変更は、モデルが返信テキストで何気なく述べるのではなく、明示的なツール呼び出しを通じて行われます。

ドキュメントはまた、このメカニズムが実際に会話にどう現れるかも説明しています: "In a session that has the task-tracking tools, Claude keeps a written todo list, updating each item's status as it works. You see each change in the message stream as a structured tool call."[^S4]（タスク追跡ツールを持つセッションでは、Claudeは書かれたTODOリストを保持し、作業しながら各アイテムのステータスを更新します。各変更はメッセージストリームの中で構造化されたツール呼び出しとして見えます。）この文は重要な区別を明確にしています: 進捗は会話の中に受動的に「反映される」のではなく、それ自体で識別・解析できるツール呼び出しとして能動的に書き込まれます。それが構造化された状態と、文章に散らばった進捗記述との本当の違いです。

```agentmentor-check
{
  "id": "mem-zh-04-structured-vs-prose",
  "label": "タスクの進捗をどう表現すべきかを判断する",
  "prompt": "エージェントが返信に「型定義の更新を終えました。次は呼び出し元を処理します」と書きます。TODOのステータスをpendingからin_progress、そしてcompletedに移動する構造化されたツール呼び出しと比較して、ホストコードが現在の進捗を確実に読み取れるかという点で、両者の違いは何でしょうか？",
  "whyHere": "このチェックはTODOライフサイクルが構造化されたツール呼び出しで表現されることを示した直後に置かれています。学習者が「モデルがそう言った＝状態が記録された」と想定していないか、自然言語の記述と構造化された状態がプログラムによる確実な解析可能性において根本的に異なることに気づいていないかをテストします。",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "違いはありません — どちらも型定義が更新されたという事実を表現しており、ホストコードは同じ方法で読み取ります。",
      "correct": false,
      "feedback": "正しくありません。自然言語の記述はモデルが生成したテキストの塊です；そのフォーマットと表現は毎回変わりうるものであり、そこから確実な「どのステップ、どのステータス」を引き出すのはホストコードにとって困難です。構造化されたツール呼び出しには固定されたフィールド（たとえばタスクIDとステータス値）があり、ホストコードは推測も自然言語理解も不要で直接読み取れます。"
    },
    {
      "id": "b",
      "text": "違いがあります — 構造化されたツール呼び出しにはホストコードが直接ステータスに解析できる固定されたフィールドがある一方、自然言語の記述は同じ情報を抽出するのに追加の解釈が必要で、遥かに信頼性が低いです。",
      "correct": true,
      "feedback": "正しい。構造化された状態の価値は、プログラムが直接読み取れることです — タスクIDやステータス値のようなフィールドは固定されているため、ホストコードは「この文がタスクが完了したことを意味するか」を推測するために言語理解に依存する必要がありません。まさにそれゆえ、ドキュメントはタスク追跡ツールがすべてのステータス変更をメッセージストリームの中でテキスト記述ではなく構造化されたツール呼び出しとして出現させることを強調しています。"
    },
    {
      "id": "c",
      "text": "違いはありますが、それは構造化されたツール呼び出しがより形式的に見えるだけで — 実際に伝える情報量は同じです。",
      "correct": false,
      "feedback": "違いは「より形式的に見える」だけではありません。重要なのはプログラムが確実に解析できるかどうかです — 自然言語の記述には固定されたフォーマットや表現がないため、ホストコードは「どのステップ、どのステータス」のような構造化された事実を安定して抽出できません；構造化されたツール呼び出しには固定されたフィールドがあります。それはスタイルの問題ではなく、実質的な機能上の違いです。"
    }
  ]
}
```

## チェックポイント: 復旧を最初からやり直す以外のものにする

構造化されたTODOリストを手に入れたら、次の問いは: リスト自体はどこに存在するのでしょうか？それがこのセッションの会話履歴の中にのみ存在するなら、セッションが本当に終了した瞬間（短い中断ではなく、レッスン3の「セッションが終了すると、ウィンドウ内のすべてが消える」という考え方のような完全な終了）、進捗記録もそれと共に消えます。根本的に、APIはステートレスです: "The Messages API is stateless, which means that you always send the full conversational history to the API."[^S6]（Messages APIはステートレスです。つまり、常に完全な会話履歴をAPIに送信します。）

それが**チェックポイント**が解決する問題です: ある時点でのタスクの状態 — どのステップが完了し、どのステップが現在で、どのステップが残っているか — をデータの一部に書き込み、セッションの寿命の外側のどこかに永続化します。チェックポイントはレッスン3の外部メモリと同じ基本メカニズムを使います（ファイルに書き込み、後で読み戻す）；違いは、チェックポイントは「覚える価値のある知識」を保存するのではなく、「タスクがどこまで進んだか」を保存することです — 実行を再開するために直接使える種類の状態です。

チェックポイントがあれば、**復旧可能性**がついに成立します: プロセス再起動後、エージェントは「どこまでやったか」を推測する必要がありません。最新のチェックポイントを読み、「ステップ1と2は完了済み、ステップ3はin_progress」と見て、ステップ1と2をやり直す代わりにステップ3から続けます。

ここでのコスト比較は具体的です。リファクタリングタスクの「型定義を更新する」ステップが冪等（再実行しても同じ結果を生む）なら、ゼロからやり直しても無駄な時間がかかるだけです。しかし、あるステップが「マイグレーション記録をデータベースに挿入する」のような非冪等な操作なら、最初からやり直すと2つの重複した記録を挿入し、データを破損させる可能性さえあります。チェックポイントが節約するのは時間だけではありません — そのような非冪等な操作を誤って再実行するリスクです。

```agentmentor-check
{
  "id": "mem-zh-04-checkpoint-durability",
  "label": "タスク状態が信頼できるためにどこに存在しなければならないかを判断する",
  "prompt": "エージェントのTODOリスト状態は常に現在のセッションの会話履歴の中にのみ存在し、別のチェックポイントファイルには一度も書き込まれませんでした。このセッションが完全に閉じられた場合（短い中断ではなく本当の終了）、次に同じタスクが開始されるとき、エージェントは「最初の2つのステップは既に完了」という進捗をまだ読み取れるでしょうか？",
  "whyHere": "このチェックはチェックポイントが実際に復旧をサポートするためにはセッションの寿命の外側に状態を書き込まなければならないことを示した直後に置かれています。学習者が「TODOリストが構造化されている」と「TODOリストがセッション終了後も必ず残る」を混同していないかをテストします — 構造化されていることは「プログラムが読み取れるか」を解決するだけで、「セッション終了後もそこにあるか」を解決しません。",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "はい、読み取れます。なぜならTODOリストは既に構造化された状態であり、構造化されたデータはセッション終了時に失われないからです。",
      "correct": false,
      "feedback": "「構造化されている」と「永続的である」は別のことです。構造化は「ホストコードが現在の状態を確実に解析できるか」を解決します；「セッション終了後もこのデータがそこにあるか」を解決しません。レッスン3で扱いました — セッションが終了すると、他の場所に移動されなかったウィンドウ内のものは永久に消えます。このTODOリストが会話履歴の中にのみ存在し、チェックポイントファイルに書き込まれなかった場合、セッションと共に同じように消えます。"
    },
    {
      "id": "b",
      "text": "いいえ、読み取れません。なぜならこの状態はこのセッションの履歴の中にのみ存在し、チェックポイントに書き込まれなかったため、セッション終了時に消えるからです。",
      "correct": true,
      "feedback": "正しい。TODOリストが構造化されているかどうかは、プログラムが確実に解析できるかに影響するだけで、セッション終了後も残るかには影響しません。セッションが完全に閉じた後も読み取れるようにするには、レッスン3の外部メモリと同じように、別のチェックポイントファイルに書き込まなければなりません — 会話履歴の中にのみ存在する構造化された状態は、セッション終了時に消えるという運命から逃れられません。"
    },
    {
      "id": "c",
      "text": "はい、読み取れます。なぜならタスク追跡ツールはすべてのステータス変更を自動的にディスクに同期するからです。",
      "correct": false,
      "feedback": "それは想定できない前提です。タスク追跡ツール自身の仕事は、ステータス変更をメッセージストリームの中で構造化されたツール呼び出しとして出現させることです。それらをディスクに同期する追加のレイヤーがあるか、開発者自身がその永続化を実装しなければならないかは、特定の設計に依存します — 「構造化されている＝自動的に永続化される」とデフォルトで想定することはできません。"
    }
  ]
}
```

## 構造化された状態は中断を生き延びられるものにするために存在する

このレッスンの冒頭のシナリオに戻ります: プロセス再起動後、エージェントは最初からやり直すべきでしょうか、中断された場所から再開すべきでしょうか？今や明確に答えられます — 2つのことがなされたかどうかに依存します。タスクの進捗が（会話の文章に散らばるのではなく）構造化された状態として表現されたか、そしてその状態が（この1つのセッションの履歴の中にのみ存在するのではなく）チェックポイントに書き込まれたか？両方が必要です。構造化された状態があってもチェックポイントがなければ、状態はセッション終了時に消えます；チェックポイントがあっても構造化された状態がなければ、チェックポイントに書き込まれたもの自体が曖昧な自然言語であり、それを読み戻しても「タスクがどのステップに到達したか」を確実に知ることはできません。

このレッスンは、進捗をどう表現し保存するかについてでした。次のレッスンは、同じくらい重要な別の問いに向かいます: この保存された状態とメモリは攻撃者の標的になりうるのでしょうか — 攻撃者がチェックポイントやメモリファイルにコンテンツを書き込めるとしたら何が起こるのでしょうか？

<!-- exercises -->
## 💻 演習

### レベル1: 単一タスク実行のライフサイクル状態をラベル付けする

エージェントが「プロジェクトにユニットテストを追加する」というタスクを受け取り、3つのTODOに分解します: "テストケースを書く"、"テストスイートを実行する"、"失敗したケースを修正する"。以下の時系列順に従って、各TODOが各時点でどのライフサイクル状態（pending / in_progress / completed / deleted）にあるべきかを書き出してください。

1. タスクが分解されたばかりです；3つのどれも開始していません。
2. エージェントがテストケースの作成を開始します。
3. テストケースが書かれました；エージェントがテストスイートの実行を開始します。
4. テストの実行で2つの失敗ケース（ケースAとケースB）が判明します。エージェントは"失敗したケースを修正する"を2つの新しいTODO、"ケースAを修正"と"ケースBを修正"に細分化し、ケースAの修正を開始します。
5. ケースAを修正中に、ケースBは実際にはテスト自体の書き方が間違っていて変更の必要がないことが判明したため、"ケースBを修正"は削除されます。

<!-- rubric -->
- 時点1: 3つすべてがpending
- 時点2: "テストケースを書く"がin_progress、残りはpending
- 時点3: "テストケースを書く"がcompleted、"テストスイートを実行する"がin_progress
- 時点4: "テストスイートを実行する"がcompleted、元の"失敗したケースを修正する"が細分化され、"ケースAを修正"がin_progress、"ケースBを修正"がpending
- 時点5: "ケースBを修正"がdeletedに変わる。痕跡なくリストから消えるのではない

<!-- answer -->
参考解答: 時点1では、3つのTODOすべてが**pending**です（作成されたが開始していない）。時点2では、"テストケースを書く"が**in_progress**になり、他の2つはpendingのままです。時点3では、"テストケースを書く"が**completed**になり、"テストスイートを実行する"がin_progressになります。時点4では、"テストスイートを実行する"が**completed**になります；元の包括的な"失敗したケースを修正する"が2つの新しいTODO、"ケースAを修正"と"ケースBを修正"に細分化され、"ケースAを修正"が**in_progress**、"ケースBを修正"が**pending**です。時点5では、ケースBは修正が不要だと分かったため、"ケースBを修正"はTaskUpdateコールを通じてステータスを**deleted**に設定されるべきです — ライフサイクルの4番目のステップ「削除」は、まさにこの「不要になった」ケースを扱うものであり、アイテムが記録なしにリストから静かに消えるのではありません。

<!-- hint -->
4つの状態はライフサイクルの4つのステップに対応します: 作成はpendingに、開始はin_progressに、完了はcompletedに、不要になったはdeletedに対応します。このレッスンの「TODOのライフサイクル」セクションに照らして、各アイテムを1つずつ確認してください。

<!-- hint -->
時点5は「deletedになる」ステップを見逃して「このアイテムはもう存在しない」と扱いがちな箇所です。このレッスンの「すべてのステータス変更は明示的なツール呼び出しを通じて行われる」という点を思い出してください — 削除も暗黙の消失ではなく、明示的なステータス変更が必要です。

### レベル2: 復旧不可能なタスク設計を診断する

あるチームがこの実行ロジックを設計しました: エージェントのタスク進捗は各返信の自然言語の要約の中にのみ現れます、「これまでに最初の2つのステップが完了し、現在3つ目を処理中」のような形で。その要約は現在のセッションのメッセージ履歴の中にのみ存在します；チームはチェックポイントファイルを一切書き込みません。システムはリソース制限により時々プロセスを再起動し、再起動後に同じタスクが再開されます。

この設計の2つの問題（1つは「状態が構造化されているか」について、1つは「状態が永続化されているか」について）を指摘し、それぞれに対応する修正の方向を示してください。

<!-- rubric -->
- 問題1: 自然言語の要約は構造化された状態ではないため、ホストコードは「正確にどのステップに到達したか」を確実に解析できない
- 問題2: 進捗はチェックポイントなしでセッション履歴の中にのみ存在するため、プロセス再起動でセッションが失われると、状態も共に失われる
- 修正はそれぞれ: 固定されたフィールドを持つTODOリスト（pending/in_progress/completed/deletedのステータスセットのような）に切り替える、さらにリストの状態をチェックポイントファイルに書き込む

<!-- answer -->
参考解答: 問題1 — 進捗が自然言語の要約（"これまでに最初の2つのステップが完了"）としてのみ存在し、固定されたフィールドがないため、ホストコードには「正確にどのステップが完了し、どのステップが現在か」を確実に解析する方法がありません。要約の表現はターンごとに変わりうるため、解析ロジックは間違いやすいか、自動化できません。修正は、各TODOが明示的なステータスフィールド（pending / in_progress / completed / deleted）を持つ構造化されたTODOリストに切り替え、自然言語を再解釈するのではなく明示的なツール呼び出しでステータス変更を行うことです。問題2 — 構造化されたリストに切り替えた後も、この状態は現在のセッションのメッセージ履歴の中にのみ存在し、チームは別のチェックポイントファイルを書き込みません。プロセス再起動でセッションが終了すると、履歴とその中のTODOリストは共に消え、次に再開されるときに再び進捗を知る方法がありません。修正は、さらにTODOリストの状態をチェックポイントファイルに永続化することで、プロセス再起動後にエージェントがまず最新のチェックポイントを読み、セッション履歴だけに依存するのではなく、どのステップから続けるかを決定できるようにすることです。

<!-- hint -->
2つの問題を別々に考えてください: 1つは「プログラムがこの進捗記述を確実に読み取れるか」、もう1つは「読み取れるとしても、プロセス再起動後もその記述はそこにあるか」。このレッスンはこれら2つを構造化された状態とチェックポイントと呼んでいます — 異なるレベルの問題を解決します。

<!-- hint -->
レッスン3の「セッションが終了すると、ウィンドウ内のすべてが消える」を思い出してください — TODOリストがセッション履歴の中にのみ存在するなら、どれだけ構造化されていても、プロセス再起動でセッションが終了するとそれと共に消えます。まさにそれゆえチェックポイントが存在します。

<!-- /exercises -->

## まとめ

- タスクの進捗がホストコードによって確実に読み取られるには、構造化された状態にならなければなりません；自然言語の返信に散らばった進捗記述は「今どのステップにいるか」を安定して解析できません
- TODOの完全なライフサイクルは4つのステップです — 作成（pending）、開始（in_progress）、完了（completed）、削除（deleted） — そして各ステップはメッセージストリームで観察できる明示的な構造化されたツール呼び出しを通じて行われます
- 構造化されていることと永続的であることは別のことです: 構造化は「プログラムが読み取れるか」を解決し、チェックポイントは「プロセス再起動やセッション終了後も状態がそこにあるか」を解決します — 両方が必要です
- チェックポイントはある時点でのタスクの状態をセッションの寿命の外側のどこかに書き込み、復旧を「最初からやり直す」ではなく「中断された場所から続ける」に変え、特に非冪等な操作の誤った再実行を回避します
- 構造化された状態は、外部メモリと同様に、永続化されると独自の攻撃対象になります — 次のレッスンのトピックです；保存するほど、保持しなければならない境界が増えます

[>> レッスン5: メモリの境界と安全性](./05-memory-boundaries-and-safety.md)
