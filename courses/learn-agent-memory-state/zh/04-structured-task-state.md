# 第 4 课：结构化状态：让 Agent 记住任务进行到哪

> 学习目标：
> - 解释为什么把任务进度埋在对话文字里不可靠，必须变成结构化状态
> - 说出待办事项从创建到删除要经历的完整生命周期
> - 区分"从头重来"和"从中断处恢复"分别付出的代价
> - 判断一份任务状态该留在会话历史里，还是该额外写进检查点

> 前置要求：完成第 3 课，理解外部记忆的两种模式 | 上一课 [第 3 课 <<](./03-external-memory-files.md) | 下一课 [第 5 课 >>](./05-memory-boundaries-and-safety.md)

## 进程重启之后，Agent 还知道自己做到哪一步了吗

一个 Agent 正在执行一个多步骤任务：重构一个模块，涉及"改类型定义""改调用点""跑测试""更新文档"四个步骤。它刚做完第二步，进程因为一次意外重启中断了。重新拉起来之后，它面对的是同一个任务描述——这时候它是该从第一步重新做一遍，还是知道自己已经做完了前两步，直接从第三步继续？

答案取决于一件事：任务进行到哪一步，有没有被记录成一份**结构化状态**，而不只是散落在一堆对话文字里。如果"已经改完类型定义了"这句话只是模型某一轮回复里的一句自然语言描述，淹没在几十条消息中间，宿主代码没办法可靠地从里面提取出"当前处于第几步"这个明确的事实。但如果这个进度被表达成一份有固定字段的**任务清单**——每一项任务都有明确的状态标记——宿主代码就能直接读出：第一步、第二步是 completed，第三步还没开始。

这也是这一课要解决的核心问题：第 1、2、3 课讲的都是"内容"该怎么管理——对话历史、记忆文件；这一课讲的是"进度"该怎么表达，才能让 Agent 或者宿主应用在中断之后，准确地知道任务做到哪了。

## 任务清单的生命周期：创建、激活、完成、删除

以 Claude Code 的任务追踪工具为例，官方文档给出了待办事项在执行过程中要经历的完整生命周期，一共四步：[^S4]

1. **创建（Created）**：识别出一项任务时，把它作为待办事项加入清单，状态是 pending（待处理）。
2. **激活（Activated）**：真正开始执行这项工作时，把状态改成 in_progress（进行中）。
3. **完成（Completed）**：任务顺利完成时，把状态标记为 completed（已完成）。
4. **删除（Removed）**：不再需要某项待办事项时，通过一次 TaskUpdate 调用把它的状态设成 deleted（已删除）来移除它。[^S4]

这四步不是自然语言里含糊的"我做完了""我在做了"，而是明确的四个状态值：pending、in_progress、completed，以及表示删除的 deleted。每一次状态变化，都是通过一次明确的工具调用完成的，而不是靠模型在回复文字里随口一提。

官方文档还说明了这套机制具体是怎么体现在对话里的：在一个拥有**任务追踪工具**的会话里，Claude 会维护一份写下来的待办事项清单，随着工作推进更新每一项的状态，你能在消息流里看到每一次变化——它是以一次结构化的工具调用的形式出现的。[^S4] 这句话点出了关键区别：进度不是被动地"体现"在对话里，而是主动地被写成一次可以被单独识别、单独解析的工具调用，这正是"结构化状态"和"散落在文字里的进度描述"的本质差别。

```agentmentor-check
{
  "id": "mem-zh-04-structured-vs-prose",
  "label": "判断任务进度该怎么表达",
  "prompt": "一个 Agent 在回复里写道：「我已经改完了类型定义，接下来要处理调用点。」这句话和一次把某个待办事项状态从 pending 改成 in_progress、再改成 completed 的结构化工具调用，在「能不能让宿主代码可靠地读出当前进度」这件事上，有什么区别？",
  "whyHere": "刚讲完任务清单的生命周期是通过结构化工具调用体现的，这里要检查学习者是不是以为「模型说了就等于状态被记录了」，而没意识到自然语言描述和结构化状态在能否被程序可靠解析上有本质差别",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "没有区别，两者都表达了「类型定义已经改完」这个事实，宿主代码理解起来是一样的",
      "correct": false,
      "feedback": "不对。自然语言描述是模型输出的一段文字，格式和用词每次都可能不一样，宿主代码要从里面可靠地解析出「当前是第几步、状态是什么」很困难。结构化工具调用有固定的字段（比如任务 ID 和 status 取值），宿主代码可以直接读取，不需要猜测或者做自然语言理解。"
    },

    {
      "id": "b",
      "text": "有区别，结构化工具调用有固定字段，宿主代码可以直接解析出状态，而自然语言描述需要额外的理解才能提取出同样的信息，可靠性差很多",
      "correct": true,
      "feedback": "正确。结构化状态的价值就在于它是可以被程序直接读取的——任务 ID、status 取值这些字段是固定的，宿主代码不需要靠语言理解去猜「模型这句话是不是在说任务完成了」。这也是为什么官方文档强调，任务追踪工具让每一次状态变化都以结构化工具调用的形式出现在消息流里，而不只是文字描述。"
    },
    {
      "id": "c",
      "text": "有区别，但区别只在于结构化工具调用看起来更正式，实际能传达的信息量是一样的",
      "correct": false,
      "feedback": "区别不只是「看起来正式」。关键在于能不能被程序可靠地解析——自然语言描述的格式和用词不固定，宿主代码没法稳定地从中提取出「当前处于第几步、状态是什么」这类结构化事实；结构化工具调用有固定字段，这是两者在功能上的实质差别，不是风格问题。"
    }
  ]
}
```

## 检查点：让恢复不等于从头重来

有了结构化的任务清单，下一个问题是：这份清单本身存在哪里？如果它只存在于这一次会话的对话历史里，一旦这次会话真正结束（不是短暂中断，而是彻底关闭，比如第 3 课讲过的"会话一结束，窗口里的一切就没了"），这份进度记录也会跟着消失——归根到底，API 是无状态的，服务器不替你的会话在请求之间保留任何东西[^S6]。

这就是**检查点**要解决的问题：把任务在某个时间点的状态——已完成哪些步骤、当前在哪一步、还剩哪些步骤——写成一份数据，持久保存到会话生命周期之外的地方。检查点和第 3 课讲的外部记忆用的是同一套底层手段（写文件、之后再读回来），区别在于检查点里存的不是"值得记住的知识"，而是"任务进行到哪一步"这类可以直接用来恢复执行的状态。

有了检查点，**可恢复性**才成立：进程重启之后，Agent 不需要凭空猜测"我做到哪了"，而是直接读取最近一份检查点，看到"步骤一、步骤二状态是 completed，步骤三是 in_progress"，从步骤三继续，而不是把步骤一、步骤二重新做一遍。

这里的代价对比是具体的：重构任务里"改类型定义"这一步，如果本身是幂等的（重复执行结果一样），从头重来只是浪费时间；但如果某一步是"往数据库里插入一条迁移记录"这种非幂等操作，从头重来可能会插入两条重复记录，甚至污染数据。检查点省下来的不只是时间，还包括避免这类非幂等操作被意外重复执行的风险。

```agentmentor-check
{
  "id": "mem-zh-04-checkpoint-durability",
  "label": "判断任务状态存在哪里才靠谱",
  "prompt": "一个 Agent 的任务清单状态，一直只存在于当前会话的对话历史里，从来没有被单独写进过一份检查点文件。如果这次会话被彻底关闭（不是短暂中断，而是真正结束），下次重新开始同一个任务时，Agent 还能读到「已经完成了前两步」这个进度吗？",
  "whyHere": "刚讲完检查点要把状态写到会话生命周期之外才能真正支持恢复，这里要检查学习者是不是把「任务清单是结构化的」和「任务清单一定能在会话结束后存活下来」搞混了——结构化只解决了「能不能被程序读懂」，没有解决「会话结束后还在不在」",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "能读到，因为任务清单已经是结构化状态了，结构化的数据不会因为会话结束而丢失",
      "correct": false,
      "feedback": "「结构化」和「持久化」是两回事。结构化解决的是「宿主代码能不能可靠地解析出当前状态」，不解决「会话结束后这份数据还在不在」。第 3 课讲过，会话一结束，窗口里没被搬到别处的内容就彻底消失了——如果这份任务清单只存在于对话历史里，没有被单独写进检查点文件，它照样会跟着这次会话一起消失。"
    },
    {
      "id": "b",
      "text": "读不到，因为这份状态只存在于这次会话的历史里，没有被写进检查点，会话结束后它就跟着消失了",
      "correct": true,
      "feedback": "正确。任务清单是不是结构化，只影响它能不能被程序可靠解析，不影响它能不能在会话结束后存活。要让它在会话彻底结束之后还能被读到，必须像第 3 课讲的外部记忆一样，单独写进一份检查点文件——只存在于对话历史里的结构化状态，照样逃不过会话结束就消失的命运。"
    },
    {
      "id": "c",
      "text": "能读到，因为任务追踪工具会自动把所有状态变化同步保存到磁盘上",
      "correct": false,
      "feedback": "这是一个不能想当然假设的前提。任务追踪工具本身负责的是让状态变化以结构化工具调用的形式出现在消息流里，是否有额外一层机制把它同步写到磁盘、是否需要开发者自己实现这一层持久化，取决于具体设计——不能默认「结构化了就等于自动持久化了」。"
    }
  ]
}
```

## 结构化状态，是为了让中断变得可以承受

回到本课开头的场景：进程重启之后，Agent 该从头重来，还是从中断处恢复，答案现在可以说清楚了——取决于两件事有没有做到位：任务进度有没有被表达成结构化状态（而不是散落在对话文字里），这份状态有没有被写进检查点（而不是只活在这一次会话的历史里）。两者缺一不可：只有结构化状态、没有检查点，状态照样在会话结束后消失；只有检查点、没有结构化状态，写进检查点的内容本身就是含糊的自然语言，下次读出来照样没法可靠地知道进行到哪一步。

这一课讲的是"进度怎么表达、怎么留存"，下一课要讲的是另一个同样关键的问题：这些被留存下来的状态和记忆，会不会反过来成为攻击者的目标——如果攻击者能往检查点或者记忆文件里写入内容，会发生什么。

<!-- exercises -->
## 💻 练习

### Level 1：给一次任务执行标注生命周期状态

一个 Agent 收到任务"给项目加上单元测试"，拆成三个待办事项：「补充测试用例」「跑测试套件」「修复失败的用例」。按下面的时间顺序，写出每个待办事项在每个时间点应该处于生命周期的哪个状态（pending / in_progress / completed / deleted）。

1. 任务刚被拆解出来，三项都还没开始。
2. Agent 开始写测试用例。
3. 测试用例写完了，Agent 开始跑测试套件。
4. 跑测试发现有两个用例失败（用例 A、用例 B），Agent 把「修复失败的用例」这一项细化成两个新待办事项「修复用例 A」「修复用例 B」，并开始修复用例 A。
5. 修复用例 A 的过程中发现，用例 B 其实是测试本身写错了、根本不需要改，于是「修复用例 B」这一项被移除。

<!-- rubric -->
- 时间点 1：三项全部是 pending
- 时间点 2：「补充测试用例」是 in_progress，其余仍是 pending
- 时间点 3：「补充测试用例」是 completed，「跑测试套件」是 in_progress
- 时间点 4：「跑测试套件」是 completed，原「修复失败的用例」被细化，「修复用例 A」是 in_progress、「修复用例 B」是 pending
- 时间点 5：「修复用例 B」这一项状态变成 deleted，而不是直接从清单里凭空消失不留痕迹

<!-- answer -->
参考答案：时间点 1，三项待办事项都是 **pending**（已创建但未开始）。时间点 2，「补充测试用例」变成 **in_progress**，另外两项仍是 pending。时间点 3，「补充测试用例」变成 **completed**，「跑测试套件」变成 in_progress。时间点 4，「跑测试套件」变成 **completed**；原来那项笼统的「修复失败的用例」被细化成「修复用例 A」和「修复用例 B」两个新待办事项，「修复用例 A」是 **in_progress**，「修复用例 B」是 **pending**。时间点 5，发现用例 B 不需要修复，「修复用例 B」这一项应该通过一次 TaskUpdate 调用把状态设成 **deleted**——生命周期的第四步「删除」正是用来处理这种「不再需要」的情况，而不是让它悄无声息地从清单里消失不留任何记录。

<!-- hint -->
四个状态对应生命周期的四步：创建对应 pending，激活对应 in_progress，完成对应 completed，不再需要对应 deleted——按顺序对照本课"任务清单的生命周期"那一节逐条核对。

<!-- hint -->
时间点 5 容易被漏掉「变成 deleted」这一步，直接当成「这一项不存在了」——回想本课强调的「每一次状态变化都是通过一次明确的工具调用完成的」，删除同样需要一次显式的状态变更，不是隐式消失。

### Level 2：诊断一个不可恢复的任务设计

一个团队设计了这样的执行逻辑：Agent 的任务进度只体现在它每一轮回复里的自然语言总结，比如"目前已经完成了前两步，正在处理第三步"。这份总结只存在于当前会话的消息历史里，团队没有额外写任何检查点文件。系统偶尔会因为资源限制重启进程，重启后同一个任务会被重新拉起。

指出这个设计里的两个问题（一个跟"状态是否结构化"有关，一个跟"状态是否持久化"有关），并给出对应的修正方向。

<!-- rubric -->
- 问题一：自然语言总结不是结构化状态，宿主代码难以可靠解析出「具体完成到第几步」
- 问题二：进度只存在于会话历史里，没有写检查点，进程重启导致会话丢失后状态也随之丢失
- 修正方向分别对应：改用有固定字段的任务清单（比如 pending/in_progress/completed/deleted 这套状态），以及把清单状态额外写进一份检查点文件

<!-- answer -->
参考答案：问题一，进度只以自然语言总结的形式存在（"目前已经完成了前两步"），没有固定字段，宿主代码没办法可靠地解析出"具体是哪几步完成了、当前在哪一步"——不同轮次的总结用词可能不一致，解析逻辑很容易出错或者根本没法自动化。修正方向是改用结构化的任务清单，每个待办事项都有明确的状态字段（pending / in_progress / completed / deleted），状态变化通过显式的工具调用完成，而不是依赖对自然语言的二次理解。问题二，就算改成了结构化清单，这份状态也只存在于当前会话的消息历史里，团队没有单独写检查点文件——进程重启导致这次会话结束后，历史和其中的任务清单一起消失，下次重新拉起时同样无从得知进度。修正方向是把任务清单的状态额外持久化到一份检查点文件里，进程重启后先读取最近的检查点，再决定从哪一步继续，而不是仅仅依赖会话历史。

<!-- hint -->
把这两个问题分开想：一个是"这份进度描述，程序能不能可靠读懂"；另一个是"就算读懂了，进程重启之后它还在不在"。本课把这两件事分别叫做结构化状态和检查点，它们解决的是不同层面的问题。

<!-- hint -->
回想第 3 课"会话一结束，窗口里的一切就没了"——如果任务清单只活在会话历史里，那么无论它多么结构化，进程重启导致会话结束之后，它一样会跟着消失，这正是检查点存在的意义。

<!-- /exercises -->

## 小结

- 任务进度只有变成结构化状态，才能被宿主代码可靠地读取；散落在自然语言回复里的进度描述，无法稳定解析出"当前进行到哪一步"
- 待办事项的完整生命周期是创建（pending）、激活（in_progress）、完成（completed）、删除（deleted）四步，每一步都通过一次明确的结构化工具调用完成，能在消息流里被观察到
- 结构化和持久化是两件不同的事：结构化解决"能不能被程序读懂"，检查点解决"进程重启或会话结束后状态还在不在"——两者缺一不可
- 检查点把任务在某个时间点的状态写到会话生命周期之外，让恢复变成"从中断处继续"，而不是"从头重来"，尤其能避免非幂等操作被意外重复执行
- 结构化状态和外部记忆一样，一旦被持久化下来，自身也会成为下一课要讨论的攻击目标——留存下来的东西越多，需要守住的边界也越多

[>> 第 5 课：记忆的边界与安全](./05-memory-boundaries-and-safety.md)
