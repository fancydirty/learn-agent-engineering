# 第 3 课：外部记忆：文件与检索

> 学习目标：
> - 解释为什么跨会话的记忆必须写到窗口之外，写进文件
> - 区分 CLAUDE.md 这类人工编写的记忆文件和 Auto memory 这类模型自己写的记忆文件
> - 说出「按需取回」相对于「全量载入」的取舍是什么
> - 能给一个记忆文件读写工具加上安全的路径边界检查
>
> 前置要求：完成第 2 课，理解摘要压缩和工具结果清理的区别 | 上一课 [第 2 课 <<](./02-managing-conversation-history.md) | 下一课 [第 4 课 >>](./04-structured-task-state.md)

## 会话一结束，窗口里的一切就没了

上一课结尾留了一个没解决的问题：一个记录助手 Agent，用户上周告诉过它"不吃辣"，这周开了一次新的对话，窗口里空空如也——它压根不知道用户上周说过这句话。

这不是截断、摘要压缩或者工具结果清理能解决的问题。这三种机制处理的都是"这一次对话内，窗口装不下了怎么办"；而这里的问题是，这次对话从一开始就没有上周那次对话的任何内容。官方 Cookbook 把这条界线说得很直接：清理和压缩都只作用于当前上下文，对"新会话开始时窗口一片空白"这个问题谁也帮不上忙——记忆解决的就是这个问题[^S2]。窗口这个容器本身，生命周期只到这一次会话结束——会话一关，窗口里那些没被搬到别处的东西，就真的没了。

想让信息活过这次会话，唯一的办法是在会话结束前，把它写到窗口之外的地方——也就是写进**外部记忆**：一个不受这次对话生命周期限制的存储，通常就是磁盘上的文件。下次会话开始时，再把这个文件的内容读出来，重新塞进新一轮的上下文窗口。

## CLAUDE.md：人写的、每次都整份载入的记忆

有一种最直接的外部记忆模式，就是让人类自己维护一份**记忆文件**，固定放在项目里，每次会话开始都完整读一遍。Claude Code 里的 CLAUDE.md 就是这种模式的代表：官方文档说，CLAUDE.md 文件会在每次会话开始时被**载入上下文**，和这次对话本身一起消耗 token 预算，官方建议的体量目标是每份文件控制在 200 行以内——文件越长，消耗的上下文越多，Agent 对指令的遵循度也会跟着下降。[^S3] 注意 200 行是软性建议，真正的硬上限是 4 MiB：超过这个大小的 CLAUDE.md 会被整个跳过。[^S3]

```markdown
# CLAUDE.md

## 代码风格
- 用 2 个空格缩进,不要用 tab
- 优先用 const,能不用 let 就不用 let

<!-- 这一段是给未来的我自己看的笔记,不需要出现在 Agent 的上下文里 -->
## 内部备忘
- 上次重构支付模块时踩过的坑:xxx
```

这份文件有个细节值得注意：官方文档说明，CLAUDE.md 里的**块级 HTML 注释**会在内容被注入到 Agent 上下文之前被剥离掉。[^S3] 也就是说，写在 `<!-- -->` 里的内容，人打开文件时能看到，但 Agent 读到的这份文件是不包含这段注释的——这给了人类一个"给自己留备注、但不占用 Agent token 预算"的办法。

CLAUDE.md 还有一个特性，跟第 2 课讲的摘要压缩直接相关：官方文档指出，项目根目录的 CLAUDE.md 能在 `/compact` 之后存活下来——压缩发生后，Claude 会重新从磁盘读取这份文件，把它重新注入会话。[^S3] 换句话说，这类记忆文件不是被摘要压缩"顺带保留"的，而是被单独重新读取、重新注入的——它压根不依赖那次压缩有没有把它的内容保留在摘要里。

```agentmentor-check
{
  "id": "mem-zh-03-claudemd-size",
  "label": "判断记忆文件该不该无限加长",
  "prompt": "有人觉得 CLAUDE.md 里信息越全越好，干脆把项目的完整需求文档、历史决策记录、每个模块的详细说明全部塞进去，写到了 2000 行。这样做会有什么问题？",
  "whyHere": "刚讲完 CLAUDE.md 每次会话都整份载入上下文、200 行是官方建议的体量目标，这里要检查学习者是不是把「记忆文件」理解成「越全越好的知识库」，而没意识到它本身要占用 token 预算、还会影响指令遵循度",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "没有问题，记忆文件写得越全，Agent 掌握的背景信息就越多，回答会更准确",
      "correct": false,
      "feedback": "不对。CLAUDE.md 是每次会话开始都整份载入上下文的，200 行是官方建议的体量目标，超出这个体量会消耗更多上下文、还会降低 Agent 对指令的遵循度。「记得越多」不等于「用得越好」，这正是第 1 课讲过的上下文腐化在记忆文件这个具体场景里的体现。"
    },
    {
      "id": "b",
      "text": "会有问题，文件越长占用的上下文越多，而且官方文档明确说过长文件会降低 Agent 对指令的遵循度",
      "correct": true,
      "feedback": "正确。CLAUDE.md 每次会话都被整份载入，200 行是官方建议的体量目标——文件越长，消耗的上下文越多，Agent 对指令的遵循度也会下降（真正的硬上限是 4 MiB，超过会被整个跳过）。真正需要经常变化、体量大的信息，应该考虑用按需取回的方式，而不是一股脑写进每次都会整份载入的文件里。"
    },
    {
      "id": "c",
      "text": "没有问题，只要文件内容准确，长度不会影响 Agent 的表现",
      "correct": false,
      "feedback": "长度会直接影响表现。CLAUDE.md 的内容要和这次对话一起载入上下文、一起消耗 token 预算，过长的文件不只是「占地方」，官方文档明确说过它会降低 Agent 对指令的遵循度，不是一个可以忽略的细节。"
    }
  ]
}
```

## Auto memory：模型自己写、按需取回的记忆

CLAUDE.md 是人写的，每次都整份载入。还有另一种互补的模式：让模型自己在对话过程中把值得记住的东西写下来，存成自己的记忆文件——Claude Code 把这套机制叫作 **Auto memory**（自动记忆）。它和 CLAUDE.md 的分工是互补的，一个对比表格把区别说得很清楚：CLAUDE.md 由人来写，Auto memory 由模型自己写。[^S3]

模型自己写的记忆，通常会拆成两层：一份索引文件（比如 `MEMORY.md`），外加一堆按主题拆开的具体记忆文件。索引文件本身也不是无限载入的——官方文档给出的规则是：每次会话开始，只加载 `MEMORY.md` 的前 200 行，或者前 25KB，以先达到的那个为准，超出这个门槛的内容不会在会话开始时被载入。[^S3]

这就是**按需取回**：会话一开始，Agent 看到的只是索引里的条目摘要（比如"这个话题的详细笔记存在某个文件里"），而不是每一份具体记忆文件的完整内容。官方文档说得很直接：主题文件不会在启动时被加载，Claude 在需要这份信息时，才用标准的文件工具按需去读[^S3]。只有当当前任务真的用得上某个主题时，那份具体记忆文件的内容才会被临时搬进这一轮的上下文窗口。

对照来看，CLAUDE.md 和 Auto memory 处理的是记忆的两个不同维度：

- **CLAUDE.md**——人工筛选过、体量克制、每次都要用得上的规则和约定，适合"这个项目本来就该这么做"这类稳定信息，主动全量载入。
- **Auto memory**——数量可能很大、只在特定任务下才用得上的具体细节，适合按需取回，避免把窗口预算浪费在这次任务根本不需要的记忆上。

两者都属于外部记忆，区别只在"谁来写"和"什么时候被载入"——这也呼应了第 2 课的心智模型：记忆的作用是把信息移出窗口、让它跨会话留存，至于具体是整份主动载入，还是按需取回，取决于这份信息有多稳定、有多常用。

```agentmentor-check
{
  "id": "mem-zh-03-on-demand-retrieval",
  "label": "判断按需取回解决了什么问题",
  "prompt": "一个 Agent 的 Auto memory 里积累了 50 份主题记忆文件，涉及各种历史任务的细节。如果每次会话开始都把这 50 份文件的完整内容一次性全部载入上下文，会出现什么问题？这正是「按需取回」想避免的什么情况？",
  "whyHere": "刚讲完索引文件加具体记忆文件的两层结构，这里要检查学习者是不是理解「按需取回」的动机——不是因为技术上做不到全量载入，而是全量载入会重新引发第 1、2 课讲过的窗口容量和上下文腐化问题",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "会把窗口迅速填满，而且大部分内容和当前任务无关，重新引发上下文腐化——这正是按需取回要避免的情况",
      "correct": true,
      "feedback": "正确。50 份记忆文件的完整内容全部载入，窗口容量会被迅速占满，而且这次任务大概率只跟其中一两份相关，其余的都是噪音，会重新引发第 1 课讲过的上下文腐化。按需取回的做法是先只加载索引摘要，任务真正需要哪个主题，再单独去取那一份，避免把窗口预算浪费在用不上的记忆上。"
    },
    {
      "id": "b",
      "text": "不会有问题，记忆文件里的内容都是 Agent 自己认为重要的，全部载入只会让回答更全面",
      "correct": false,
      "feedback": "「Agent 自己认为重要」是写入时的判断，不代表这次任务都用得上。全量载入 50 份文件会占满窗口、拖累性能，这正是官方文档里 MEMORY.md 只加载索引前 200 行或 25KB、其余按需取回的原因，不是无关紧要的细节。"
    },
    {
      "id": "c",
      "text": "不会有问题，因为记忆文件都存在磁盘上，不占用上下文窗口的容量",
      "correct": false,
      "feedback": "「存在磁盘上」和「被载入这一轮请求」是两回事。一旦被载入，内容就会出现在 messages 或系统提示词里，照样计入这次请求的上下文窗口[^S1]——这也是第 1 课讲过的「窗口里有什么模型才知道什么」的另一面：载入了就要占用容量，不是免费的。"
    }
  ]
}
```

## 给记忆文件的读写，加上安全边界

无论是 CLAUDE.md 这类人写的文件，还是 Auto memory 这类模型自己写的文件，一旦 Agent 拿到了读写记忆文件的工具，就得考虑一个具体的工程问题：这个工具能不能被诱导着去读写项目目录之外的文件？

一个只做字符串前缀匹配的路径检查，看起来能挡住"跳出记忆目录"的请求，但其实有个经典漏洞——如果记忆根目录是 `/project/memory`，一个天真的 `startsWith("/project/memory")` 检查，会把 `/project/memory-evil` 这样的路径也误判成"在目录内"，因为它确实是以这串字符开头的，尽管这是一个完全不同的、位于记忆目录之外的目录。安全的写法要么让路径完全等于根目录本身，要么确保它是以"根目录加路径分隔符"开头：

```javascript
const path = require("node:path");
const MEMORY_ROOT = path.resolve("./memory");

async function readMemoryFile({ path: relPath }) {
  const abs = path.resolve(MEMORY_ROOT, relPath);
  const inRoot = abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep);

  if (!inRoot) {
    return "拒绝执行:路径超出了记忆目录范围,这个工具不允许读取记忆目录外的文件。";
  }

  const fs = require("node:fs/promises");
  return await fs.readFile(abs, "utf-8");
}
```

`abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep)` 这个组合，才真正保证了只有"等于根目录本身"或者"根目录加分隔符开头"的路径能通过——`/project/memory-evil` 不会被误判成 `/project/memory` 目录内的路径，因为它不满足这两个条件里的任何一个。这个模式在第 6 课搭建持久记忆层的读写工具时会直接复用，第 5 课还会讲清楚，如果这道边界检查形同虚设，记忆文件本身会变成什么样的攻击目标。

<!-- exercises -->
## 💻 练习

### Level 1：给一份笔记选合适的记忆位置

下面四条信息，分别判断更适合写进 CLAUDE.md，还是写进 Auto memory 的某份主题文件里，并说明理由。

1. "这个项目统一用 4 个空格缩进，不要用 tab"——从项目建立那天起就没变过。
2. "上周三排查过一次线上超时问题，根因是数据库连接池设置得太小，当时改成了 50"——一次性的历史事件记录，以后遇到类似问题可能会用得上。
3. "用户在上次对话里提到，他们团队的代码审查流程是先过 lint 再找人审"——只有跟这个用户交互的会话才用得上。
4. "本次对话中用户临时要求把某个函数改成同步写法"——只跟当前这一次任务相关，下次会话大概率不会再提。

<!-- rubric -->
- 第 1 条判断为 CLAUDE.md，理由提到"稳定、每次都用得上"
- 第 2、3 条判断为 Auto memory，理由提到"体量可能较大、只在特定任务下才用得上，适合按需取回"
- 第 4 条判断为都不需要写进外部记忆，理由提到"只跟这一次会话相关，不需要跨会话留存"

<!-- answer -->
参考答案：第 1 条适合写进 **CLAUDE.md**——这是从项目建立起就稳定不变的约定，每次会话都用得上，符合 CLAUDE.md「人工筛选、体量克制、每次都要用得上」的定位。第 2、3 条适合写进 **Auto memory** 的主题文件——它们都是具体的、可能会越积越多的历史细节，只有遇到类似场景才需要被取回，不适合每次都整份载入，正好符合「按需取回」的场景。第 4 条**不需要**写进任何外部记忆——它只对这一次会话有意义，不需要跨会话留存，写进外部记忆反而是浪费：这类信息留在当前会话的上下文窗口里就够了，会话结束它自然消失，这正是符合预期的行为。

<!-- hint -->
判断标准有两个：这条信息「稳不稳定、变不变」，以及它「是不是每次会话都用得上，还是只在特定场景下才用得上」。

<!-- hint -->
第 4 条容易被误判成"应该记下来"——但回想本课开头的问题：外部记忆要解决的是"跨会话留存"，如果一条信息本来就只对这一次会话有意义，写进外部记忆并不能带来任何好处，只是白占存储和之后检索的成本。

### Level 2：诊断一段有漏洞的路径检查代码

下面这段代码想限制一个记忆读取工具，只能读 `MEMORY_ROOT` 目录内的文件：

```javascript
const MEMORY_ROOT = "/project/memory";

async function readMemoryFile({ path: relPath }) {
  const abs = require("node:path").resolve(MEMORY_ROOT, relPath);

  if (!abs.startsWith(MEMORY_ROOT)) {
    return "拒绝执行:路径超出范围。";
  }

  return await require("node:fs/promises").readFile(abs, "utf-8");
}
```

找出这段代码里的安全漏洞，给出一个能够绕过这道检查、读到 `MEMORY_ROOT` 之外文件的具体路径例子，并写出修正后的检查条件。

<!-- rubric -->
- 指出漏洞是 `startsWith` 单独使用存在同前缀绕过问题
- 给出一个具体能绕过检查的路径例子（例如解析后落在 `/project/memory-evil` 之类的目录）
- 修正后的条件必须同时包含"等于根目录本身"和"以根目录加路径分隔符开头"两个条件

<!-- answer -->
参考答案：漏洞在于 `abs.startsWith(MEMORY_ROOT)` 是单纯的字符串前缀匹配，没有考虑"同前缀但其实是另一个目录"的情况。举例：如果磁盘上恰好存在一个目录 `/project/memory-evil`，当 `relPath` 解析后 `abs` 变成 `/project/memory-evil/secrets.txt` 时，`"/project/memory-evil/secrets.txt".startsWith("/project/memory")` 的结果是 `true`——因为它确实以这串字符开头，尽管 `memory-evil` 是一个完全不同的、并不在 `MEMORY_ROOT` 内的目录，检查就这样被绕过了。修正后的条件应该是 `abs === MEMORY_ROOT || abs.startsWith(MEMORY_ROOT + path.sep)`，只有路径完全等于根目录本身、或者以"根目录加上路径分隔符"开头，才算真正在根目录内，`/project/memory-evil` 既不等于 `/project/memory`，也不是以 `/project/memory/` 开头，两个条件都不满足，会被正确拒绝。

<!-- hint -->
想一个具体反例：有没有一个目录名，是以 `MEMORY_ROOT` 这串字符原样开头，但其实完全是另一个目录？

<!-- hint -->
本课"给记忆文件的读写，加上安全边界"那一节给出的正确写法里，`||` 两边的两个条件缺一不可——只写其中一个都不够，想清楚为什么两个条件加在一起才堵上了这个漏洞。

<!-- /exercises -->

## 小结

- 会话一结束，窗口里没被搬走的内容就彻底消失了；想让信息跨会话留存，只能在会话结束前把它写进窗口之外的外部记忆
- CLAUDE.md 由人来写，每次会话都整份载入上下文，官方建议的体量目标是 200 行（硬上限 4 MiB，超过会被整个跳过），块级 HTML 注释会在注入前被剥离，项目根目录的 CLAUDE.md 在 `/compact` 之后会被重新读取、重新注入[^S3]
- Auto memory 由模型自己写，拆成索引文件加具体主题文件两层，索引只加载前 200 行或 25KB，主题文件不在启动时加载、需要时才被按需读取[^S3]，避免把窗口预算浪费在用不上的记忆上
- 两者是互补关系：CLAUDE.md 适合稳定、每次都用得上的规则；Auto memory 适合体量大、只在特定任务下才需要的细节
- 记忆文件的读写工具必须做安全的路径边界检查，`abs === ROOT || abs.startsWith(ROOT + path.sep)` 这个组合条件缺一不可，单独的 `startsWith` 检查存在同前缀绕过漏洞

[>> 第 4 课：结构化状态：让 Agent 记住任务进行到哪](./04-structured-task-state.md)
