# 第 5 课：评审回路，以及把模式组合成图

> 学习目标：
> - 实现评审-优化回路（生成一版、评一版、按意见再改一版），并用两个判断标志决定这个回路值不值得建
> - 写出比「最多 N 轮」更聪明的停止条件，并把确定性检查排在裁判前面
> - 把五个模式组合成本课所称的「图」，并在自己的文档里说清这套画法是自创隐喻、锚在哪句一手依据上
>
> 前置要求：读完第 1–4 课（谁持有计划、链式与路由、并行化、编排者-工人），能手写一个以 `stop_reason` 驱动的 harness 循环 | 上一课 [第 4 课 <<](./04-orchestrator-workers.md) | 下一课 [第 6 课 >>](./06-build-a-graph.md)

## 一版产出总差口气

你让一个 agent 写一份数据库迁移方案。第一版交上来，读着像那么回事：背景、步骤、时间窗都有。但你扫一眼就发现两个洞——回滚步骤只写了一句「必要时回滚」，风险等级压根没排。你在聊天框里回了两行字把这两点说了，第二版交上来，两个洞补上了，整份东西的可用度上了一个台阶。

这个过程你已经重复过几十次了。每次都是：产出差口气，人提两句意见，产出明显变好。

问题不在于模型写不好，而在于**你的那两句意见并不难产生**。「回滚步骤要具体到命令」「每一步要标风险等级」——这是一份检查清单就能覆盖的东西。既然你能把它说清楚，模型多半也能把它说清楚。那这两句话为什么要每次都由你来说？

这个形状值得写成一个回路。

## 评审-优化：把「再改一版」写进控制流

一手材料对它的定义只有一句：一个 LLM 调用生成响应，另一个在回路里提供评估与反馈[^S1]。

前四课的四个模式各有各的拓扑：链式是把任务拆成一串步骤、每步处理上一步的输出[^S1]；路由是分类后分发到专门的后续任务[^S1]；并行化是同时跑、结果由代码聚合[^S1]；编排者-工人是让一个中心 LLM 动态拆解任务、派给工人、再合成结果[^S1]。它们的共同点是数据一路向前流。评审回路是第一个**带回边**的模式——产出会绕回去，重新进入生成节点。

它在产品里长什么样，Claude Code 的动态工作流文档给了一句大白话：跑一个检查器、修掉没过的、重复直到通过或不再有进展[^S5]。另一句描述的是同一分工的另一种用法：让独立的代理互相对抗式地评审对方的发现，之后再上报[^S5]。它是上报前的一次交叉互评，没有回边、不迭代——把它并进评审回路是本课的归类，不是原文说的同一个拓扑。

### 一个词汇的三种叫法

这里要显式搭一座桥，不然你会以为自己在学三样东西。

本系列第 6 门课教多 Agent 协作时，把「一个产出、一个挑刺」这种分工叫**生产者-评审者**。那是我们自己的教学叫法。一手材料里，Claude Code 的工作流文档用一句话描述了它在产品里的用法：让独立的代理对抗式地互审彼此的发现（「对抗式互审」是本课给这句起的简称）。同一个形状，一手材料里有两个说法：Anthropic 的模式原典叫它 **evaluator-optimizer**（评审-优化）[^S1]；Claude Code 的工作流文档不给名字，只描述用法——就是上面那句「对抗式地互审彼此的发现」[^S5]。

三个名字，一个形状。区别在你从哪个角度看它：讲协作分工时看到的是两个角色，讲编排模式时看到的是一条回边，讲产品能力时看到的是一种可复用的质量手法。

还有一层分工要说清楚。本系列第 10 门课花了整整一门的篇幅教你**怎么当好一个裁判**：怎么写量表、怎么收紧裁判的输出格式、为什么裁判要有自己独立的上下文、为什么干活的那个不能当裁判。那门课教的是裁判本身的质量。本课不重复那些，本课教的是**怎么把裁判接进控制流**——它在回路的哪个位置、什么时候跑、跑几轮、什么时候停。

## 什么时候值得建这个回路

一手材料给的适用判据是：评估标准清晰、并且迭代打磨能带来可测量的价值时，这个工作流特别有效；两个好匹配的标志是，第一，当人把反馈说清楚时，LLM 的响应确实能被明显改进；第二，LLM 自己也能给出这种反馈[^S1]。

这段引文你见过。本系列第 10 门课引的就是这同一句，当时用它回答的问题是「评审—修改回路值不值得建」。同一句判据，换到编排语境下问的是同一件事，只是这次你要把答案落成控制流里的一个环。

拆开用，这两个标志各自在挡不同的坑：

**第一个标志挡的是「改也白改」。** 有些任务，人把意见说得再清楚，第二版也不会更好——因为问题出在输入数据缺失、或者任务本身定义模糊，不在产出的措辞上。这种情况下建回路，你只是在花两倍的钱得到两版同样不可用的东西。验证方法很土但有效：**你自己先手动做三次**。三次里有几次是「人一提就明显变好」？如果三次里有两次是「提了也没用」，别建。

**第二个标志挡的是「裁判给不出那种反馈」。** 就算人一提就好使，也得问：模型能不能自己提出同类的意见？如果你的意见依赖只有你知道的东西（这个客户上季度投诉过什么、法务上周口头交代了什么），模型没有这些信息，它给出的反馈就是另一种东西了。这时候要么把那些信息喂进裁判的提示词里、把它变成模型能判的标准，要么承认这个环节需要人。

**还有一个前置条件比这两个标志更早生效：评估标准清晰。** 标准不清晰的时候，回路会稳定地产出一种特定的失败——裁判每轮给出方向不同甚至互相矛盾的意见，产出在两个版本之间来回横跳，轮数烧完，最后一版还不如第一版。这不是回路的问题，是标准还没定下来。

### 确定性的检查排在裁判前面

一手材料对确定性系统与非确定性系统的定义式区分是：确定性系统给定相同输入每次产出相同输出，而像 Agent 这样的非确定性系统，即使起始条件相同也可能生成不同的响应[^S3]。

裁判是非确定性的。任何一条「代码能判死」的规则交给裁判，你都是拿一个每次结果可能不同的东西去判一件本来每次结果都该相同的事——同时还多付一次模型调用的钱。

本系列第 10 门课把这条纪律叫分层判分：能用代码判的用代码判，代码判不了的才交给模型。本课把它照搬进回路的节点顺序里。第 2 课引过的那句在这里照样管用——你可以在任意中间步骤上加程序化检查，确保流程还在轨道上[^S1]。回路里的每一版草稿都是一个中间步骤。

具体到迁移方案那个例子：「每一步是否都有对应的回滚命令」可以用正则或结构化解析判死，属于关卡；「回滚命令写得是否可信」得靠裁判。前者没过就直接把缺哪几步告诉写作者，连裁判都不用惊动。

## 回路的代码骨架

```javascript
const MAX_ROUNDS = 3;

async function reviewLoop(task) {
  let draft = null;
  let bestDraft = null;
  let feedback = null;
  let bestScore = -1;
  let rounds = 0;

  while (rounds < MAX_ROUNDS) {
    rounds++;

    // 节点 A：生成。第一轮 feedback 是 null，之后带着上一轮的意见重写
    draft = await runAgent(writerPrompt(task, draft, feedback));

    // 确定性关卡排在裁判前面：代码能判死的，不花钱问模型
    const gate = checkDeterministic(draft);
    if (!gate.pass) {
      feedback = gate.problems.join('\n');
      continue;
    }

    // 节点 B：裁判。独立上下文、独立提示词，不看写作过程
    const verdict = await runVerdict(rubricPrompt(task, draft));
    if (verdict.pass) {
      return { draft, rounds, reason: 'passed' };
    }

    // 停止条件：这一轮没比上一轮好，就别再烧下一轮
    if (verdict.score <= bestScore) {
      return { draft: bestDraft ?? draft, rounds, reason: 'no-progress' };
    }

    bestScore = verdict.score;
    bestDraft = draft;
    feedback = verdict.notes;
  }

  return { draft: bestDraft ?? draft, rounds, reason: 'max-rounds' };
}
```

几个细节值得单说。

**`runAgent` 是一个完整的 harness 循环。** 这一点从第 2 课起就没变过：这段脚本里的每个 `await runAgent(...)` 背后，都是本系列第 7 门课那个以 `stop_reason` 驱动的循环在跑。这里只是在循环外面又套了一层代码写的控制流。

**这几个 `reason` 是不同的结局，别把它们混成一个布尔值（真实系统里常常还要再分，比如关卡连续失败单列一档）。** `passed` 可以直接交付；`max-rounds` 意味着轮数烧完了但还没过，多半要转人工；`no-progress` 意味着模型卡住了，继续烧钱也不会更好。这三种结局在观测数据里应该是三条能分开数的线——本系列第 11 门课教的那套记录方式，在这里要落到 `reason` 这个字段上。

**关卡失败也算一轮。** `continue` 之前 `rounds` 已经加过了。这是有意的：关卡反复不过说明写作者的提示词有问题，让它无限重试只会把钱烧在同一个坑里。

**两种停止条件都要有。** 一手材料谈 Agent 循环时说过：任务常常在完成时终止，但加上停止条件（比如最大迭代数）来维持控制也很常见[^S1]。这是「最多 N 轮」的出处，它是一根保险丝——保证这段代码在任何情况下都会停。而「不再有进展」这个停法来自另一处：跑一个检查器、修掉没过的、重复直到通过或不再有进展[^S5]。它比保险丝聪明，因为它盯的是**这一轮有没有比上一轮好**，而不是跑了几轮。

分数没涨就退出是最省事的实现，但不是唯一的实现。如果你的裁判不输出分数，可以改成盯**未通过项的数量**有没有减少；如果任务本身波动大，可以改成「连续两轮没涨才退出」，用一个 `stalled` 计数器。挑哪一种取决于你的裁判有多稳，不取决于哪种听起来高级。（注意骨架里那个 `bestDraft`：分数没涨就退出的前提，是你手上一直留着分数最高的那一版；只跟 `bestScore` 不留 `bestDraft`，`no-progress` 和 `max-rounds` 两个出口就会把更差的当前版交出去。）

## 把模式组合起来：本课把它叫「图」

五个模式到这里就齐了。接下来的问题是怎么把它们摆在一起。

先给官方立场：这些构件不是硬性规定，它们是开发者可以塑形和组合、以适配不同用例的常见模式；成功的关键，和任何 LLM 功能一样，是测量表现并对实现做迭代[^S1]。

也就是说，「怎么组合」这件事，一手材料给的是许可，不是配方。配方得你自己写。

### 图术语的诚实声明

**本课从头到尾用的这套「图 / 节点 / 边」的画法，是本课自己的工程隐喻，不是官方术语（前面几节已经用到「节点」「回边」，用的就是这个意思）。**

这句话请照抄进你自己的架构文档。「图」「节点」「边」「DAG」「状态机」这些词，在本课引用的全部一手来源里一次都没有出现过。一手材料的词汇是 workflows（工作流）、patterns（模式）、orchestrator-workers（编排者-工人）、fan out（扇出）——它讲的是模式清单，不是拓扑结构。

我们仍然要用「图」这个词，因为五个模式摆在一起时需要一种语言把它们说清楚，而「图」是最省力的那种。但这个隐喻必须有个锚点，不然它就是凭空发明的行话。锚点是一手材料里真实存在的这一句：工作流脚本自己持有循环、分支与中间结果，模型的上下文里只装最终答案[^S5]。

这句话已经把图的三个要素说全了：循环（回边）、分支（分叉点）、中间结果（状态）。我们做的只是给它们各起了个名字。

### 画法约定

在本课的画法里：

- **节点** = 一次 `runAgent` 循环，或者一段纯代码（关卡、分类、聚合、切批）。标注每个节点是哪一种，是画图时最有价值的动作——它逼你回答「这一步真的需要模型吗」。
- **边** = 「谁的输出喂给谁」。边不是数据结构，只是脚本里下一行代码读了上一行的变量。
- **状态** = 脚本变量。一手锚点在这里也有一句：中间结果留在脚本变量里，而不是落进模型的上下文[^S5]。**没有「节点间传递的状态对象」这种官方概念**，那是我们从别的领域借来的说法；本课不建这个抽象，需要什么就传什么变量。

```agentmentor-check
{
  "id": "orc-zh-05-graph-is-ours",
  "label": "同事说「节点」「边」在官方文档里搜不到",
  "prompt": "你们团队的架构文档用「节点」「边」组织了五个编排模式。评审时一位同事留了条意见：「你们写的『节点』『边』我去 Anthropic 文档里怎么搜不到？是不是引用造假？」他说得没错——这套画法确实是你们自己定的。你怎么回，文档又该怎么改？",
  "whyHere": "本课刚把「图 / 节点 / 边」这套画法端上来，而它在一手材料里一次都没出现过。这道题练的是引用诚实：自创的组织隐喻可以用，但必须在文档里标明它是自创的，并说清它锚在哪句真实原文上。本系列第 10 门课教的验证精神，在这里收个尾。",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "搜不到就说明有问题，把图术语从文档里全删掉，只用官方原词重写一遍，宁可啰嗦也不冒风险。",
      "correct": false,
      "feedback": "反应过度了。自创隐喻不等于造假——造假是把自创说法伪装成官方说法。删掉画法，文档就失去了组织五个模式的那套语言，读者反而更难读。问题只出在没标注来源，改标注就够了，不用连词汇一起扔。"
    },
    {
      "id": "b",
      "text": "回复他：图、节点、边是软件工程里的通用术语，编译器和数据流领域用了几十年，属于常识，不需要在文档里解释来源。",
      "correct": false,
      "feedback": "这正是问题所在。这些词确实通用，但你在文档里把它们当成了这套 Agent 编排模式的官方分类——读者会顺理成章地以为「评审回路是官方五模式里的一种节点类型」。被误读的不是「图」这个词本身，是它和一手材料之间的关系。"
    },
    {
      "id": "c",
      "text": "在文档开头补一段：标明图 / 节点 / 边是我们团队的画法、不是官方词汇，并点出它锚在哪句原文上，再逐处核对引用。",
      "correct": true,
      "feedback": "对。自创词汇的成本不是不能用，是必须标价。标明「这是我们的画法」，再指出它锚在哪句真实原文上，读者就同时拿到了两样东西：一套好用的组织语言，和一条能查证的引用链。同事搜不到的疑问也就自动解决了——他搜的是官方词汇，而你从没声称那是官方词汇。"
    }
  ]
}
```

## 五个模式在这套画法下的形状

```text
链式        A ──> B ──> C                    一条线

路由           ┌──> B1
            A ─┼──> B2                       一个分叉点
               └──> B3

并行           ┌──> W1 ──┐
            A ─┼──> W2 ──┼──> 汇合           一把扇子：扇出，再汇合
               └──> W3 ──┘

编排者-工人    ┌──> W? ──┐                   分叉点是动态的：有几条边、
            A ─┼──> W? ──┼──> 汇合           每条边干什么，由 A 看了输入才定
               └──> W? ──┘

评审回路    A ──> J ──┐                      一个带回边的环
            ^         │
            └───否────┘
```

第四个形状的注解值得重读一遍。并行化和编排者-工人在形状上长得一样（原文用的词是 topographically similar），关键差异是子任务不是预定义的，而是由编排者根据具体输入决定的[^S1]。画在纸上，这个差异就是「三条边是我画的」和「三条边是 A 画的」的区别——纸面看不出来，但代码里差得很远。

### 一个组合示例

把路由、扇出、汇合、评审回路串在一起：

```text
[分类] ─┬─ 简单 ──> [直接答] ──────────────────────> 交付
        │
        └─ 复杂 ──┬─> [工人1] ─┐
                  ├─> [工人2] ─┼─> {汇合} ─> [起草] <──────┐
                  └─> [工人3] ─┘                │          │
                                                v          │
                                             {关卡} ─否─────┤
                                                │ 过        │
                                                v          │
                                             [评审] ─否─────┘
                                                │ 是
                                                v
                                               交付

节点类型：[ ] = runAgent 循环    { } = 纯代码
边 = 谁的输出喂给谁。{关卡} 是确定性检查，排在 [评审] 之前；关卡未过或评审否，都打回 [起草]。
[分类] 这里画成模型循环而不是 {纯代码}，是因为退款/技术/投诉的边界模糊——边界清楚时应换成传统分类器（这就是分层判分：能用代码判的先用代码判）。
```

第 6 课实现的是这张图的**一个变体**：那批工单的验收标准恰好都能写成规则，所以 [评审] 那层退化成一道 {关卡}，扇出也从「一条复杂条目分给三个工人」换成「一批工单各派一个处理者」。哪几处变了、为什么变，第 6 课开头会逐条列。这里先看形状，代码留到下一课。

## 组合带来的工程红利

把控制流搬进代码，好处不只是「看得懂」。有几条是有一手依据的：

**逐步留痕带来可恢复。** 运行时会随着运行推进逐个追踪每个代理的结果，这正是一次运行能在同一会话内恢复的原因[^S5]。翻译成本课的画法：图的每个节点天然就是一个检查点位置——节点跑完，结果落进脚本变量，这个变量就是「跑到哪了」的记录。本系列第 9 门课教的检查点设计在这里不用另起炉灶，节点边界就是天然的落点。

**细粒度扇出保住更多进度。** 一手材料的原话是：把工作扇出给许多小代理的工作流，比一条长代理保住更多进度[^S5]。一条跑了四十分钟的长代理挂掉，四十分钟全没了；四十个各跑一分钟的小节点挂掉一个，你只丢一分钟，而且知道丢的是哪一分钟。

**可重复的质量手法能复用。** 把计划搬进代码，还让工作流可以套用一种可重复的质量模式，而不只是多跑几个代理：它可以让独立的代理对抗式地互审彼此的发现之后再上报，或者从几个角度各起草一份方案再互相权衡，从而得到比单次通过更可信的结果[^S5]。这里的关键词是「可重复」——手动做一次互审是操作，写进脚本才是能力。

**确定性护栏包着非确定性的代理。** Anthropic 在多 Agent 研究系统的复盘里写道：他们把基于 Claude 的 AI 代理的适应性，与重试逻辑、定期检查点这类确定性护栏结合起来[^S2]。翻译成本课的画法：图的骨架是确定性的（谁调用谁、什么时候停、失败了走哪条边），节点内部是非确定性的。这个分层不是审美偏好，是让系统可运维的前提。

## 组合的纪律：每加一层都要过一关

红利说完了，说约束。

**每加一层都要过「可测量的改进」这一关。** 一手材料在两处叮嘱同一件事，第二处还专门加了「重申一次」：只有当复杂度能明显改善结果时，你才该考虑增加它[^S1]。这条对本课尤其要命——五个模式摆在你面前，最容易犯的错是全都用上。多加一个节点，就多一次模型调用、多一处可能失败的地方、多一个要排查的对象。加之前先问：去掉它，指标会掉吗？答不上来就说明还没测。

**节点级的重试与超时是工程实践，不是官方设计。** 一手材料关于这件事只有一个从句（重试逻辑与定期检查点这类确定性护栏[^S2]）。所以下面这些是以工程实践的口径写的，你不会在任何一手文档里找到它们的背书：给每个 `runAgent` 节点包一层超时，超时后要么重试要么把这个节点标为失败继续走；重试次数按节点的性质定（只读的检索节点可以多试几次，有副作用的写入节点最好一次都别自动重试）；节点失败时要区分「这条边可以跳过」和「整张图必须停」，别让一个可选节点的失败拖垮整次运行。这些都是普通的分布式系统常识，只是搬到了 Agent 上，不要把它们当成什么新东西。

**深度是有界的。** 产品级的参照摆在那里：默认情况下，一个子代理可以再派生自己的子代理，最多到主对话之下三层[^S4]。三层这个数字不是本课发明的门槛，但它传达的意思很清楚——真实产品里的嵌套深度不是无限的，有人认真想过在哪停。你的图也该有个类似的答案。如果你画出来的图有五层嵌套，先怀疑是任务拆得太碎，而不是先去想怎么支持更深。

## 图不是目标，是任务形状的描述

有一种失败模式在这门课的最后特别值得防：**先选一个酷炫的拓扑，再找任务往里塞。**

顺序应该反过来。先把任务本身的依赖形状画出来——哪几步必须排队（前一步的输出是后一步的输入），哪几步互不相干（谁先跑都一样），哪一步需要看了输入才知道要拆成几份，哪一步的产出需要有人挑刺才靠得住。这张图画完，用哪几个模式基本上已经定了：排队的地方是链，互不相干的地方是扇子，看了才知道的地方是编排者，需要挑刺的地方是回路。

模式是任务形状的名字，不是可以任意挑选的菜单。

而这条纪律之上还有一条更早生效的：找到可能的最简单方案，只在需要时才增加复杂度[^S1]。这句话在第 1 课就出现过，在本课结尾还是同一句。五个模式学完之后，「一次 LLM 调用就够了」仍然是一个完全合法的答案——一手材料自己也说，对很多应用来说，用检索和上下文示例把单次 LLM 调用优化好通常就足够了[^S1]。

完整的、能跑的组合代码在第 6 课。本课到此为止，你手上有的是五个模式、一套画法，和一份关于什么时候不该用它们的清单。

## 💻 练习

<!-- exercises -->

### Level 1：画两张图，并给回路设计停止条件

不写代码。用本课的画法给下面两个任务各画一张 ASCII 图（用 ```text 围栏），**每个节点都要标注它是 `runAgent` 循环还是纯代码**。

**任务一 · 客服工单**：工单进来先分类（退款 / 技术 / 投诉），按类走不同的处理流程；处理完做风险分级，高风险的那些要经过一个评审回路才能发出，低风险的直接发。

**任务二 · 40 个模块的代码评估**：一次要评估 40 个模块，分批扇出、并行评，跑完汇合，再由一个节点写汇总报告；报告要过一道 gate（40 个模块是否都有结论、分数是否在合法区间、引用是否可解析），没过就退回重写。

画完之后，为这两张图里各自的那个回路（任务一的评审回路、任务二的报告 gate 回环）分别设计停止条件：最大轮数是必备的保险丝，不参与挑选；在「通过」与「不再有进展」之间定出哪个是主要停法，并说明你为什么留下或放弃另一个。

<!-- rubric -->

- 两张图都逐节点标注了 `runAgent` 还是纯代码，并且分类、风险分级、切批、汇合、gate 这类判断被标成了纯代码（分类如果用 LLM 做也可以，但要说明为什么不用传统分类器）
- 任务一的图里有一个真正的分叉点（三条或更多互斥的边），任务二的图里同时出现了扇出和汇合两个动作
- 两个回路都画出了回边（未通过 → 回到生成节点），不是画成一条直线
- 每个回路都保留了最大轮数这根保险丝，并在「通过」与「不再有进展」之间说清主次、给出和任务性质挂钩的理由
- 「不再有进展」被定义成一个可判定的量（分数没涨、未通过项数量没减少），而不是一句「模型看起来没改善」
- 说明了每种停止结局之后发生什么（交付 / 转人工 / 上报），没有把三种结局混成一个布尔值

<!-- answer -->

**任务一参考图**

```text
[分类] ──┬── 退款 ──> [退款处理] ──┐
  纯代码 │                          │
（关键词 ├── 技术 ──> [技术处理] ──┤
 + 规则）│    runAgent              │
         └── 投诉 ──> [投诉处理] ──┘
              runAgent  │  runAgent
                        v
                   {风险分级}  纯代码（金额阈值 / 敏感词 / VIP 标记）
                        │
           低风险 ──────┴────── 高风险
              │                    │
              │              [起草回复] <────────┐  runAgent
              │                    │             │
              │              {合规关卡}  纯代码  │
              │                    │             │
              │               [评审] ──── 否 ────┘  runAgent
              │                    │ 是
              v                    v
           [发出] ─────────────> [发出]   纯代码
```

分类这一步标成纯代码是有意的：工单分类是典型的「传统分类模型或算法也能准确处理」的场景，能不用模型就不用。如果你的类目边界很模糊、规则写不出来，改成 `runAgent` 也对，但要在图上写明理由。

**任务一的停止条件：通过 + 最大轮数。**

放弃「不再有进展」。原因是这个回路评的是**合规**，「没通过」意味着这条回复不能发出去，它不是一个可以将就的质量分。如果因为分数没涨就退出并发出，你等于用一个省钱的规则把不合规的内容放了出去。最大轮数在这里的作用是保险丝：烧完轮数还没过，结局是**转人工**，不是转发出。三种结局分别是 `passed` → 直接发、`max-rounds` → 进人工队列并附上裁判最后一轮的意见、关卡连续失败 → 报警（说明起草提示词坏了）。

**任务二参考图**

```text
[读取 40 个模块清单]   纯代码
          │
      {切批：5 批 × 8 个}   纯代码（并发上界写死在代码里）
          │
   ┌──────┼──────┬──────┬──────┐
   v      v      v      v      v
[评估] [评估] [评估] [评估] [评估]   runAgent × 5，批内逐模块
   │      │      │      │      │
   └──────┴──汇合─┴──────┴──────┘   纯代码（收结果、记失败、按需补跑）
          │
     [写汇总报告] <──────────┐   runAgent
          │                  │
     {报告 gate} ─── 没过 ───┘   纯代码：40 个模块都有结论？
          │                       分数在 0–100？引用能解析？
          │ 过
          v
        交付
```

**任务二的停止条件：通过 + 不再有进展。**

这里的 gate 检查的是**可枚举的缺失**（哪几个模块没结论、哪几条引用解析不了），所以「有没有进展」可以定义得非常硬：这一轮的未通过项数量比上一轮少了吗？少了就继续，没少就停——一轮下来缺失数量纹丝不动，通常说明缺失的根因在上游（那几个模块的评估结果本来就是空的），报告节点再改一百遍也变不出来。这时候停下来，把缺失清单和上游失败记录一起交给人，比继续烧钱有用。

放弃「最大轮数」作为主要停法（可以叠一个很大的值当兜底保险丝，比如 10，但它平时不该触发）。原因是「不再有进展」在这个任务上判得更早也更准，轮数上限只会在它失灵时才轮到出场。

<!-- hint -->

标注节点类型时，遇到「这一步到底算不算纯代码」的犹豫，问一句：**这一步的输出，同样的输入跑两次会不一样吗？** 会不一样的是 `runAgent`，不会的是纯代码。你图里那些做汇总、做筛选、做判定的节点，多半都落在后者。

<!-- hint -->

设计停止条件时，先想清楚**每种停法对应的下一步动作是什么**。如果「不再有进展」和「通过」触发的是同一个动作（都交付），那这个回路多半根本不需要「不再有进展」；如果三种结局对应三种不同的下游处理，那这三种你可能都得留。

### Level 2：三个场景，判这个回路值不值得建

不写代码。用本课的两个判断标志——(1) 人把反馈说清楚时，产出确实能被明显改进；(2) LLM 自己也能给出这种反馈——逐个裁决下面三个场景，并给出你的处理建议。

**场景一 · 文案打磨**：一个营销落地页的首屏文案生成器。产品经理说，现在生成的文案「能用但没劲」，她每次都会提两三条具体意见（卖点没落在第一句、行动号召太软、长度超了移动端两行），提完之后的版本明显更好。团队问要不要上一个评审回路。

**场景二 · 财务合计校验**：一个从原始凭证生成月度财务摘要的 agent。摘要里的各项合计经常对不上，有人提议加一个「审计 agent」，让它读摘要、挑出算错的地方、退回去重算，直到审计 agent 认可为止。

**场景三 · 组件命名风格**：一个给新组件起名并写文档的 agent。三个前端同事对产出的评价长期不一致：A 说命名太啰嗦，B 说不够描述性，C 觉得都行但文档语气太正式。有人提议上评审回路，让一个「风格评审 agent」把关。

<!-- rubric -->

- 三个场景都逐一对照了两个标志，每个标志给出明确的「符合 / 不符合」判断和理由，而不是只给一个整体印象分
- 场景一判为「值得建」，并且给出了停止条件设计，没有停在「值得」两个字上
- 场景二判为「不该建评审 agent」，明确指出替代方案是确定性验证器（用代码重算合计并比对），并说明理由是代码能判死的东西不该交给非确定性的裁判
- 场景三判为「问题出在前置条件」，指出评估标准不清晰、连人自己的判断都不一致，结论是先去把标准定下来而不是先建回路
- 至少在一个场景里点明了「回路建错了会怎样」的具体后果（多花的钱、来回横跳的产出、被放过的错误）
- 没有把「值得建」等同于「轮数越多越好」

<!-- answer -->

**场景一：值得建。**

标志一符合，而且证据是现成的——产品经理的意见提完之后版本明显更好，这个「人一提就好使」的循环已经在人工状态下跑了很多遍，不需要再验证。标志二也符合：她提的三条意见（卖点位置、行动号召强度、长度）都能写进量表，模型完全给得出同类反馈。评估标准也算清晰。

建议：把她那三条意见固化成裁判的量表，其中**长度超没超移动端两行**这一条不要交给裁判，用字数统计判死，放在关卡里。停止条件用「通过 + 最大轮数」，轮数设 2–3；文案这种东西轮数多了容易越改越平，最大轮数在这里不只是保险丝，本身就是一种质量控制。烧完轮数没过就把最后一版连同裁判意见一起给产品经理，让人做最后一刀。

**场景二：不该建这个审计 agent。**

两个标志在这里都不是关键——它们上面还压着一条更早生效的判断：这件事代码判得死。合计对不对是一个确定性问题：把原始凭证的数字拿过来重新加一遍，对不上就是对不上，每次结果都一样。让一个非确定性的裁判去判一件本该确定的事，你付出的是两笔钱（多一次模型调用）加一份风险（裁判自己也可能把对的判成错的、把错的放过去）。

建议：写一个验证器，用代码重算每一项合计与勾稽关系，不通过就把「哪一项、算出来多少、应该是多少」这三个数直接扔回给生成节点。这仍然是一个回路，形状和评审回路一样，但回路里的那个节点是纯代码不是 agent——这就是本系列第 10 门课那条分层判分纪律：代码能判的用代码判，代码判不了的才轮到模型。真要给模型留位置，留给「摘要的文字叙述是否和数字一致」这类代码判不了的部分。

**场景三：先别建回路，先去把标准定了。**

标志一就不符合：三个同事的意见互相矛盾，说明不存在一个「人一提就变好」的共识反馈——A 的意见落地了，B 会更不满意。标志二更谈不上，模型没法给出一种连人类之间都没达成一致的反馈。前置条件「评估标准清晰」在这里直接不成立。

如果硬建，你会稳定地拿到那个可预测的失败：裁判每轮的意见方向不同，命名在啰嗦和简略之间来回横跳，轮数烧完，最后一版不比第一版好，而且三个同事仍然各有各的不满——你只是把一场没结论的争论自动化了，还得为每一轮付钱。

建议：让三个人先坐下来把命名规则写成一份能判定的清单（缩写用不用、动词在前还是名词在前、最长几个词、文档用第几人称）。清单一旦存在，标志一和标志二会同时成立，那时候再回来建回路，而且大半条件已经能用关卡判死了。**这个场景的真正教训是：评审回路不产生标准，它只执行标准。**

<!-- hint -->

裁决之前先做一件事：把「人会提的那条意见」原封不动写下来。写完看它长什么样——如果它长得像一条能当场判真假的断言（「这一段少了工单号」），那多半该交给代码；如果它长得像一条需要品味的判断（「语气太软」），才轮到裁判。

<!-- hint -->

任何一个场景卡住的话，试着扮演那个裁判：把你手上那点输入摆出来，看你自己能不能写出一条给写作者的、明确的反馈意见。你写不出来，模型多半也写不出来——那就不是模型的问题，是标准的问题。

<!-- /exercises -->

## 小结

- 评审-优化是一个 LLM 调用生成响应、另一个在回路里提供评估与反馈[^S1]；它在产品里的形态是「跑一个检查器、修掉没过的、重复直到通过或不再有进展」[^S5]，对抗式互审是同一分工的另一种用法（单次交叉互评，不含回边），把它并进评审回路是本课的归类[^S5]。本系列第 6 门课叫它生产者-评审者，那是我们的教学词汇，一手词汇是 evaluator-optimizer[^S1]。
- 值不值得建看两个标志：人把反馈说清楚时 LLM 的响应确实能被明显改进，以及 LLM 自己也能给出这种反馈；评估标准清晰、迭代打磨有可测量价值时它特别有效[^S1]。标准不清晰的时候，先定标准，别先建回路。
- 停止条件不止一种：最大迭代数这类停止条件是用来维持控制的[^S1]，「不再有进展」是另一种更省钱的停法[^S5]；三种结局（通过 / 烧完轮数 / 没进展）对应三种不同的下游动作，别混成一个布尔值。确定性的关卡排在裁判前面。
- 五个模式可以组合：这些构件不是硬性规定，是开发者可以塑形与组合以适配不同用例的常见模式，成功的关键是测量表现并对实现做迭代[^S1]。
- 「图 / 节点 / 边」是本课自己的画法，不是官方术语；它的一手锚点只有一句——工作流脚本自己持有循环、分支与中间结果，模型的上下文只装最终答案[^S5]，加上中间结果留在脚本变量里这一条[^S5]。在自己的文档里用这套词汇时，把这段声明一起写上。
- 组合的红利有据可查：运行时逐个追踪每个代理的结果，这是一次运行能在同一会话内恢复的原因[^S5]；把工作扇给许多小代理比一条长代理保住更多进度[^S5]；把计划搬进代码还能套用可重复的质量手法（对抗式互审、多角度起草再权衡）[^S5]；确定性护栏（重试逻辑与定期检查点）包着非确定性的代理[^S2]。
- 约束同样清楚：只有当复杂度能明显改善结果时才该考虑增加它[^S1]；节点级的重试与超时是普通工程实践，一手只有一个从句的背书[^S2]；深度也有界，产品级的参照是子代理最多嵌套到主对话之下三层[^S4]；最简单的方案优先[^S1]。

[>> 第 6 课：实战：把你的 harness 升级成一张小图](./06-build-a-graph.md)
