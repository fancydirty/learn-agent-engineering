# 레슨 2: 이어 붙이고, 갈라 보내기: 체이닝과 라우팅

> 학습 목표:
> - 네 가지 일을 하는 프롬프트를 체인으로 쪼개고, 무엇을 내주고 무엇을 얻는지 말로 옮기기
> - 체인의 단계 사이에 프로그램적 게이트를 설치해, 기준 미달의 중간 결과가 멈춰야 할 자리에서 멈추게 하기
> - 어떤 작업에 체이닝이 필요한지, 라우팅이 필요한지, 둘 다인지, 아무것도 아닌지 정하고, 라우팅 호출을 출력이 조여진 값싼 한 번의 호출로 쓰기
>
> 전제: 레슨 1을 읽었고, `stop_reason`이 모는 하네스 루프를 손으로 쓸 수 있으며(이 시리즈의 코스 7), 결정론적 검증기를 이해하고 있어야 합니다(이 시리즈의 코스 10) | 이전: [<< 레슨 1](./01-when-one-loop-isnt-enough.md) | 다음: [레슨 3 >>](./03-parallelization.md)

## 하나의 프롬프트가 네 가지 일을 하면 — 하나는 떨어집니다

새 기능의 도움말 문서를 써야 합니다. 제품 요구사항 읽기 → 개요 초안 잡기 → 개요를 바탕으로 본문 쓰기 → 문서에 비권장 용어가 있는지 점검하기.

첫 버전은 아마 이 네 단계를 하나의 프롬프트에 몽땅 밀어 넣고 하네스 루프에 건네줄 것입니다. 첫 실행은 괜찮아 보입니다. 문제는 두 번째 실행, 그리고 세 번째 실행에서 드러납니다. 이번에는 개요는 좋은데 본문에 한 절이 빠져 있고, 다음번에는 본문은 온전한데 지난 버전에 폐기된 '사용자 그룹'이 섞여 있고, 그다음에는 중간에 개요를 다시 쓰고서 본문과 맞지 않는 개요를 내놓습니다.

이 실패들은 겉모습은 다르지만 뿌리는 같습니다. 한 번의 호출 안에서 모델은 요구사항 이해, 구조 설계, 본문 생성, 일관성 점검을 동시에 저글링해야 합니다. 어느 것이 밀려날지 여러분은 통제할 수 없고, 그것이 일어나는 것을 볼 수도 없습니다. 더 나쁘게는 개입할 자리가 없습니다. 출력을 손에 쥐었을 때는 네 가지가 모두 끝난 뒤이고, '개요가 규격에 안 맞았다'는 사실은 최종 원고 안에 묻혀 있습니다.

레슨 1은 '누가 계획을 쥐고 있는가'라는 축을 다뤘습니다. 이 한 방짜리 프롬프트는 계획을 통째로 모델에게 넘깁니다. 이 레슨이 가장 먼저 하는 일은 그것을 되찾아 오는 것입니다.

## 체이닝: 레이턴시를 정확성과 맞바꾸기

이 패턴의 공식 정의는 두 문장뿐이고, 모든 단어가 무게를 지고 있습니다.

프롬프트 체이닝은 작업을 일련의 단계로 분해하며, 각 LLM 호출은 앞선 호출의 출력을 처리합니다. 프로세스가 여전히 궤도 위에 있는지 확인하기 위해, 임의의 중간 단계에 프로그램적 점검(아래 도식의 "gate"를 참고)을 더할 수 있습니다[^S1]. 이 워크플로를 쓸 때: 이 워크플로는 작업을 쉽고 깔끔하게 고정된 하위 작업으로 분해할 수 있는 상황에 이상적입니다. 주된 목표는 각 LLM 호출을 더 쉬운 작업으로 만들어, 레이턴시를 더 높은 정확성과 맞바꾸는 것입니다[^S1].

'각 LLM 호출을 더 쉬운 작업으로 만든다' — 그 반 문장이 진단과 처방을 함께 줍니다. 한 번의 호출이 네 가지를 하는 것은 어려운 작업이고, 한 번의 호출이 개요만 잡고 아무것도 쓰지 않는 것은 쉬운 작업입니다. 체이닝은 일의 양을 줄이지 않습니다. 모델이 한 번의 호출에서 완수해야 하는 일을 더 단순하게 만듭니다.

비용은 가격표에 그대로 찍혀 있습니다. 레이턴시입니다. 단계가 하나 늘 때마다 완전한 왕복이 하나 붙습니다. Anthropic은 같은 글의 첫머리에 이 회계를 펼쳐 놓았습니다 — 에이전트 시스템은 더 나은 작업 성능을 위해 레이턴시와 비용을 맞바꾸는 일이 잦으며, 여러분은 이 맞바꿈이 언제 말이 되는지 따져 봐야 합니다[^S1]. '느리고 비싸다'는 체이닝의 우발적 부작용이 아닙니다.

```text
요구사항 ──▶ 개요 초안 ──▶ 본문 집필 ──▶ 용어 점검 ──▶ 전달
```

각 구간의 입력은 앞 구간의 출력입니다. 한 고리가 비뚤어지면 그 아래로 전부 따라갑니다.

## 체인의 한 단계는 완전한 하네스 루프입니다

체인의 한 단계는 'API 호출 한 번'이 아니라 **완전한 하네스 루프 하나**입니다. 이 시리즈의 코스 7에서 손으로 쓴 그 while 루프와 같습니다. messages를 보내고, `stop_reason`을 확인하고, `tool_use`면 도구를 실행해 결과를 되돌려 보내고, 아니면 텍스트를 돌려줍니다.

이 용법에는 출처가 있습니다. Anthropic이 에이전트를 평가하는 방법을 서술했을 때 권장한 구성이 정확히 이 형태였습니다. 직접적인 LLM API 호출, 단순한 에이전트 루프(LLM API 호출과 도구 호출을 번갈아 감싸는 while 루프), 평가 작업 하나당 루프 하나, 각 평가 에이전트에게는 하나의 작업 프롬프트와 여러분의 도구를 준다[^S3]. 그 글은 평가에 관한 것이었지만, 구성 요소 자체는 범용입니다. 작업 하나, 루프 하나, 코드가 모는 것. 체이닝은 이 블록들을 코드가 순서를 정하는 방식으로 꿰는 것입니다.

나머지 레슨은 같은 표기를 씁니다:

```javascript
// runAgent는 코스 7의 하네스 루프를 함수로 감싼 것
// 입력: 자기완결적인 작업 프롬프트 하나 + 이 단계가 쓸 수 있는 도구, 출력: 끝까지 돌린 뒤의 최종 텍스트
const answer = await runAgent(client, task, tools);
```

체인 전체는 한눈에 읽히는 순차 코드입니다:

```javascript
const outline = await runAgent(client, outlineTask(req));
const g1 = gateOutline(outline);        // 게이트 1: 개요의 구조가 맞는가?
if (!g1.ok) fail(g1.why);               // 아니면 여기서 멈춤 — 아래로 흘려보내지 않는다

const doc = await runAgent(client, writingTask(outline));
const g2 = gateTerms(doc);              // 게이트 2: 금지된 비권장 용어를 담고 있는가?
if (!g2.ok) fail(g2.why);

const report = await runAgent(client, checkTask(doc), { read_glossary });
```

이 줄들에 **없는** 것에 주목하세요. '모델이 다음에 무엇을 할지 정하는' 자리가 없습니다. 순서는 하드코딩되어 있고, 중간 결과 `outline`과 `doc`은 평범한 스크립트 변수입니다. 모델은 각 단계 안에서는 여전히 자율적이지만(도구를 원하는 만큼 호출할 수 있습니다), 단계 사이의 통제권은 코드의 손에 있습니다. 덤으로 얻는 이득이 있습니다. 각 단계의 프롬프트가 한 가지에만 엄격해질 수 있다는 것입니다. 개요 프롬프트는 제목만 요구하고 본문을 금지하며, 집필 프롬프트는 문체와 금지 용어에 집중합니다 — 이 두 벌의 요구사항은 하나의 프롬프트에 담기면 서로 부딪칩니다.

이 형태는 제품에서도 나타납니다. Claude Code의 공식 문서는 여러 단계짜리 워크플로에 대해 Claude가 서브에이전트를 순차적으로 쓰게 하라고 권합니다. 각 서브에이전트가 자기 작업을 마치고 결과를 Claude에게 돌려주면, Claude가 관련 컨텍스트를 다음 서브에이전트에게 넘깁니다[^S4]. 차이는 레슨 1의 축에 착지합니다. 제품 형태에서는 '무엇을 넘길지'를 Claude가 정하고, 여러분이 스크립트를 쓸 때는 그것이 여러분의 코드입니다.

## 게이트: 코스 10의 검증기를 단계 사이로 옮기기

정의의 마지막 반 문장이야말로 '하나의 큰 프롬프트'를 넘어 체이닝이 실제로 더해 주는 것입니다. 프로세스가 여전히 궤도 위에 있는지 확인하기 위해 임의의 중간 단계에 프로그램적 점검을 더할 수 있다[^S1]. 원문은 이 점검을 "gate"라 부르며, 게다가 따옴표에 넣어 놓았습니다. `(see "gate" in the diagram below)` — 한쪽은 곧은 따옴표, 한쪽은 굽은 따옴표로, 출처 그대로입니다. 여기서의 오타가 아닙니다.

'프로그램적'이 핵심입니다. 이것은 코드이지 또 한 번의 모델 호출이 아니며, `if` 몇 줄이면 됩니다.

이 시리즈의 코스 10은 결정론적 검증기를 가르쳤습니다. 코드로 옳고 그름을 가릴 수 있는 것에는 돈을 써서 모델에게 묻지 말라는 것입니다. 그 코스는 검증기를 **종단 상태**에 설치했습니다. 모든 것이 돌아간 뒤에 출력이 받아들일 만한지 확인하는 것이죠. 체이닝은 같은 점검에 새 자리를 하나 줍니다. **단계 사이**입니다.

```javascript
function gateTerms(doc) {
  const hits = BANNED.filter((w) => doc.includes(w));
  if (hits.length > 0) {
    return { ok: false, why: `문서에 금지된 비권장 용어가 들어 있습니다: ${hits.join(', ')}` };
  }
  return { ok: true };
}
```

일곱 줄, 모델 호출은 한 번도 없고, 같은 입력이면 언제나 같은 판정이 나옵니다. 바로 그 '본문에 비권장 용어가 섞이는' 실패를 막아 줍니다. 개요에 챕터 제목이 몇 개인지 세는 게이트도 그만큼 단순합니다.

게이트가 실패했을 때 무엇을 할지는 설계 결정입니다. **멈추고 에러를 보고하기**(이 체인을 아직 조율하는 중이라면 가장 낫지만, 실패 메시지가 어느 단계에서 실패했는지 말해 줘야 합니다. 그러지 않으면 '안 됐다'만 알 뿐 어느 단계의 프롬프트를 고쳐야 할지 모릅니다), **실패 이유를 같은 단계의 프롬프트에 되먹여 재시도하기**(재시도 상한과 함께), 아니면 **기록해 두고 대체값으로 계속 진행하기**(이 단계가 하중을 지지 않을 때만). 레벨 2 연습은 첫 번째 방식을 씁니다.

이것은 또한 이 시리즈 코스 6의 회계를 정리해 줍니다. 여러분이 위임하는 일에는 자기완결적인 프롬프트가 있어야 합니다 — 목표, 출력 형식, 쓸 수 있는 도구, 경계, 이 네 가지를 모두 적어야 합니다. 각 단계의 작업은 정확히 그렇게 생긴 위임 프롬프트입니다. 이 네 요소에는 정확한 1차 출처가 있고, 레슨 4가 오케스트레이터의 위임을 다룰 때 항목별로 펼쳐 볼 것입니다.

## 라우팅: 먼저 분류하고, 그다음 갈라 보내기

체이닝은 '하나의 작업을 단계로 쪼개는 것'을 다룹니다. 완전히 다른 형태의 작업 부류가 하나 더 있습니다. 들어오는 것이 한 가지가 아니라 여러 종류이고, 각각 자기만의 처리를 갖는 경우입니다.

공식 정의는 이렇습니다. 라우팅은 입력을 분류해 전문화된 후속 작업으로 보냅니다. 이 워크플로는 관심사의 분리와 더 전문화된 프롬프트의 구축을 가능하게 합니다. 이 워크플로가 없으면 한 종류의 입력에 맞춘 최적화가 다른 입력에서의 성능을 해칠 수 있습니다[^S1].

마지막 문장이 라우팅이 존재하는 이유입니다. 고객 메일이 환불, 장애, 청구 세 범주로 나뉜다고 합시다. 여러분은 하나의 프롬프트로 전부 처리합니다. 환불을 잘 처리하려고 '먼저 주문 번호와 결제 수단을 확인할 것'이라는 줄을 더합니다. 이 규칙은 장애 메일에는 순수한 잡음이고, 모델은 이것을 써서 화면이 하얗게 됐다고 신고한 사람에게 결제 수단을 묻습니다. 여러분은 '장애면 주문 번호를 묻지 말 것'이라는 줄을 또 더하고, 프롬프트는 덧대기 위에 덧대기를 키우기 시작합니다.

이 워크플로를 쓸 때: 라우팅은 따로 다루는 편이 나은 뚜렷한 범주들이 있고, 분류 자체를 LLM으로든 더 전통적인 분류 모델이나 알고리즘으로든 정확하게 다룰 수 있는 복잡한 작업에 잘 맞습니다[^S1]. 그 마지막 전제 조건은 케이크 위의 장식이 아닙니다. 분류가 틀리면 은밀한 방식으로 틀립니다. 장애 흐름으로 라우팅된 환불 메일은 성실한 문제 해결 답변을 받게 됩니다.

코드에서 라우팅은 체이닝보다 단순합니다:

```javascript
const HANDLERS = {
  refund: handleRefund,
  incident: handleIncident,
  billing: handleBilling,
  other: handleFallback,
};
const LABELS = Object.keys(HANDLERS);

async function classify(client, letter) {
  const raw = await runAgent(client, [
    '목표: 아래 고객 메일을 분류해 줘.',
    `출력 형식: 다음 중에서 고른 한 단어만 출력해: ${LABELS.join(', ')}. 설명 없이, 문장부호 없이.`,
    `메일: ${letter}`,
  ].join('\n'));

  const label = raw.trim();
  return LABELS.includes(label) ? label : 'other';   // 표에 없으면 폴백으로
}

const category = await classify(client, letter);
const reply = await HANDLERS[category](client, letter);
```

주목할 것이 세 가지입니다.

**분류 호출의 출력을 한 단어로 조이세요** — 이 시리즈의 코스 10이 LLM 판정자를 논할 때 같은 수법을 썼습니다. 허용되는 값을 나열하고, 설명하지 말라고 하는 것. 출력을 조이면 파싱 단계가 결정론적이 됩니다. **표에 없으면 폴백으로** — `LABELS.includes(label) ? label : 'other'` 이 한 줄은 방어적인 현학이 아닙니다. 모델은 이따금 '아마 incident 같은데, 아니면 다른 것일 수도'라고 돌려주고, 그러면 `HANDLERS[그 문자열 전체]`는 `undefined`이며 다음 줄에서 터집니다. 폴백 가지를 하나 남겨 두면 분류의 불확실성이 이 한 줄 안에 갇힙니다.

**분류기가 꼭 모델일 필요는 없습니다** — 정의는 전통적인 분류 모델이나 알고리즘도 해당한다고 분명히 말합니다[^S1]. 메일이 고정된 주문 번호 형식을 달고 오거나 전용 폼 진입점에서 온다면, 정규식 하나로 충분하고 훨씬 빠릅니다.

갈라 보낸 뒤에는 각 핸들러가 무엇이든 될 수 있습니다. 하네스 루프여도 되고, 체인이어도 되고, 모델이 전혀 없는 코드 조각이어도 됩니다.

```agentmentor-check
{
  "id": "orc-zh-02-chain-vs-one-prompt",
  "label": "세 번의 호출 대 하나의 큰 프롬프트",
  "prompt": "문서 집필 체인을 세 단계로 쪼갰습니다. 동료가 읽어 보고 말합니다. '세 단계로 쪼개면 모델을 세 번 부르는 거잖아 — 느리고 비싸. 큰 프롬프트 하나로 모델이 세 단계를 스스로 따라가게 하면 되지 않아?' 어떻게 답하겠습니까?",
  "whyHere": "체인, 게이트, 라우팅을 모두 다뤘습니다. 비용 회계와 쪼개기의 전제 조건을 함께 맞춰 놓기에 알맞은 자리입니다. 양쪽 다 절반만 맞습니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "동료가 맞습니다. 요즘 모델은 충분히 강해서, 프롬프트 하나에 단계를 분명히 써 두면 모델이 순서대로 따라갑니다. 쪼갠 것과 효과는 같으면서 왕복 두 번의 시간과 돈을 아낍니다.",
      "correct": false,
      "feedback": "이것이 바로 첫머리 시나리오가 실패한 형태입니다. 체이닝에 대한 공식의 자리매김은 각 LLM 호출을 더 쉬운 작업으로 만드는 것입니다. 한 번의 호출 안에서는 모델이 여전히 요구사항 이해, 구조 설계, 본문 생성, 용어 점검을 동시에 저글링해야 하고, 어느 것이 밀려날지 여러분은 통제할 수 없습니다."
    },
    {
      "id": "b",
      "text": "쪼개는 쪽이 언제나 낫습니다. 모델 호출이 많을수록 생각할 기회가 많아집니다. 작업을 깔끔하게 분해할 수 있는지는 상관없습니다 — 최대한 쪼개면 정확성은 자연히 올라갑니다.",
      "correct": false,
      "feedback": "너무 단정적입니다. 체이닝의 공식 전제 조건은 작업을 쉽고 깔끔하게 고정된 하위 작업으로 분해할 수 있어야 한다는 것입니다. 깔끔하게 쪼갤 수 없는 작업은 단계 사이에 정보가 끼여 잃게 됩니다. 동료가 걱정하는 비용도 실제 돈입니다."
    },
    {
      "id": "c",
      "text": "느리고 비싼 것은 사실입니다 — 체이닝의 자리매김이 바로 레이턴시를 정확성과 맞바꾸는 것이니까요. 하지만 큰 프롬프트 하나에는 프로그램적 점검을 놓을 자리가 없어서, 기준 미달의 개요가 끝까지 흘러 내려갑니다.",
      "correct": true,
      "feedback": "정답입니다. 두 가지를 모두 인정하고 있습니다. 비용은 체이닝이 명시적으로 값을 매겨 둔 것이고, 쪼개서 얻는 것은 호출마다 작업이 쉬워지는 것만이 아니라 프로그램적 점검을 설치할 수 있는 자리이기도 합니다. 한 방짜리 프롬프트는 개입할 자리조차 주지 않습니다. 전제 조건도 중요합니다. 깔끔하게 쪼갤 수 없다면 억지로 쪼개지 마세요."
    }
  ]
}
```

## 현행 API 어휘 속의 라우팅 두 변종

위의 라우팅 정의는 2024년 말의 패턴 글에서 왔고, 그 글에는 도구 생태계 서술이 낡았다는 배너가 달려 있습니다. 그러니 확인해 볼 값어치가 있습니다. 이 패턴은 현행 퍼스트파티 어휘 안에서도 살아 있는가? 그렇습니다, 게다가 이름으로 불립니다. Claude 플랫폼의 멀티 에이전트 오케스트레이션 문서에는 라우팅에 해당하는 항목이 둘 있습니다:

- **전문화(Specialization)**: 하나의 에이전트에 모든 능력을 싣는 대신, 보안 에이전트나 문서 에이전트처럼 도메인에 집중한 시스템 프롬프트와 도구를 가진 에이전트로 라우팅합니다[^S6]. 이것이 `HANDLERS` 표의 공식 표현입니다.
- **에스컬레이션(Escalation)**: 복잡한 하위 작업의 일부에 대해 더 유능한 에이전트나 모델에 자문합니다[^S6].

두 번째 것은 따로 언급할 값어치가 있습니다. 이것은 **주제**가 아니라 **난이도**로 갈라 보냅니다. 분류기가 판정하는 것은 '이것이 환불인가 장애인가'가 아니라 '이 메일을 내 값싼 티어로 처리할 수 있는가'입니다. 이것은 주제 분류보다 정확하게 판정하기가 더 어려우므로, 에스컬레이션 경로에는 더 안정적인 쓰기 방식이 있습니다. 값싼 티어를 먼저 돌리고, 출력이 게이트를 통과하지 못하면 에스컬레이트하는 것입니다 — 판정하기 어려운 분류 문제를 점검 가능한 검증 문제로 바꾸는 것이죠.

## 쪼개지 말아야 할 때를 아는 것

**체인의 고리 하나하나가 레이턴시를 더합니다.** 이것은 최적화되지 않은 구현이 아니라 공식 정의가 매겨 둔 가격입니다. 레이턴시를 더 높은 정확성과 맞바꾸는 것[^S1]. 사용자는 모든 구간의 합만큼 기다립니다. 사용자가 UI에서 결과를 동기적으로 기다리고 있다면, 체인에 고리를 하나 더 붙이기 전에 그 사람이 아직 거기 있을지 생각해 보세요.

**범주가 하나뿐일 때 라우팅은 순수한 간접비입니다.** 라우팅의 이득은 관심사의 분리에서 옵니다[^S1]. 입력이 정말 한 종류뿐이라면, 분류 호출의 비용과 레이턴시를 치르고 돌려받는 것은 아무것도 없으며, 잘못 분류할 기회만 하나 더 생깁니다.

**작업을 깔끔하게 쪼갤 수 없을 때는 억지로 쪼개지 마세요.** '쉽고 깔끔하게 고정된 하위 작업으로 분해할 수 있어야 한다'는 조건에는 이빨이 있습니다[^S1]. 잘 고치려면 전체 그림을 앞뒤로 봐야 하는 초안을 '먼저 구조를 고치고, 그다음 표현을 고친다'로 쪼개면, 두 번째 단계는 첫 번째 단계가 적어 두지 않은 이유들에 접근할 수 없고 문자 그대로의 텍스트만 보고 고칠 것입니다. 이 경우에는 하나의 루프, 하나의 컨텍스트가 실제로 더 낫습니다 — 이것이 정확히 레슨 1의 역방향 조건이 쓰이는 자리입니다.

**확신이 없으면 먼저 재세요.** Anthropic은 이 말을 두 번 했습니다. 복잡성을 더하는 것은 그것이 결과를 명백히 개선할 때에만 고려하라[^S1]. 이 시리즈 코스 10의 평가 트랙은 이것을 위해 만들어졌습니다. 단일 프롬프트 버전을 돌려 점수를 하나 얻고, 쪼갠 버전을 돌려 또 하나를 얻고, 차이가 얼마나 나는지 그리고 그 몇 초의 추가 레이턴시만 한지 보는 것 — 그것이 동료와의 논쟁에 들고 갈 수 있는 증거입니다.

마지막으로 경계를 그어 둡니다. 체이닝과 라우팅은 둘 다 **형태가 고정된** 오케스트레이션입니다. 체인에 단계가 몇 개인지, 라우터에 범주가 무엇인지, 모두 여러분이 코드를 쓸 때 정해집니다. 여러 단계를 동시에 돌리고 그다음 집계하는 것은 레슨 3의 병렬화이고, 입력을 보기 전에는 하위 작업이 몇 개인지조차 모르는 것은 레슨 4의 오케스트레이터-워커입니다.

## 💻 연습

<!-- exercises -->

### 레벨 1: 체이닝인지, 라우팅인지, 아무것도 아닌지 정하기(코드 없음)

아래 네 작업 각각에 대해 체이닝을 쓸지, 라우팅을 쓸지, 둘 다일지, 아무것도 아닐지 정하세요. 결론 말고도 이것을 분명히 쓰세요. 체이닝이라 판단했다면 단계 사이의 프로그램적 게이트는 무엇을 점검합니까? 라우팅이라 판단했다면 분류기는 어디에 놓이고, 범주는 몇 개이며, 모델입니까 코드입니까?

1. 고객 메일함에 매일 수백 통이 들어오고, 환불/장애/청구 세 범주의 처리가 완전히 다른데, 지금은 하나의 프롬프트를 함께 쓰고 있어서 한 범주의 표현을 바꾸면 다른 범주에 영향을 준다.
2. 영문 계약서를 먼저 구조화된 리스크 의견서로 검토해야 하고(각 항목에 조항 위치, 리스크 등급, 설명 포함), 그다음 이 의견서를 팀별로 한국어와 일본어 버전으로 번역해야 한다.
3. 뒤섞인 티켓 큐: 한 단계로 답할 수 있는 '비밀번호 재설정'도 있고, 로그를 확인하고 진단하고 해결책을 제안하고 답변을 써야 하는 '데이터 마이그레이션 실패' 같은 여러 단계짜리 작업도 있다.
4. 사용자가 입력창에 한 문장을 치면, 오타를 고쳐서 교정된 문장을 돌려줘야 한다.

<!-- rubric -->

- 네 결론이 각각 라우팅 / 체이닝 / 둘 다 / 아무것도 아님이다
- 작업 1의 근거가 '한 종류의 입력에 맞춘 최적화가 다른 입력에서의 성능을 해칠 수 있다'에 착지하고, 범주 집합을 제시하며, 거기에 폴백 범주가 포함되어야 한다
- 작업 2는 두 번역 버전에 순차 의존이 없다는 점을 짚어야 하고(병렬화는 레슨 3의 몫이므로 알아보는 것으로 충분), 구체적인 게이트를 적어도 하나 제시해야 한다
- 작업 3은 두 층 구조를 설명해야 한다. 바깥 층은 복잡도로 라우팅하고, 복잡하다고 판정된 가지가 안쪽에서 체인이 된다
- 작업 4는 '호출 한 번이면 충분하다'를 분명히 말하고, 근거가 쪼개기의 레이턴시가 이득에 비례하지 않는다는 데 착지해야 한다
- 모든 게이트는 순수 코드로 점검 가능해야 한다. '모델에게 괜찮은지 보게 하기'는 안 된다

<!-- answer -->

**1. 고객 메일을 주제로 갈라 보내기 — 라우팅.**

기준이 사용 사례와 정확히 맞습니다. 따로 다루는 편이 나은 뚜렷한 범주들이 있고, 분류 자체를 정확하게 다룰 수 있습니다[^S1]. 문제에 나온 '한 범주의 표현을 바꾸면 다른 범주에 영향을 준다'는 문장은 '이 워크플로가 없으면 한 종류의 입력에 맞춘 최적화가 다른 입력에서의 성능을 해칠 수 있다'는 공식 진술의 생중계판입니다[^S1]. 분류기는 맨 앞에 놓고, 범주 집합은 `refund / incident / billing / other`이며, 'other'가 앞의 셋에 맞지 않는 메일과 모델이 기대한 형식으로 답하지 않은 경우를 받아 냅니다. 값싼 모델 호출 한 번, 출력은 한 단어로 조입니다. 환불 메일 대부분이 고정된 애프터서비스 폼 진입점에서 온다면, 그 경로는 순수 코드 규칙으로 먼저 지름길을 낼 수 있습니다.

**2. 계약서 검토로 구조화된 의견서를 낸 뒤 번역 — 체이닝.**

작업이 고정된 하위 작업으로 깔끔하게 쪼개집니다. 검토, 번역. 두 번째 단계의 입력이 첫 번째 단계의 출력이며, 이것이 정확히 체이닝의 정의 형태입니다[^S1]. 두 언어 사이에는 순차 의존이 없습니다 — 이 '같은 단계를 여러 항목에 걸쳐 반복하는' 형태는 레슨 3 병렬화의 영역이며, 여기서는 그것을 알아보고 두 개의 직렬 단계로 쓰지 않기만 하면 됩니다.

게이트는 검토와 번역 사이에 놓고, 내용이 아니라 구조를 점검하며, 전부 순수 코드입니다. 출력이 `JSON.parse`로 배열까지 파싱될 것, 각 항목에 `clause`, `level`, `note` 세 필드가 있을 것, `level`이 `high / medium / low` 집합 안에 들 것, 배열 길이가 0보다 클 것(의견이 0건으로 검토된 계약서는 완벽한 계약서라기보다 앞 단계가 궤도를 벗어났을 가능성이 큽니다). 번역 단계는 자기 입력이 나쁘다는 것을 발견할 수 없고 그저 그 나쁜 것을 충실히 두 언어로 옮길 뿐이므로, 이 게이트가 상류에서 막아야 합니다.

**3. 뒤섞인 티켓 — 둘 다.**

바깥은 라우팅, 안쪽은 체이닝입니다. 분류기는 큐 입구에 놓고, 판정하는 것은 주제가 아니라 복잡도이며, 범주는 `한 단계로 답 가능 / 진단 필요 / other`로 둘 수 있습니다. '한 단계로 답 가능'은 호출 한 번이나 아예 템플릿으로 직행하고, '진단 필요'는 체인으로 펼쳐집니다. 로그 확인 → 원인 진단 → 해결책 제안 → 답변 작성. 이것은 난이도에 의한 갈라 보내기이고, 공식 어휘의 에스컬레이션과 같은 범주입니다 — 그 항목은 복잡한 하위 작업의 일부에 대해 더 유능한 에이전트나 모델에 자문한다고 말하며[^S6], 여기서는 '더 유능한 에이전트'를 체인으로의 확장으로 바꾼 것입니다.

체인 내부에는 순수 코드 게이트가 적어도 둘 있습니다. 진단 단계의 출력은 로그 한 줄 이상의 참조를 달고 있어야 하고(정규식으로 타임스탬프 형식 점검), 답변 작성 단계의 출력은 해결책 단계가 준 티켓 번호를 담고 있어야 합니다(문자열 포함 점검). 복잡도 분류기 자체를 정확하게 만들기 어렵다면, '일단 한 단계로 답 가능으로 처리하고, 출력이 게이트를 통과하지 못하면 에스컬레이트'로 바꾸세요.

**4. 한 문장 오타 교정 — 아무것도 아님.**

호출 한 번이면 충분합니다. 깔끔하게 분리될 수 있는 고정된 하위 작업이 없고('오류 찾기'와 '오류 고치기'를 따로 하면 두 번째 단계가 어차피 문장 전체를 다시 읽어야 하므로 헛도는 것과 같습니다), 따로 처리해야 할 범주도 없습니다. 쪼개기의 비용은 구체적입니다. 왕복 레이턴시가 하나 더 붙고, 사용자는 입력창을 쳐다보며 결과를 기다리고 있습니다. 체이닝의 자리매김은 레이턴시를 정확성과 맞바꾸는 것이며[^S1], 그 전제는 정확성을 실제로 얻을 수 있다는 것입니다. 이 작업은 얻을 수 없습니다. 품질을 정말 올리고 싶다면 먼저 프롬프트에 예시 몇 개를 더하세요. Anthropic도 패턴에 들어가기 전에 이 길을 남겨 두었습니다. 많은 애플리케이션에서는 검색과 인컨텍스트 예시로 단일 LLM 호출을 최적화하는 것으로 대개 충분합니다[^S1].

<!-- hint -->

패턴을 판단하러 달려가기 전에, 작업마다 두 가지를 먼저 물어보세요. 들어오는 것이 '한 가지'입니까, '여러 종류'입니까? 한 가지라면, 각각이 더 단순하고 순서가 고정된 단계들로 쪼개집니까? 첫 질문이 '여러 종류'라고 답하면 라우팅을 생각하고, 둘째 질문이 '그렇다'고 답하면 체이닝을 생각하세요.

<!-- hint -->

게이트를 쓸 때 스스로 점검하세요. 이 점검을 `if`와 문자열 함수로 쓸 수 있습니까? 그리고 같은 입력이면 언제나 같은 결론이 나옵니까? 쓰다 보니 '이 개요가 충분히 좋은지 모델에게 판단하게 하기'가 되고 있다면, 그것은 이 레슨이 말하는 프로그램적 점검이 아닙니다. 그것은 리뷰 루프이고, 레슨 5의 몫입니다.

### 레벨 2: 3단계 체인을 써서 돌려 보기(코드 작성)

첫머리의 문서 집필 작업을 3단계 체인으로 구현하는 `chain.mjs`를 쓰세요. **개요 초안 → 개요를 바탕으로 본문 집필 → 용어 일관성 점검**, 그 사이에 게이트 둘. 요구사항:

1. 스텁 client를 쓸 것(이 시리즈의 코스 8~11에서 쓴 방식): 고정된 응답 큐, 네트워크 없음, 비용 없음, 같은 입력이면 언제나 같은 결과.
2. `runAgent(client, task, tools)`는 진짜 하네스 루프일 것 — `stop_reason`으로 판정하고, `tool_use`면 도구를 실행해 결과를 되돌려 보낼 것. 3단계는 도구 호출을 실제로 한 번 거쳐야 함(용어집 읽기).
3. 게이트 1: 개요에 `## `로 시작하는 챕터 제목이 정확히 3개 있어야 함. 게이트 2: 본문에 금지된 비권장 용어가 없어야 함(두세 단어짜리 표를 직접 정의). 둘 다 순수 코드, 모델 호출 추가 없음.
4. 게이트가 실패하면 어느 **단계**에서 왜 실패했는지 출력하고 0이 아닌 코드로 종료할 것. 세 단계가 모두 통과하면 0으로 종료.
5. 고정 응답을 두 벌 준비해 두 번 돌릴 것. 한 번은 전부 통과, 한 번은 두 번째 게이트에서 멈춤. 두 실행의 실제 출력과 종료 코드를 모두 붙여 넣을 것.

<!-- rubric -->

- 스텁 client가 FIFO 응답 큐이고, 큐가 비면 조용히 `undefined`를 돌려주는 대신 에러를 내야 한다(그러지 않으면 체인이 끝까지 돌았다고 여기지만 실제로는 헛돈 것이다)
- `runAgent`에 진짜 `while` 루프와 `stop_reason` 분기가 있고, `await` 한 번 뒤에 돌아오지 않는다. 3단계의 응답 시퀀스에 `tool_use`가 들어 있어 도구 가지가 실제로 실행된다
- 두 게이트 함수 모두 client를 부르지 않고, 반환값이 실패 이유를 달고 있으며, 같은 입력이면 언제나 같은 출력이다
- 실패 메시지가 어느 두 단계 사이인지와 구체적으로 어느 단어가 적중했는지를 적는다. '실패'만 출력하는 것은 해당하지 않는다
- 종료 코드: 전부 통과 0, 게이트 실패 0이 아님(파라미터 오류 등에는 다른 0이 아닌 코드를 줘도 된다)
- 두 번째 응답 벌이 게이트 2를 발동시키고, 3단계는 정말로 실행되지 않았다(출력에 그 단계의 출력이 없다)
- 붙여 넣은 출력이 실제로 실행된 것이다. 단계 번호, 적중 단어, 종료 코드가 모두 스크립트와 맞는다

<!-- answer -->

완전한 스크립트:

```javascript
// chain.mjs — 3단계 체인: 개요 초안 → 본문 집필 → 용어 점검
// 이 레슨이 만든 예시 스크립트입니다. 1차 자료에는 오케스트레이션 스크립트의 코드 예시가 없습니다
// 사용법: node chain.mjs pass    (세 단계 모두 통과)
//         node chain.mjs stale   (두 번째 게이트에서 멈춤)

const BANNED = ['사용자 그룹', '하위 계정'];

// ---------- 스텁 client: 고정된 응답 큐, 네트워크 없음 ----------
const say = (t) => ({ stop_reason: 'end_turn', content: [{ type: 'text', text: t }] });
const useTool = (id, name, input = {}) => ({
  stop_reason: 'tool_use',
  content: [{ type: 'tool_use', id, name, input }],
});

function makeStubClient(script) {
  const queue = [...script];
  return {
    messages: {
      async create() {
        if (queue.length === 0) throw new Error('스텁 큐가 비었습니다: 스크립트가 미리 정해 둔 것보다 많이 모델을 불렀습니다');
        return queue.shift();
      },
    },
  };
}

// ---------- 한 단계 = 하나의 완전한 하네스 루프 ----------
async function runAgent(client, task, tools = {}) {
  const messages = [{ role: 'user', content: task }];
  while (true) {
    const res = await client.messages.create({
      model: 'claude-sonnet-4-5',
      max_tokens: 2048,
      messages,
      tools: Object.values(tools).map((t) => t.schema),
    });
    messages.push({ role: 'assistant', content: res.content });

    if (res.stop_reason !== 'tool_use') {
      return res.content
        .filter((b) => b.type === 'text')
        .map((b) => b.text)
        .join('\n')
        .trim();
    }

    const results = [];
    for (const block of res.content) {
      if (block.type !== 'tool_use') continue;
      const tool = tools[block.name];
      results.push({
        type: 'tool_result',
        tool_use_id: block.id,
        content: tool ? await tool.run(block.input) : `모르는 도구: ${block.name}`,
      });
    }
    messages.push({ role: 'user', content: results });
  }
}

// ---------- 두 게이트: 순수 코드, 모델에게 묻지 않음 ----------
function gateOutline(outline) {
  const headings = outline.split('\n').filter((l) => l.startsWith('## '));
  if (headings.length !== 3) {
    return { ok: false, why: `개요에는 챕터 제목이 정확히 3개 필요한데, 실제로는 ${headings.length}개입니다` };
  }
  return { ok: true, detail: `챕터: ${headings.map((h) => h.slice(3)).join(' / ')}` };
}

function gateTerms(doc) {
  const hits = BANNED.filter((w) => doc.includes(w));
  if (hits.length > 0) {
    return { ok: false, why: `문서에 금지된 비권장 용어가 들어 있습니다: ${hits.join(', ')}` };
  }
  return { ok: true, detail: `금지 용어 목록 ${BANNED.length}개, 적중 0개` };
}

function fail(where, why) {
  console.error(`\n✗ gate failed @ ${where}`);
  console.error(`  이유: ${why}`);
  console.error('  체인은 여기서 멈추고, 이후 단계는 실행되지 않습니다.');
  process.exit(1);
}

// ---------- 도구 ----------
const glossary = {
  schema: {
    name: 'read_glossary',
    description: '제품 용어집을 읽어 "옛 용어 → 새 용어" 대응을 돌려준다',
    input_schema: { type: 'object', properties: {}, required: [] },
  },
  run: async () => '사용자 그룹 → 팀 스페이스\n하위 계정 → 멤버 계정\n승인 흐름 → 워크플로',
};

// ---------- 세 벌의 고정 응답 ----------
const OUTLINE = ['## 권한 모델', '## 팀 스페이스 만들기와 멤버 관리', '## 자주 묻는 질문'].join('\n');

const CLEAN_DOC = [
  '## 권한 모델',
  '멤버 계정은 각각 하나의 팀 스페이스에 속하며, 권한은 팀 스페이스 안의 역할로 정해집니다.',
  '## 팀 스페이스 만들기와 멤버 관리',
  '관리자는 콘솔에서 팀 스페이스를 만든 뒤, 멤버 계정을 초대해 합류시킬 수 있습니다.',
  '## 자주 묻는 질문',
  '멤버 계정은 여러 팀 스페이스에 동시에 속할 수 있고, 권한은 합집합이 됩니다.',
].join('\n');

const STALE_DOC = CLEAN_DOC.replace('멤버 계정을 초대해', '하위 계정을 초대해');

const REPORT = '용어 일관성 점검 통과: 전문이 "팀 스페이스"와 "멤버 계정"으로 통일되어 있고, 비권장 용어의 혼입은 없습니다.';

const SCRIPTS = {
  pass: [say(OUTLINE), say(CLEAN_DOC), useTool('t1', 'read_glossary'), say(REPORT)],
  stale: [say(OUTLINE), say(STALE_DOC)],
};

// ---------- 체인 본체 ----------
const mode = process.argv[2] ?? 'pass';
if (!SCRIPTS[mode]) {
  console.error(`모르는 모드: ${mode} (가능한 값: ${Object.keys(SCRIPTS).join(', ')})`);
  process.exit(2);
}
const client = makeStubClient(SCRIPTS[mode]);
const req = '"팀 스페이스" 기능의 도움말 문서를 쓴다. 독자는 권한을 처음 설정하는 관리자다.';

console.log(`모드: ${mode}`);

console.log('[1/3] 개요 초안');
const outline = await runAgent(client, [
  '목표: 아래 요구사항으로 도움말 문서의 개요를 잡아 줘.',
  '출력 형식: 챕터 제목만, 한 줄에 하나씩, "## "로 시작해서 정확히 3개.',
  '도구 안내: 이 단계에는 도구가 필요 없어.',
  '경계: 제목만 나열하고, 본문은 쓰지 마.',
  `요구사항: ${req}`,
].join('\n'));
const g1 = gateOutline(outline);
console.log(g1.ok ? `      게이트 1 통과 — ${g1.detail}` : '      게이트 1 실패');
if (!g1.ok) fail('1단계 → 2단계 (개요 구조)', g1.why);

console.log('[2/3] 개요를 바탕으로 본문 집필');
const doc = await runAgent(client, [
  '목표: 아래 개요를 바탕으로 도움말 문서의 본문을 써 줘.',
  '출력 형식: 개요의 "## " 제목을 유지하고, 절마다 1~2문장.',
  '도구 안내: 이 단계에는 도구가 필요 없어.',
  `경계: 새 용어만 쓰고, 다음은 금지: ${BANNED.join(', ')}.`,
  `개요:\n${outline}`,
].join('\n'));
const g2 = gateTerms(doc);
console.log(g2.ok ? `      게이트 2 통과 — ${g2.detail}` : '      게이트 2 실패');
if (!g2.ok) fail('2단계 → 3단계 (금지된 비권장 용어)', g2.why);

console.log('[3/3] 용어 일관성 점검');
const report = await runAgent(
  client,
  [
    '목표: 아래 문서의 용어가 일관되는지 점검해 줘.',
    '출력 형식: 한 문장 결론.',
    '도구 안내: 먼저 read_glossary를 호출해 용어표를 받고, 그다음 전문과 대조해.',
    '경계: 용어 문제만 보고하고, 문서를 다시 쓰지는 마.',
    `문서:\n${doc}`,
  ].join('\n'),
  { read_glossary: glossary },
);

console.log(`\n--- 점검 결론 ---\n${report}`);
console.log('\n✓ 세 단계, 두 게이트 모두 통과.');
process.exit(0);
```

첫 번째 실행, 세 단계 모두 통과(Node v26.3.0):

```text
$ node chain.mjs pass
모드: pass
[1/3] 개요 초안
      게이트 1 통과 — 챕터: 권한 모델 / 팀 스페이스 만들기와 멤버 관리 / 자주 묻는 질문
[2/3] 개요를 바탕으로 본문 집필
      게이트 2 통과 — 금지 용어 목록 2개, 적중 0개
[3/3] 용어 일관성 점검

--- 점검 결론 ---
용어 일관성 점검 통과: 전문이 "팀 스페이스"와 "멤버 계정"으로 통일되어 있고, 비권장 용어의 혼입은 없습니다.

✓ 세 단계, 두 게이트 모두 통과.
$ echo $?
0
```

두 번째 실행, 집필 단계가 비권장 용어가 든 본문을 내놓아 게이트 2에서 멈춤:

```text
$ node chain.mjs stale
모드: stale
[1/3] 개요 초안
      게이트 1 통과 — 챕터: 권한 모델 / 팀 스페이스 만들기와 멤버 관리 / 자주 묻는 질문
[2/3] 개요를 바탕으로 본문 집필
      게이트 2 실패

✗ gate failed @ 2단계 → 3단계 (금지된 비권장 용어)
  이유: 문서에 금지된 비권장 용어가 들어 있습니다: 하위 계정
  체인은 여기서 멈추고, 이후 단계는 실행되지 않습니다.
$ echo $?
1
```

출력과 대조해 볼 값어치가 있는 것이 셋입니다. 두 번째 실행에는 `[3/3]` 줄이 없습니다. 3단계는 정말로 실행되지 않았고, 나쁜 것이 아래로 흘러가지 않았습니다. 실패 메시지가 '2단계 → 3단계'와 구체적인 단어 '하위 계정'을 정확히 짚어 주므로, 집필 단계의 프롬프트를 고치러 가면 된다는 것을 압니다. `stale` 큐에는 응답이 둘뿐입니다 — 언젠가 게이트 2가 실수로 지워지면 스크립트는 3단계로 걸어 들어가 응답을 받지 못하고, 스텁 client가 '스텁 큐가 비었습니다'를 던집니다. 실패 방식이 조용하지 않고 시끄럽습니다. 겸사겸사 청구서도 계산해 두면, `pass` 실행은 모델을 실제로 네 번 불렀습니다. 3단계의 용어 점검이 도구 왕복을 한 번 거쳤으므로, 단계 수는 셋이고 왕복 수는 넷이며, 비용은 왕복 수로 계산해야 합니다.

<!-- hint -->

체인을 잇기 전에 스텁 client와 `runAgent`를 따로따로 돌게 만드세요. `say(...)` 하나만 든 큐로 단계 하나를 돌려 `undefined`가 아니라 텍스트가 돌아오는지 확인하고, 도구 가지는 따로 검증하세요. 큐의 첫 응답이 `tool_use`에서 멈추고 두 번째가 마무리 텍스트가 되게 하면, 루프는 정확히 두 바퀴 돌아야 합니다.

<!-- hint -->

'어느 단계에서 실패했는지'는 쓸 때 `fail()`에 파라미터로 넘겨야 합니다. 나중에 스택에서 보이기를 기대하지 마세요. 게이트마다 고정된 'X단계 → Y단계' 문구를 주고, 실패할 때 적중한 단어와 함께 출력하세요. 종료 코드는 `process.exit(1)`을 쓰고, 그냥 `throw`하지 마세요. `throw`가 내는 것도 종료 코드 1이 맞지만, 스택 정보가 여러분이 공들여 출력한 몇 줄을 덮어 버립니다.

<!-- /exercises -->

## 정리

- 체이닝은 작업을 일련의 단계로 분해해 각 호출이 앞선 출력을 처리하게 하며, 각 호출을 더 쉬운 작업으로 만든다[^S1]. 값은 명시적으로 매겨져 있다. 주된 목표는 레이턴시를 더 높은 정확성과 맞바꾸는 것이다[^S1]
- 체인의 한 단계는 API 호출 한 번이 아니라 완전한 하네스 루프 하나다. Anthropic이 평가를 위해 권장한 구성이 바로 이 구성 요소다. 작업 하나, while 루프 하나, 코드가 직접 API를 호출하는 것[^S3]
- 쪼갠 뒤에 늘어나는 자리가 핵심이다. 프로세스가 여전히 궤도 위에 있는지 확인하기 위해 임의의 중간 단계에 프로그램적 점검을 더할 수 있다[^S1]. 이것은 코스 10의 결정론적 검증기를 설치 위치만 바꿔 옮긴 것이다. 종단 상태에서 단계 사이로. 제품 형태로는 서브에이전트를 순차 실행하고 각자가 마치면 상위 층이 관련 컨텍스트를 다음으로 넘기는 모습이 된다[^S4]
- 라우팅은 입력을 분류해 전문화된 후속 작업으로 갈라 보내며, 관심사의 분리와 더 전문화된 프롬프트를 얻는다. 그것이 없으면 한 종류의 입력에 맞춘 최적화가 다른 입력에서의 성능을 해칠 수 있다[^S1]. 전제 조건은 뚜렷한 범주가 있고 분류 자체를 LLM으로든 전통적인 분류 알고리즘으로든 정확하게 다룰 수 있다는 것이다[^S1]
- 분류 호출의 출력은 라벨 하나로 조이고, 표에 없는 답을 받아 낼 폴백 가지를 하나 남긴다
- 이 패턴은 현행 퍼스트파티 어휘 안에서 살아 있다. 전용 프롬프트와 도구를 가진 에이전트로 도메인에 따라 갈라 보내는 것을 전문화라 부르고, 복잡한 하위 작업의 일부에 대해 더 유능한 에이전트나 모델에 자문하는 것을 에스컬레이션이라 부른다[^S6]. 뒤엣것은 난이도로 갈라 보내는 라우팅이다
- 억지로 하지 말 것: 범주가 하나뿐일 때 라우팅은 순수한 간접비이고, 작업을 깔끔하게 쪼갤 수 없을 때 억지로 쪼개면 단계 사이에서 정보를 잃는다[^S1]. 확신이 없으면 먼저 재라 — 복잡성은 '결과를 명백히 개선한다'는 문턱을 넘어야 한다[^S1]

[>> 레슨 3: 병렬화: 섹셔닝과 투표](./03-parallelization.md)
