# 레슨 1: 하나의 루프로는 부족할 때

> 학습 목표:
> - 단일 루프 에이전트를 무너뜨리는 실제 작업을 써서, '더 큰 윈도를 쓰면 된다'가 형태의 문제를 풀지 못하는 이유 보이기
> - 워크플로와 에이전트의 아키텍처적 구분을 진술하고, '누가 계획을 쥐고 있는가'로 임의의 시스템을 그 축 위에 놓기
> - 위로 올라갈 트리거와 그대로 머물 조건을 열거하되, 레이턴시와 토큰 비용을 먼저 테이블에 올리기
>
> 전제: 이 시리즈의 앞선 11개 코스를 마쳤고, `stop_reason`이 모는 하네스 루프를 손으로 쓸 수 있어야 합니다(코스 7 '에이전트 하네스 기초: 루프와 통제') | 다음: [레슨 2 >>](./02-chaining-and-routing.md)

## 모듈 30에 이르면, 뭔가 잘못됩니다

백엔드 리포지토리를 사내 RPC 프레임워크 v1에서 v2로 마이그레이션해야 합니다. 착수하기 전에 인벤토리가 필요합니다. 리포지토리에는 모듈이 40개 있고, 모듈마다 마이그레이션 평가가 있어야 합니다 — 위험 지점을 열거하고, 변경 범위를 추산하고, 의존성 매니페스트를 붙이는 일입니다. 어려운 작업은 아닙니다, 그저 수가 많을 뿐입니다. 손에는 이 시리즈의 코스 7에서 만든 그 하네스가 있습니다:

```javascript
while (response.stop_reason === "tool_use") {
  messages.push({ role: "assistant", content: response.content });
  const toolResults = await runToolUses(response.content);
  messages.push({ role: "user", content: toolResults });
  response = await client.messages.create({ model, max_tokens, tools, messages });
}
```

(하네스란 모델 호출을 감싸는 호스트 쪽 코드입니다. 요청을 보내고, 모델이 쓰고 싶어 한 도구를 실행하고, 결과를 다시 밀어 넣고, 계속할지 말지를 결정합니다.)

40개 모듈 목록을 한꺼번에 건네주고, '하나씩 평가하고, 모듈마다 보고서 하나씩'이라고 써 놓은 뒤, 커피를 내리러 갑니다.

처음 다섯 모듈은 훌륭합니다. 호출 지점을 grep으로 훑고, 설정을 읽고, 테스트 파일을 확인하고, 보고서는 기대했던 것보다 상세합니다.

모듈 15쯤에서 돌아와 로그를 확인합니다. `messages` 배열은 이미 장관입니다. 앞선 14개 모듈의 grep 출력, 통째로 읽어 들인 설정 파일 덩어리, 파일 쓰기 성공 영수증, 갔다가 버린 경로의 흔적 — 이 모든 것이 아직도 그 하나의 타임라인 위에 얹혀 있습니다. 이 내용 중에 틀린 것은 없었습니다. 각각은 그 시점에 필요했습니다. 그러나 지금의 기능은 하나로 줄어들었습니다. 자리를 차지하는 것.

모듈 30에 이르면 품질이 무너집니다. 이름이 비슷하다는 이유로 모듈 27의 결론을 모듈 30에 복사해 붙이고, 그전까지 매번 하던 '커스텀 인터셉터 확인' 단계를 조용히 건너뛰고, 모듈 34쯤에서는 출력 형식마저 흔들리기 시작합니다.

첫 반응은 아마 이럴 것입니다. 더 큰 컨텍스트 윈도를 가진 모델로 갈아타기.

그 반응은 절반까지만 데려다줍니다. 윈도를 두 배로 키우면 붕괴 지점은 아마 모듈 30에서 55로 옮겨 갑니다. 다음 리포지토리는 모듈이 120개입니다. 문제를 푼 것이 아니라 집행유예를 산 것입니다.

실제로 바닥난 것은 윈도가 아니라 **'하나의 루프'라는 형태**입니다. 독립적인 40개 항목이 하나의 타임라인, 하나의 주의 예산을 억지로 나눠 쓰고 있습니다. 모듈 30의 평가 품질이 앞선 29개 모듈이 남긴 잔여물의 양에 좌우됩니다 — 이 둘은 서로 아무 상관이 없는데도 말입니다.

이 코스가 하는 일은 그 형태를 갈아 끼우는 것입니다.

## 공식 용어부터 정리합니다

**에이전트는 정교한 작업을 다룰 수 있지만, 그 구현은 대개 단순합니다. 전형적으로는 환경 피드백에 기반해 루프 안에서 도구를 쓰는 LLM일 뿐입니다**[^S1]. 이 시리즈의 코스 7에서 쓴 그 `while`이 바로 그것입니다, 한 줄도 다르지 않습니다. 그러니 자기 위치를 확인해 두세요. 여러분은 이미 에이전트를 만들었고, 이 코스는 0에서 시작하지 않습니다.

한 층 위로. Anthropic은 이 모든 변형을 **에이전트 시스템**(agentic systems, 평범하게 말하면 모델과 도구와 어떤 형태의 제어 흐름으로 조립되어 스스로 여러 단계를 걸어갈 수 있는 시스템)으로 분류하지만, 이 넓은 범주 안에서 중요한 아키텍처적 구분을 하나 긋습니다[^S1]:

- **워크플로란 LLM과 도구가 미리 정의된 코드 경로를 통해 오케스트레이션되는 시스템입니다**[^S1]. '미리 정의된 코드 경로'가 핵심 어구입니다 — 다음에 무슨 일이 일어날지가 코드에 적혀 있습니다.
- **반면 에이전트란 LLM이 자기 프로세스와 도구 사용을 동적으로 방향 지으며, 작업을 어떻게 완수할지에 대한 통제를 계속 쥐고 있는 시스템입니다**[^S1]. 다음에 무슨 일이 일어날지는 모델이 그 자리에서 정합니다.

앞으로 몇 레슨에 걸쳐 반복해서 쓸 기초 용어가 하나 더 있습니다. **컴퓨팅에서 결정론적 시스템은 동일한 입력이 주어지면 매번 동일한 출력을 내지만, 비결정론적 시스템은 — 에이전트처럼 — 같은 시작 조건에서도 다양한 응답을 생성할 수 있습니다**[^S3].

이 정의를 여러분의 `while`에 대 보면 두 가지가 꿰매어져 있는 것이 보입니다. 요청을 어떻게 보낼지, 도구 호출을 어떻게 실행할지, 언제 멈출지 — 이것들은 결정론적이고, 여러분이 코드로 썼습니다. 한편 '다음에 무엇을 grep할지, 이 보고서를 다 썼는지' — 이것들은 비결정론적이고, 모델이 그 자리에서 정합니다. 앞으로 할 수술은 이 두 절반 사이에서 결정권을 옮기는 일입니다.

## '누가 계획을 쥐고 있는가'라는 축

Claude Code의 문서는 이것을 더 직접적으로 묻습니다. **서브에이전트, 스킬, 에이전트 팀, 워크플로는 모두 여러 단계짜리 작업을 실행할 수 있습니다. 차이는 누가 계획을 쥐고 있는가입니다**[^S5].

('서브에이전트' = 자기만의 컨텍스트 윈도 안에서 독립적으로 일하고 요약만 돌려주는 어시스턴트. '팬아웃' = 여러 서브에이전트를 동시에 일하게 내보내는 것. 두 용어 모두 이 시리즈의 코스 6에서 다뤘습니다.)

이 한 문장이 비슷해 보이는 여러 접근을 갈라냅니다. '40개 모듈 평가를 실행한다'로 보겠습니다:

- 모델이 하나의 대화 안에서 40개를 전부 진행하게 둡니다 — 계획은 모델의 손에 있고, 게다가 암묵적이며, 대화 이력 속에 숨어 있습니다. 그 의도한 경로를 짐작하려면 로그를 파헤쳐야 합니다.
- 모델을 오케스트레이터로 세우고, 어느 모듈에 어느 서브에이전트를 파견할지 매 턴 모델이 정하게 합니다 — 계획은 여전히 모델의 손에 있고, 다만 조금 더 명시적입니다.
- 스크립트를 하나 씁니다. 그 스크립트가 40개 모듈을 `for`로 직접 돕니다 — 계획은 코드 안에 있습니다. 파일을 열어 끝까지 읽을 수 있고, 내일 그대로 다시 돌릴 수 있습니다.

세 번째 접근에는 정확한 서술이 있습니다. **워크플로는 계획을 코드로 옮깁니다. 워크플로 스크립트가 루프와 분기와 중간 결과를 직접 쥐고 있으므로, Claude의 컨텍스트에는 최종 답만 남습니다**[^S5]. 그리고 **중간 결과는 Claude의 컨텍스트에 착지하는 대신 스크립트 변수 안에 머뭅니다**[^S5]. 모듈 27의 grep 출력은 JavaScript 배열 안에 머무르므로, 모듈 30의 평가는 자연히 그것을 볼 수 없습니다 — 모델이 무시하는 법을 배워서가 아니라, 애초에 볼 기회 자체가 없었기 때문입니다.

이 축의 양 끝에 대해, 공식 안내는 각각 적합성에 관한 한 문장씩을 줍니다. **복잡성이 정당화될 때, 워크플로는 잘 정의된 작업에 예측 가능성과 일관성을 제공하는 반면, 유연성과 모델 주도의 의사결정이 규모 있게 필요할 때는 에이전트가 더 나은 선택입니다**[^S1].

**한 가지 분명히 해 둘 것**: 이 코스는 이 축을 '결정성 스펙트럼'이라 부르고, 레슨 5의 조합 패턴을 '그래프', '노드', '엣지'라고 부릅니다. 이 이름표들은 **이 코스가 만든 엔지니어링 은유이며 1차 자료에는 결코 등장하지 않습니다** — 1차 자료가 주는 것은 두 끝점의 정의(미리 정의된 코드 경로 대 모델 주도의 결정)[^S1]와 '누가 계획을 쥐고 있는가'라는 틀[^S5]뿐입니다. 스펙트럼과 그래프라는 은유를 빌려 오는 이유는 실제로 존재하는 패턴들을 배열하기에 편해서입니다. 어느 공식 문서에서도 이 용어들을 읽을 수 없으니, 공식 개념인 것처럼 제시하지 마세요.

## 위로 올라가야 할 때

'하나의 루프로는 부족하다'는 직감처럼 들리지만, 말로 옮길 수 있는 트리거가 있습니다.

**트리거 1: 작업에 하나의 대화가 조율할 수 있는 것보다 많은 에이전트가 필요하거나, 오케스트레이션을 읽고 다시 돌릴 수 있는 스크립트로 성문화하고 싶을 때**[^S5]. 앞 절반은 능력의 문제이고 뒤 절반은 엔지니어링의 문제입니다 — 하나의 대화가 겨우 조율할 수 있다 해도 '내일 똑같이 다시 돌릴 수 있는가'는 그 자체로 이유가 됩니다.

**트리거 2: 작업이 하나의 에이전트가 컨텍스트에 담을 수 있는 것보다 클 때, 또는 같은 단계를 많은 항목에 걸쳐 돌려야 할 때**[^S5]. 이 두 문장을 합치면 첫머리의 40개 모듈 시나리오가 정확히 그려집니다. 두 번째 문장에 주목하세요. 항목 수 자체가 이유이며, 각 항목이 어려운지 아닌지는 상관없습니다.

**트리거 3: 곁가지 작업이 메인 대화를 범람시킬 때**. 서브에이전트 문서의 상황 서술은 이렇습니다. 곁가지 작업이 다시 참조하지 않을 검색 결과나 로그나 파일 내용으로 메인 대화를 범람시킬 상황이라면 서브에이전트를 파견하세요 — 그 일을 자기 컨텍스트 안에서 하고 요약만 돌려줍니다[^S4]. **탐색과 구현을 메인 대화 바깥에 둠으로써 컨텍스트를 보존하는 것**입니다[^S4]. 이 트리거가 처방하는 것이 '서브에이전트를 파견하라'라는 데 주목하세요 — 계획은 여전히 모델의 손에 머무르며, 그것과 '계획을 코드로 옮기기'는 서로 다른 두 가지입니다. 레벨 2 연습의 B열과 C열이 이 구분을 펼쳐 보일 것입니다.

형태를 갈아 끼운 뒤가 어떤 모습인지 감을 잡으려면 이 코드를 보세요:

```javascript
// 이 코스가 만든 예시일 뿐이며, 형태를 비교하기 위한 것입니다.
// 1차 자료에는 오케스트레이션 스크립트의 코드 예시가 없습니다.
const findings = [];
for (const mod of modules) {
  // 모듈마다 새 하네스 루프: 깨끗한 messages, 이 모듈의 작업만 담김
  const messages = [{ role: "user", content: assessPrompt(mod) }];
  findings.push(await runHarnessLoop(messages)); // 중간 결과는 이 배열에 착지
}
const report = await summarize(findings);        // 요약 단계만 모델로 돌아감
```

루프는 여전히 그 루프입니다 — `runHarnessLoop` 안에는 이 시리즈의 코스 7에서 쓴 그 `while`이 있습니다. 바뀐 것은 하나뿐입니다. **누가 40까지 세는가**. 전에는 모델이 셌습니다. 이제는 `for`가 셉니다.

```agentmentor-check
{
  "id": "orc-zh-01-bigger-window",
  "label": "윈도가 모자란 것인가, 형태가 틀린 것인가",
  "prompt": "40개 모듈 마이그레이션 평가가 무너졌습니다. 모듈 30 이후로 결론을 서로 오염시키고 점검 항목을 건너뛰기 시작합니다. 동료가 슬쩍 보더니 말합니다. '컨텍스트가 더 큰 모델로 갈아타면 돼. 아키텍처를 바꿀 필요는 없어.' 어느 판단이 더 잘 버틸까요?",
  "whyHere": "위로 올라갈 트리거를 방금 읽었습니다. 가장 흔한 오판은 오케스트레이션의 존재를 모르는 것이 아니라, 형태의 문제를 용량의 문제로 다루는 것입니다. 이 문제가 그 둘을 갈라냅니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "동료가 맞습니다. 붕괴의 직접 원인은 컨텍스트가 가득 찬 것이므로, 윈도를 두 배로 키우면 이 리포지토리는 통과합니다. 아키텍처를 건드릴 필요는 없습니다.",
      "correct": false,
      "feedback": "윈도가 크면 붕괴 지점이 뒤로 밀리는 것은 사실이고, 이 리포지토리는 정말 통과할지도 모릅니다. 하지만 '얼마면 충분한가'는 항목 수에 달려 있고, 그 항목 수는 작업이 주는 것이지 여러분이 고르는 것이 아닙니다. 앞선 29개 모듈이 남긴 읽기·쓰기 잔여물은 그 시점에는 모두 정당했지만, 그럼에도 뒤따르는 모듈의 주의 예산을 먹습니다(그 소모가 어떻게 일어나는지는 이 시리즈의 코스 8에서 다뤘습니다). 다음 리포지토리가 모듈 120개가 되면, 이 청구서를 다시 계산하게 됩니다."
    },
    {
      "id": "b",
      "text": "윈도를 바꾸는 것은 붕괴 지점을 미룰 뿐입니다. 근본 원인은 같은 단계가 많은 항목에 걸쳐 돌아가고 앞선 잔여물이 뒤의 주의를 먹는 것이므로, 계획을 코드로 옮겨야 합니다.",
      "correct": true,
      "feedback": "정답입니다. 이 작업은 두 트리거를 동시에 밟습니다. '하나의 에이전트가 컨텍스트에 담을 수 있는 것보다 크다'와 '같은 단계를 많은 항목에 걸쳐 돌려야 한다' — 둘 다 형태 층위의 신호입니다. 계획을 코드로 옮기고 나면 40개 모듈 각각이 깨끗한 하네스 루프를 돌고, 모듈 30의 평가 품질은 더 이상 앞선 29개의 잔여물에 좌우되지 않으며, 중간 결과는 스크립트 변수 안에 머뭅니다."
    },
    {
      "id": "c",
      "text": "단일 루프 방식이 한물갔다는 뜻입니다. 앞으로 어떤 작업이든 오케스트레이션으로 시작해야 합니다. 하나의 루프에서 출발하던 시절은 넘겨야 합니다.",
      "correct": false,
      "feedback": "너무 멀리 갔습니다. 공식 노선은 가장 단순한 해법을 먼저 찾고 필요할 때에만 복잡성을 더하라는 것이며, 이는 에이전트 시스템을 아예 만들지 않는 것을 뜻할 수도 있습니다. 많은 애플리케이션에서는 검색과 인컨텍스트 예시로 단일 LLM 호출을 최적화하는 것으로 대개 충분합니다. 그리고 필요한 단계 수를 예측할 수 없어 고정 경로로 하드코딩할 수 없는 열린 문제는 자율 루프에 그대로 두어야 합니다."
    }
  ]
}
```

## 그대로 머물러야 할 때

트리거 신호 뒤에는 그 반대편의 똑같이 긴 점검 목록이 있고, 이쪽이 건너뛰기 더 쉽습니다.

**가능한 한 가장 단순한 해법을 찾고, 필요할 때에만 복잡성을 늘리세요. 이는 에이전트 시스템을 아예 만들지 않는 것을 뜻할 수도 있습니다**[^S1]. **다만 많은 애플리케이션에서는 검색과 인컨텍스트 예시로 단일 LLM 호출을 최적화하는 것으로 대개 충분합니다**[^S1]. 함수 하나를 12군데에서 이름 바꾸는 그 일에는 하네스도, 오케스트레이션도 필요 없습니다 — 호출 한 번에 `grep` 한 번이면 끝납니다.

**오늘날 일부 도메인은 멀티 에이전트 시스템에 잘 맞지 않습니다**. 모든 에이전트가 같은 컨텍스트를 공유해야 하는 도메인, 또는 에이전트 사이의 의존이 많은 도메인입니다. 본문은 예를 하나 들어 놓았습니다 — **대부분의 코딩 작업은 조사보다 진짜로 병렬화 가능한 작업이 적고, LLM 에이전트는 다른 에이전트를 실시간으로 조율하고 위임하는 데 아직 능숙하지 않습니다**[^S2]. 강하게 결합된 주문 모듈을 리팩터링하고 있고 변경이 호출자 사슬을 따라 번져 나간다면, 이것을 서브에이전트 다섯으로 팬아웃하는 것은 더 느리고 더 지저분해지기만 합니다. 모두가 같은 것을 봐야 하는데, 먼저 움직인 쪽이 나머지 모두의 정보를 무효화해 버리기 때문입니다.

거꾸로, 잘 맞는 조건도 그만큼 담백하게 진술되어 있습니다. 그들은 멀티 에이전트 시스템이 **무거운 병렬화, 단일 컨텍스트 윈도를 넘어서는 정보, 수많은 복잡한 도구와의 접속을 수반하는** 작업에서 뛰어나며[^S2], **작업의 가치가 향상된 성능에 값을 치를 만큼 충분히 높은** 작업에서 그렇다는 것을 발견했습니다[^S2]. 이 세 조건에 많이 해당할수록 위로 올라갈 값어치가 커집니다.

**자율 루프에 그대로 두어야 하는** 작업 부류도 있습니다. 오케스트레이션으로 억지로 밀어 넣을 것이 아닙니다. **필요한 단계 수를 예측하기 어렵거나 불가능하고, 고정 경로를 하드코딩할 수 없는 열린 문제** — 이런 것들은 에이전트에게 줄 수 있고, 에이전트는 잠재적으로 여러 턴 동안 작동할 것이며, 여러분은 그 의사결정을 어느 정도 신뢰해야 합니다[^S1]. Anthropic 자신의 리서치 시스템이 이 유형입니다. **리서치 작업은 필요한 단계를 미리 예측하기가 매우 어려운 열린 문제를 수반합니다. 복잡한 주제를 탐색하는 고정 경로는 하드코딩할 수 없는데, 그 과정이 본질적으로 동적이고 경로 의존적이기 때문입니다**[^S2].

그러므로 '오케스트레이션으로 옮겨야 하는가'는 한 방향짜리 진행 바가 아닙니다. 40개 모듈 평가는 단계가 고정되어 있고 수만 많으므로 오케스트레이션 쪽으로 옮겨야 하고, '메시지 큐를 A에서 B로 바꿔야 하는가'는 글을 몇 편이나 읽어야 할지조차 모르므로 옮기지 말아야 합니다.

## 비용을 먼저 테이블에 올립니다

다섯 패턴을 배우기 전에 장부를 펴세요.

**에이전트 시스템은 더 나은 작업 성능을 위해 레이턴시와 비용을 맞바꾸는 일이 잦으며, 여러분은 이 맞바꿈이 언제 말이 되는지 따져 봐야 합니다**[^S1]. 여기 수사(修辭)는 한 단어도 없습니다. 이것은 **교환**을 서술하고 있습니다.

얼마나 비쌀까요? Anthropic은 자기 데이터에서 나온 관측 한 벌을 제공합니다. **에이전트는 전형적으로 채팅 상호작용보다 약 4배 많은 토큰을 쓰고, 멀티 에이전트 시스템은 채팅보다 약 15배 많은 토큰을 씁니다**[^S2]. 그래서 그들의 결론은 이렇습니다 — **경제적으로 성립하려면, 멀티 에이전트 시스템은 작업의 가치가 향상된 성능에 값을 치를 만큼 충분히 높은 작업을 필요로 합니다**[^S2].

이 숫자의 맥락에 주목하세요. 자기 데이터에서 나온 것이지 보편 벤치마크가 아니며, '모든 오케스트레이션 방식이 15배 더 든다'도 아닙니다. 다만 방향은 분명합니다. '에이전트를 더, 병렬을 더' 쪽으로 한 걸음 갈 때마다 청구서는 한 단 올라섭니다. 경계를 재빨리 그어 두면, 이 배수는 멀티 에이전트 시스템을 채팅에 견준 것이지 '계획을 코드로 옮기기' 자체의 가격표가 아닙니다 — 1차 자료는 오케스트레이션 스크립트의 독립적인 비용 수치를 준 적이 없습니다. 40개 모듈은 위로 올라갈 값어치가 있는데 12번의 이름 바꾸기는 그렇지 않은 이유도 이것입니다. 하나가 '복잡'하고 다른 하나가 '단순'해서가 아니라, 재작업 2주를 아껴 주는 마이그레이션 평가는 이 비용을 감당할 수 있기 때문입니다.

## 앞으로의 다섯 레슨

이 코스의 나머지는 가장 가벼운 형태에서 시작해 조합으로 쌓아 올립니다:

- **레슨 2: 체이닝과 라우팅**: 작업을 고정된 단계로 쪼개고 단계 사이마다 프로그램적 관문을 두기, 분류한 뒤 전문화된 프롬프트로 넘기기. '계획을 코드로'의 가장 가벼운 형태입니다.
- **레슨 3: 병렬화**: 섹셔닝(독립적인 하위 작업으로 쪼개 병렬로 실행)과 투표(같은 작업을 여러 번 돌려 다양한 출력 얻기), 그리고 결과를 코드로 집계하는 법.
- **레슨 4: 오케스트레이터-워커**: 중앙 LLM이 작업을 동적으로 분해하고, 워커 LLM에게 위임하고, 그 결과를 종합합니다. 핵심 차이는 하위 작업이 미리 정의되지 않는다는 것입니다.
- **레슨 5: 평가 루프, 그리고 패턴을 그래프로 조합하기**: 통과하거나 더 이상 진전이 없을 때까지 점검-수정-재점검하고, 그다음 앞선 패턴들을 이어 붙입니다. 그 레슨에서 다시 밝힙니다. '그래프'는 이 코스가 만든 시각화입니다.
- **레슨 6: 실습**: 이 시리즈의 코스 7에서 만든 그 단일 루프 하네스를 결정론적 오케스트레이션 스크립트로 끌어올립니다.

이 패턴 분류법은 2024년의 유물이 아닙니다. 현행 Claude 플랫폼의 멀티 에이전트 오케스트레이션 문서는 여전히 **병렬화(독립적인 하위 작업을 동시에 팬아웃하고, 코디네이터가 결과를 종합), 전문화(도메인에 집중한 시스템 프롬프트와 도구를 가진 에이전트로 라우팅), 에스컬레이션(복잡한 하위 작업의 일부에 대해 더 유능한 에이전트나 모델에 자문)**을 독립적으로 이름 지어 부릅니다[^S6]. 껍데기가 바뀌었을 뿐, 골격은 같습니다.

이 코스가 앞선 두 코스와 어떻게 갈라지는지에 대해 한 가지 더. 이 시리즈의 **코스 2**는 워크플로의 개념을 가르쳤습니다 — 단계, 상태, 분기, 도식 수준의 이해. 이 시리즈의 **코스 6**은 멀티 에이전트 협업의 분업과 소통을 가르쳤습니다 — 팬아웃, 위임 프롬프트가 자기완결적이어야 한다는 것, 프로듀서-리뷰어. 이 코스는 둘 중 어느 것도 다시 가르치지 않습니다. 다루는 것은 **제어 흐름 그 자체**입니다. 누가 세는가, 누가 분기하는가, 중간 결과는 어디로 가는가, 무너지면 무슨 일이 일어나는가. 용어를 재빨리 이어 주자면, 이 시리즈의 코스 6이 '프로듀서-리뷰어'라 부른 것을 1차 자료는 **이밸류에이터-옵티마이저**라 부르고[^S1], Claude Code의 워크플로 문서는 **독립적인 에이전트들이 서로의 발견을 적대적으로 리뷰하게 하는 것**이라 부릅니다[^S5].

## 비례성: 모든 것은 '측정 가능한 개선'을 통과해야 합니다

이 코스는 다섯 패턴과 여러 조합 방법을 가르칩니다. 그것들은 모두 하나의 자인(自認)을 공유하고, 원문은 모호하지 않습니다. **이 구성 요소들은 규범이 아닙니다. 개발자가 서로 다른 사용 사례에 맞게 다듬고 조합할 수 있는 흔한 패턴입니다. 성공의 열쇠는 다른 어떤 LLM 기능과도 마찬가지로 성능을 측정하고 구현을 반복하는 것입니다. 다시 말하지만, 복잡성을 더하는 것은 그것이 결과를 명백히 개선할 때에만 고려해야 합니다**[^S1].

'결과를 명백히 개선한다'에는 구체적인 뒷받침이 필요하고, 그것은 이 시리즈 코스 10의 평가와 코스 11의 관측 가능성에서 옵니다. 평가 세트가 없으면 '라우팅을 더해서 정말 나아졌는가'를 분명히 말할 수 없습니다. 그 규범적 텍스트의 결말은 이 순서를 따릅니다. **단순한 프롬프트에서 시작하고, 포괄적인 평가로 그것을 최적화하고, 더 단순한 해법이 부족할 때에만 여러 단계의 에이전트 시스템을 더하세요**[^S1].

그러니 패턴을 하나씩 배운 뒤에는 스스로 물어보세요. **이것을 더한 뒤에 결과가 좋아졌음을 보이는 숫자를 내놓을 수 있는가?** 못 내놓겠다면, 아직 더하지 마세요.

## 💻 연습

<!-- exercises -->

### 레벨 1: 다섯 작업을 세 등급에 넣기

코드는 필요 없습니다. 다음 5개 작업 각각이 어느 등급에 속하는지 판단하세요:

- **단일 루프로 충분**(혹은 호출 한 번으로도 충분, 복잡성을 더하지 말 것)
- **오케스트레이션으로 이동**(계획을 코드로 옮기기)
- **자율 루프에 그대로 유지**(단계 수를 모델이 정하게 두기)

첫째와 셋째 등급은 둘 다 하나의 루프로 돌아갑니다. 따로 나열한 것은 **오케스트레이션으로 옮기지 않는 이유가 다르기 때문**입니다. 하나는 일이 너무 단순해서 정당화되지 않고, 다른 하나는 일이 너무 열려 있어 분해되지 않습니다. 등급을 판단하는 것은 이유를 판단하는 것입니다. 판단마다 **이 레슨에 나온 구체적 조건**을 지목하는 한 문장의 근거가 필요합니다(같은 단계가 많은 항목에 걸쳐 돌아감 / 하나의 컨텍스트보다 큼 / 하나의 대화로 조율하기에 너무 많음 / 단계 수를 예측할 수 없어 고정 경로로 하드코딩할 수 없음 / 단순한 작업에는 복잡성이 필요 없음 / 공유 컨텍스트와 많은 의존을 요구하는 도메인은 멀티 에이전트에 잘 맞지 않음). '작업이 크다'거나 '작업이 복잡하다'로만 말하지 마세요.

1. 리포지토리의 모듈 40개에 대해 모듈마다 마이그레이션 평가서를 하나씩 쓰기. 각각에 위험 지점, 범위 추산, 의존성 매니페스트 포함.
2. `getUserProfile`을 `fetchUserProfile`로 이름 바꾸기, 리포지토리 전체에서 호출 지점 12군데.
3. '메시지 큐를 A에서 B로 바꿔야 하는가'를 조사하기. 정보 출처 목록은 미리 정해져 있지 않고, 읽어 가면서 정함.
4. 밀린 고객 지원 티켓 200건 각각에 범주 라벨 달기(범주는 총 8개), 라벨링 규칙은 문서 하나에 적혀 있음.
5. 강하게 결합된 주문 모듈 리팩터링: 클래스 3개 분리, 인터페이스 2개 이동, 변경이 호출자 사슬을 따라 번짐.

<!-- rubric -->

- 다섯 작업 각각에 등급 판단이 있고 등급이 맞다: 작업 1과 4는 '오케스트레이션으로 이동', 작업 2는 '단일 루프로 충분', 작업 3과 5는 '자율 루프에 그대로 유지'
- 각 근거가 이 레슨의 구체적 조건을 지목한다. 막연한 '작업이 크다'나 '작업이 복잡하다'가 아니다. 작업 4의 근거는 항목 수에 착지해야 하고, 개별 항목이 실제로는 단순하다는 점을 짚어야 한다
- 작업 5는 '멀티 에이전트로 팬아웃하지 말 것'을 추가로 짚고, 그 근거가 '공유 컨텍스트와 에이전트 간 의존이 많은 도메인은 잘 맞지 않음'과 '대부분의 코딩 작업은 조사보다 진짜로 병렬화 가능한 작업이 적음'에 착지한다

<!-- answer -->

**1. 40개 모듈 마이그레이션 평가 — 오케스트레이션으로 이동.**
두 트리거를 동시에 밟습니다. 하나의 에이전트가 컨텍스트에 담을 수 있는 것보다 크고, 같은 단계를 많은 항목에 걸쳐 돌려야 합니다[^S5]. 모듈마다의 평가 단계는 고정되어 있고(코드 읽기, 호출 지점 찾기, 범위 추산, 보고서 쓰기) 항목만 달라집니다. 계획을 코드로 옮기면 `for`가 40까지 세고, 모듈마다 깨끗한 하네스 루프를 열며, 중간 결과는 스크립트 변수 안에 머뭅니다[^S5].

**2. 함수 이름 12번 바꾸기 — 단일 루프로 충분, 아니 호출 한 번에 `grep` 한 번이면 됩니다.**
이것은 역방향 조건을 시험합니다. 가능한 한 가장 단순한 해법을 찾고 필요할 때에만 복잡성을 더하라, 이는 에이전트 시스템을 아예 만들지 않는 것을 뜻할 수도 있습니다[^S1]. 단계가 완전히 정해져 있고, 항목은 12개뿐이며, 각 변경이 기계적이고 서로 독립적입니다. 오케스트레이션으로 감싸면 레이턴시와 비용을 치르고 돌려받는 것은 0입니다[^S1].

**3. 메시지 큐 선정 조사 — 자율 루프에 그대로 유지.**
열린 문제이고, 필요한 단계 수를 예측하기 어렵고, 고정 경로를 하드코딩할 수 없습니다 — 이것이 정확히 에이전트가 맞는 자리입니다[^S1]. 조사류 작업은 명시적으로 이름 지어졌습니다. 그 과정은 본질적으로 동적이고 경로 의존적이며, 어떤 글을 읽을지 미리 알 수 없습니다[^S2]. 고정 단계 오케스트레이션으로 억지로 밀어 넣으면 처음에 짐작한 그 하나의 경로에 못 박아 둘 뿐입니다.

**4. 티켓 200건 라벨링 — 오케스트레이션으로 이동.**
'같은 단계를 많은 항목에 걸쳐 돌려야 한다'를 밟습니다[^S5]. 작업 1과의 차이에 주목하세요. 개별 항목은 하나하나가 **극히 단순합니다** — 규칙이 문서에 적혀 있고, LLM 호출 한 번에 예시 몇 개면 티켓 하나를 판단할 수 있습니다. 이것이 바로 '검색과 예시로 단일 호출을 최적화하는 것으로 대개 충분하다'의 전형적인 경우입니다[^S1]. 코드로 옮겨야 하는 것은 '티켓 하나를 어떻게 판단하는가'가 아니라 '200까지 어떻게 세는가'입니다. 스크립트가 세게 하면 티켓 150번과 티켓 1번이 똑같이 깨끗한 시작 컨텍스트를 얻습니다.

**5. 강하게 결합된 주문 모듈 리팩터링 — 자율 루프에 그대로 유지, 그리고 명시적으로 멀티 에이전트로 팬아웃하지 않기.**
근거가 두 층입니다. 첫째 층, 단계 수를 예측할 수 없음. 첫 클래스를 분리하고 나서야 두 번째 인터페이스를 어떻게 옮길지 알 수 있으므로, 고정 경로를 하드코딩할 수 없습니다[^S1]. 둘째 층이 핵심입니다 — 이 도메인은 오늘날 멀티 에이전트에 잘 맞지 않습니다. 모든 에이전트가 같은 컨텍스트를 공유해야 하고, 상호 의존이 많으며, 본문은 대부분의 코딩 작업이 조사보다 진짜로 병렬화 가능한 작업이 적고 LLM 에이전트가 실시간 조율과 위임에 아직 능숙하지 않다고 곧바로 지목합니다[^S2]. 해야 할 일은 하나의 루프가 작업을 완주하게 하고, 필요할 때 핵심 지점에 결정론적 점검을 더하는 것이지, 에이전트 수를 늘리는 것이 아닙니다.

<!-- hint -->

판단으로 달려가기 전에, 각 작업의 **단계**와 **항목**을 먼저 분리하세요. 이 작업의 단계는 고정되어 있습니까, 아니면 가면서 정해집니까? 반복해야 할 항목은 몇 개입니까? '고정된 단계 + 많은 항목'과 '고정되지 않은 단계'는 완전히 다른 등급으로 여러분을 데려갈 것입니다.

<!-- hint -->

작업 5는 **두 근거를 모두 써야 하는** 유일한 항목입니다. 서로 다른 두 개의 역방향 조건을 동시에 밟기 때문입니다. 하나만 쓰면 이 레슨의 점검 목록에서 가장 밟기 쉬운 역방향 조건을 놓치게 됩니다.

### 레벨 2: 같은 작업, '누가 계획을 쥐고 있는가'의 세 버전

코드는 필요 없습니다. 여전히 그 40개 모듈 마이그레이션 평가이고, 세 가지 접근입니다:

- **A: 하나의 큰 루프** — 40개 모듈 목록을 하나의 하네스 루프에 한꺼번에 건네고, 스스로 진행하게 둡니다.
- **B: 모델이 오케스트레이터가 되어 서브에이전트를 하나씩 파견** — 메인 대화에서 모델이 매 턴 어느 모듈에 어느 서브에이전트를 파견할지 정하고, 서브에이전트는 결과를 메인 대화로 돌려줍니다.
- **C: 계획을 스크립트에 씀** — 스크립트가 그 40개 모듈을 `for`로 돌고, 모듈마다 깨끗한 하네스 루프를 시작합니다.

이 표를 완성하세요(물음표가 여러분이 채울 자리입니다). 셋째 행이 핵심입니다. 모듈 25에서 프로세스가 죽거나 세션이 끊겼을 때, 각 접근은 무엇을 잃습니까?

| | A: 하나의 큰 루프 | B: 모델이 오케스트레이터 | C: 계획을 스크립트에 씀 |
| --- | --- | --- | --- |
| 누가 계획을 쥐는가 | ? | ? | ? |
| 중간 결과가 착지하는 곳 | ? | ? | ? |
| 모듈 25에서 무너지면 무엇을 잃는가 | ? | ? | ? |

<!-- rubric -->

- '누가 계획을 쥐는가' 세 칸이 모두 맞다: A와 B는 둘 다 모델이 쥐고 있고, 둘의 차이가 분명하다(A는 하나의 대화 이력에 숨은 암묵적인 것, B는 누구를 파견할지 모델이 턴마다 명시적으로 정하는 것). C는 스크립트가 루프와 분기와 중간 결과를 쥐고 있다
- '중간 결과가 착지하는 곳'이 분명히 구분된다: A와 B는 결국 컨텍스트 윈도에 착지하고(B는 한 겹 더 있다 — 서브에이전트 결과가 메인 대화로 돌아오며, 여러 서브에이전트가 각자 상세한 결과를 돌려주면 상당한 컨텍스트를 소모한다), C는 스크립트 변수 안에 머물러 모델의 컨텍스트에는 최종 답만 남는다
- '무엇을 잃는가'가 C의 회복 가능성을 **점진적 영속화를 조건으로** 답하고(대응: 런타임이 실행이 진행되는 대로 각 에이전트의 결과를 추적해 같은 세션 안에서 재개 가능하게 만든다), A와 B는 진행 전체를 잃고 C는 영속화 이후에는 주로 현재 항목 하나만 잃는다는 것을 짚는다

<!-- answer -->

| | A: 하나의 큰 루프 | B: 모델이 오케스트레이터 | C: 계획을 스크립트에 씀 |
| --- | --- | --- | --- |
| 누가 계획을 쥐는가 | 모델, 그리고 **암묵적**. 계획은 한 번도 밖으로 쓰인 적이 없고 대화 이력 속에 숨어 있어, 의도한 경로를 짐작하려면 로그를 파헤쳐야 합니다 — 이것이 정확히 'LLM이 자기 프로세스를 동적으로 방향 지으며 작업을 어떻게 완수할지에 대한 통제를 쥐고 있다'의 정의입니다[^S1] | 모델, 조금 더 **명시적**. 매 턴 '다음에는 주문 모듈을 볼 사람을 파견하겠다'고 말하지만, 여전히 턴마다 정하고 있습니다[^S5] | **스크립트**. 스크립트 자신이 루프와 분기와 중간 결과를 쥐고 있으므로, Claude의 컨텍스트에는 최종 답만 남습니다[^S5] |
| 중간 결과가 착지하는 곳 | 전부 같은 `messages` 배열 안: 40개 모듈의 grep 출력, 읽어 들인 파일들, 잘못 들어갔다 되돌아 나온 흔적이 하나의 타임라인을 나눠 씁니다 | 메인 대화로 돌아옵니다. 서브에이전트가 완료하면 그 결과가 메인 대화로 돌아오고, 각자 상세한 결과를 돌려주는 서브에이전트를 여럿 돌리면 상당한 컨텍스트를 소모할 수 있습니다[^S4] | 스크립트 변수 안에 머물며 모델의 컨텍스트에 착지하지 않습니다[^S5] |
| 모듈 25에서 무너지면 무엇을 잃는가 | 거의 전부. 진행 상황이 곧 그 대화 이력 자체이고, 세션이 사라지면 진행도 사라집니다. 앞선 24개가 디스크에 떨어졌더라도 '모듈 25부터 이어서'라는 깨끗한 진입점이 없습니다 | 메인 대화에 있던 오케스트레이션 상태 전체를 똑같이 잃습니다. 게다가 더 나쁘게, 재시작 후 모델이 계획을 처음부터 다시 생각해야 합니다 — 이전 계획은 어디에도 쓰인 적이 없습니다 | **각 결과를 가면서 영속화했다는 것을 조건으로**(스크립트 변수를 파일로 쓰는 것은 코드 한 줄입니다) 모듈 25 자체만 잃고, 모듈 26부터 이어 갈 진입점은 이미 마련되어 있습니다. 메모리에만 갖고 있었다면 프로세스가 죽는 순간 A만큼 잃습니다. 대응: 워크플로 런타임은 실행이 진행되는 대로 각 에이전트의 결과를 추적하며, 그것이 **같은 세션 안에서** 실행을 재개 가능하게 만드는 것입니다[^S5] — 프로세스를 넘어선 복구는 여전히 여러분이 직접 영속화해야 합니다 |

**추론 과정:**

세 행은 사실 같은 것의 세 단면입니다. **누가 계획을 쥐든, 중간 결과는 그 쥔 자의 그릇에 착지하고, 프로세스가 무너지면 그 그릇에 든 것을 잃습니다.**

A와 B는 둘 다 계획이 모델의 손에 있으므로 그릇은 컨텍스트 윈도이고, 컨텍스트 윈도는 세션 단위의 것이라 세션이 끝나면 0으로 돌아갑니다. 그래서 A와 B의 '진행'과 '컨텍스트'는 같은 것이고, 하나를 잃으면 다른 하나도 잃습니다. B가 A보다 강해 보이는 이유는 모듈마다의 평가가 서브에이전트 자신의 윈도 안에서 깨끗하게 끝나기 때문입니다. 하지만 요약은 여전히 메인 대화에 하나씩 쌓이고, 25번째 요약에 이르면 메인 대화도 똑같이 짓눌립니다. A의 문제를 미뤘을 뿐, 그릇을 갈아 끼우지는 못했습니다.

C는 그릇을 갈아 끼웠습니다. 중간 결과는 스크립트 변수 안에 있고, 스크립트 변수는 한 줄이면 파일이나 데이터베이스에 쓸 수 있습니다. 이것이 **점진적 추적이 회복 가능성을 가져온다**는 발상입니다. 런타임 버전은 실행이 진행되는 대로 각 에이전트의 결과를 추적해 같은 세션 안에서 재개 가능하게 하는 것이고[^S5], 스크립트 버전은 여러분이 각 결과를 직접 파일로 쓰는 것입니다. 이것은 또한 이 시리즈 코스 9의 '긴 작업이 중단을 견디게 만들기'라는 사고가 오케스트레이션 층에 착지한 자리이기도 합니다 — 진행 상황은 **모델의 컨텍스트보다 오래 사는** 곳에 있어야 합니다. 덤으로 얻는 이득도 있습니다. C에서는 모듈 25와 모듈 1이 정확히 같은 시작 컨텍스트를 얻으므로, 모듈 25의 재실행은 진짜 재시도입니다. '앞선 24개로 더러워진 현장에서 한 번 더 해 보기'가 아닙니다.

<!-- hint -->

가운데 행부터 채우세요. 각 접근의 중간 결과가 **물리적으로 어느 그릇 안에 존재하는지**를 분명히 생각하면(`messages` 배열? 메인 대화의 컨텍스트 윈도? JavaScript 변수?), 셋째 행의 답은 저절로 떠오릅니다. 무너졌을 때 잃는 것이 곧 그 그릇에 든 것입니다.

<!-- hint -->

B는 'C와 비슷하다'고 판단하기 쉽습니다. 모듈마다의 지저분한 작업을 서브에이전트의 독립된 컨텍스트 안에 격리하는 것도 사실이기 때문입니다. 생각해 보세요. 서브에이전트가 끝난 뒤 그 결과는 **어디로** 갑니까? 모듈 25쯤이면 메인 대화에 요약이 몇 개나 앉아 있습니까?

<!-- /exercises -->

## 정리

- 이 시리즈의 코스 7에서 쓴 그 `while`이 에이전트다. 에이전트는 정교한 작업을 다룰 수 있지만 그 구현은 대개 단순하고, 전형적으로는 환경 피드백에 기반해 루프 안에서 도구를 쓰는 LLM일 뿐이다[^S1]
- 이 변형들을 통틀어 에이전트 시스템이라 부르며, 그 안에서 아키텍처적 구분이 하나 그어진다. 워크플로는 LLM과 도구가 미리 정의된 코드 경로를 통해 오케스트레이션되는 시스템이고, 에이전트는 LLM이 자기 프로세스와 도구 사용을 동적으로 방향 지으며 작업을 어떻게 완수할지에 대한 통제를 쥐고 있는 시스템이다[^S1]
- 둘을 가르는 축은 '누가 계획을 쥐고 있는가'다. 워크플로는 계획을 코드로 옮기고, 스크립트 자신이 루프와 분기와 중간 결과를 쥐고 있으므로 Claude의 컨텍스트에는 최종 답만 남는다[^S5]. 이 코스는 이 축을 '결정성 스펙트럼'이라 부르고 레슨 5의 조합 시각화를 '그래프'라 부르는데, 두 용어 모두 이 코스의 은유이며 1차 자료에는 나오지 않는다
- 위로 올라갈 트리거: 작업에 하나의 대화가 조율할 수 있는 것보다 많은 에이전트가 필요하거나 오케스트레이션을 읽고 다시 돌릴 수 있는 스크립트로 성문화하고 싶을 때[^S5], 작업이 하나의 에이전트가 컨텍스트에 담을 수 있는 것보다 클 때 또는 같은 단계를 많은 항목에 걸쳐 돌려야 할 때[^S5], 곁가지 작업이 다시 참조하지 않을 내용으로 메인 대화를 범람시킬 때[^S4]
- 그대로 머물 조건: 가능한 한 가장 단순한 해법을 찾고 필요할 때에만 복잡성을 더하며, 이는 에이전트 시스템을 아예 만들지 않는 것을 뜻할 수도 있다[^S1]. 많은 애플리케이션에서는 검색과 인컨텍스트 예시로 단일 LLM 호출을 최적화하는 것으로 대개 충분하다[^S1]. 공유 컨텍스트를 요구하거나 에이전트 간 의존이 많은 도메인은 오늘날 멀티 에이전트에 잘 맞지 않으며, 대부분의 코딩 작업은 조사보다 진짜로 병렬화 가능한 작업이 적다[^S2]
- 자율 루프에 그대로 둘 것: 열린 문제, 단계 수를 예측하기 어려움, 고정 경로를 하드코딩할 수 없음[^S1] — 조사류 작업이 전형적이고, 그 과정은 본질적으로 동적이고 경로 의존적이다[^S2]
- 청구서를 먼저 받아 볼 것: 에이전트 시스템은 더 나은 작업 성능을 위해 레이턴시와 비용을 맞바꾸는 일이 잦다[^S1]. 그들 자신의 데이터에서 에이전트는 채팅보다 약 4배, 멀티 에이전트는 약 15배의 토큰을 쓰며, 경제적으로 성립하려면 작업의 가치가 그 향상을 감당해야 한다[^S2]
- 이 코스가 가르치는 모든 것은 같은 관문을 통과해야 한다. 복잡성은 결과를 명백히 개선할 때에만 더한다[^S1]. 단순한 프롬프트에서 시작하고, 포괄적인 평가로 최적화하고, 더 단순한 해법이 부족할 때에만 여러 단계의 에이전트 시스템을 더한다[^S1]

[>> 레슨 2: 이어 붙이고, 갈라 보내기: 체이닝과 라우팅](./02-chaining-and-routing.md)
