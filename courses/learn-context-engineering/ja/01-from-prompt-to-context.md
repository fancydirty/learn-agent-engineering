# レッスン1: プロンプトエンジニアリングからコンテキストエンジニアリングへ

> 学習目標:
> - プロンプトエンジニアリングとコンテキストエンジニアリングの定義を言い直し、両者が「置き換え」ではなく「進化」である理由を述べる
> - 「アテンションバジェット」と「コンテキストロット」という 2 つの考え方を使って、「ウィンドウは十分大きいのだから全部詰め込めばいい」の何が間違っているかを同僚に説明する
> - 本シリーズの「Agent Harness Fundamentals: Loops and Control」コースで組んだハーネスのループの中で、コンテキストが増える一方で減らない箇所を指摘する
>
> 前提: 本シリーズの「Agent Harness Fundamentals: Loops and Control」コースを終え、stop_reason 駆動のループが手元で動いていること | 次: [レッスン2 >>](./02-anatomy-of-context.md)

## よくある光景: ループが 1 周するたびにウィンドウは重くなる

本シリーズの「Agent Harness Fundamentals: Loops and Control」コースでは、次のようなループを手で書きました（簡略版。最大ターン数や予算などの 4 つの制御バルブはいったん省いてあります）:

```javascript
let response = await client.messages.create({
  model: MODEL, max_tokens: 1024, tools, messages, // messages 配列に注目
});

while (response.stop_reason === "tool_use") {
  messages.push({ role: "assistant", content: response.content });

  const toolResults = await runToolUses(response.content, toolImpls);
  messages.push({ role: "user", content: toolResults }); // ツール結果も入ってくる

  response = await client.messages.create({
    model: MODEL, max_tokens: 1024, tools, messages,
  });
}
```

あのときの関心はすべて制御フローに向いていました。`stop_reason` をどう読むか、制御バルブをどう取り付けるか。ここで角度を変えて `messages` 配列をじっと見てください——この配列は `push` されるだけで、一度も切り詰められません。1 ターンごとに、少なくとも 2 つのものが入ってきます。そのターンのモデルの返信（tool_use ブロックを含む）と、ツールが返した結果です。かさばるのはたいてい後者です。1 回の `list_files` が数百件のファイル名を返してくる場面や、ログ検索が数十 KB の生テキストを返してくる場面を思い浮かべてください。それらはその時点から永久にウィンドウに居座り、推論のラウンドごとに最初から読み直されます。

これは実装の手落ちではなく、エージェントというものの性質です。"An agent running in a loop generates more and more data that could be relevant for the next turn of inference"[^S1]（ループの中で動くエージェントは、次の推論ターンに関係しうるデータをどんどん生成していく）。しかもエージェントは自律性を握っているため、非常に多くのターンを走り続ける可能性があります[^S2]。本シリーズの「プロンプトエンジニアリング基礎: 効果的な指示の書き方」コースでは、1 つの指示を明確に書く方法を学びました——ですが、どれほどうまく書かれた指示でも、それはウィンドウの中の小さな 1 ブロックにすぎません。40 ターン目のエージェントの振る舞いを実際に決めるのは、その瞬間にウィンドウ全体に何が座っているか、です。

## 2 つの定義: 「良い一文を書く」から「ウィンドウ全体を管理する」へ

いまの観察を形式化すると、2 つの定義が得られます。

**プロンプトエンジニアリング**: "Prompt engineering refers to methods for writing and organizing LLM instructions for optimal outcomes"[^S1]（最適な結果を得るために LLM への指示を書き、構成する手法）。答える問いは「この指示を、最も効果が出るようにどう書き、どう並べるか？」です。

**コンテキストエンジニアリング**: "the set of strategies for curating and maintaining the optimal set of tokens (information) during LLM inference"[^S1]（LLM の推論中に、最適なトークン（情報）の集合を選び、維持するための戦略の集合）。答える問いは「今回の推論では、どのトークンがウィンドウにあるべきで、どれがあるべきでないか？」です。

視点が一段上がっていることに注目してください。前者は一度きりの執筆の問題で、書き上げればそれで固定されます。後者はループが 1 周するたびに答え直さなければならないトレードオフです。Anthropic はコンテキストエンジニアリングを、プロンプトエンジニアリングの自然な発展として明示的に位置づけています——"we view context engineering as the natural progression of prompt engineering"[^S1]——ですから、本シリーズの「プロンプトエンジニアリング基礎: 効果的な指示の書き方」コースで積み上げたスキルは、まったく無駄になりません。より大きな問題の部分集合になるのです。システムプロンプトは依然としてうまく書かれなければなりませんが、それはいまや管理すべき多くのコンテキスト要素の 1 つにすぎません。

コミュニティにはもっと過激な見方もあります。2026 年のあるエージェントエンジニアリングのロードマップは "Prompt engineering is dead as a standalone skill in 2026."[^S5] と断言しています。ただしこれはそのロードマップの主観的な断定であり、本レッスンはコンセンサスとしては扱いません——Anthropic の言い回しははるかに慎重で、置き換えではなく進化です[^S1]。とはいえ、同じロードマップがコンテキストエンジニアリングに与えた一行の定義は覚えておく価値があります: "deciding what tokens are in front of the model at every step of the loop"[^S5]（ループのどのステップでも、どのトークンをモデルの目の前に置くかを決めること）。そして同じロードマップは、ハーネスがどれだけ効くのかも言い当てています: "Same model, different harness, completely different result."[^S5] ——本シリーズの「Agent Harness Fundamentals: Loops and Control」コースの実験を通じて、すでに肌感覚として持っているはずのことです。

## アテンションバジェット: 新しいトークンはすべてツケになる

なぜこれが管理しなければならない対象なのでしょうか。ウィンドウが大きければ、あとは好きに使えるだけではないのでしょうか。ここで 1 つ目の物理的な事実が出てきます。

"LLMs have an "attention budget" that they draw on when parsing large volumes of context"[^S1]（LLM は大量のコンテキストを解析するとき、「アテンションバジェット」から引き出して使う）。厄介なのは、この予算が有限だということです: "Every new token introduced depletes this budget by some amount"[^S1]（新しく入ってくるトークンはすべて、この予算をいくらか消費する）。

たとえるなら、ウィンドウの容量は倉庫の床面積で、アテンションバジェットは荷物を探しに送り込む人員です。倉庫を 10 倍に広げても、人員はそれに合わせて増えません——棚が埋まるほど、本当に欲しい 1 点を掘り出すのは難しくなります。「念のため」の資料をもう 1 部ウィンドウに滑り込ませる行為は、無料のバックアップではありません。それを読んで関係ないと判断するコストとして、予算から実際に差し引かれます。

この見方に切り替えると、考え直す価値のある習慣がいくつも出てきます。「ウィンドウに収まるのだから、API ドキュメントを全文貼っておこう」——収まるかどうかは倉庫の話、うまく読めるかどうかは予算の話で、その 2 つは同じではありません。

## コンテキストロット: 崖ではなく、なだらかな坂

予算が絶え間なく削られ続けることのマクロな帰結には、鮮やかな名前がついています。**コンテキストロット（context rot）**——"as the number of tokens in the context window increases, the model's ability to accurately recall information from that context decreases"[^S1]（コンテキストウィンドウのトークン数が増えるほど、そのコンテキストから情報を正確に想起する能力は下がる）。

取り違えやすい点が 2 つあるので、ここで釘を刺しておきます。

**1 つ目、これは急落ではなく漸進的です。** この劣化は性能の傾斜として現れるのであって——"These factors create a performance gradient rather than a hard cliff"[^S1]——あるトークン数を超えた途端に動かなくなる硬い崖ではありません。ですからエラーは一切出ません。エージェントがじわじわ鈍くなるだけです。日常的な言い方をすると（これはエンジニアリングの経験上よく見る現れ方であって、出典に列挙されているわけではありません）、先に確認したはずの規約が忘れられ始め、すでに読んだファイルをまた読み、すでに直したバグが元に戻ります。アラームの鳴らない劣化は、エラーよりも追跡が困難です。

**2 つ目、これは特定のモデルの癖ではなく一般則です。** 他よりなだらかに劣化するモデルはありますが、"some models exhibit more gentle degradation than others, this characteristic emerges across all models"[^S1]（他より穏やかに劣化するモデルもあるが、この性質はすべてのモデルに現れる）。より強いモデルに乗り換えれば問題を先送りできますが、消し去ることはできません。

この 2 つを合わせると、本レッスン最初の礎石が得られます: "context, therefore, must be treated as a finite resource with diminishing marginal returns"[^S1]（したがってコンテキストは、限界収穫が逓減する有限の資源として扱わなければならない）。ウィンドウに押し込む 1000 番目のトークンと 10 万番目のトークンは、占める場所は同じでも、上乗せする価値はまるで違います。「多いほど安全」という直感は方向がちょうど逆で、余分に詰め込んだ「保険」の分だけ、本当に大事な情報へのモデルの注意が薄まります。

```agentmentor-check
{
  "id": "ctx-zh-01-finite-attention",
  "label": "アテンションバジェットで「安全のために全部詰め込む」という直感を検証する",
  "prompt": "毎ターン何をウィンドウに入れるか吟味しているあなたを見て、同僚がこう言いました: 「モデルのウィンドウは 200K トークンもある。プロジェクトのドキュメントも、会話履歴の全文も、ツールの説明も全部詰め込めばいい。情報が全部そろっている方が、抜けがあるより安全だ」。正しい判断はどれでしょうか？",
  "whyHere": "直前のセクションでアテンションバジェットとコンテキストロットを扱ったので、ここでは「多いほど安全」という直感を本当に手放せたかを確認します。これは本レッスンが壊すべき中心的な誤解であり、続く 5 つのレッスンはすべてこの土台の上に積み上がります。",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "同僚が正しいです。ウィンドウが埋まりきらない限り、余分な情報はただ置かれているだけで、何のコストもかかりません。",
      "correct": false,
      "feedback": "正しくありません。「埋まっていなければコストゼロ」は誤解です。大量のコンテキストを解析すること自体がアテンションバジェットを消費し、新しく入るトークンはすべて予算をいくらか削り、無関係な内容は重要情報の想起を薄めます——これらはいずれも、ウィンドウが容量いっぱいかどうかとは無関係に起こります。"
    },
    {
      "id": "b",
      "text": "新しいトークンはすべて有限のアテンションバジェットを消費し、トークンが多いほど正確な想起は悪化します。コンテキストは限界収穫が逓減する有限の資源であり、「収まる」ことは「安全」を意味しません。",
      "correct": true,
      "feedback": "正しい。ウィンドウの容量とアテンションバジェットは別ものです。前者はどれだけ入るかを決め、後者はどれだけうまく使えるかを決めます。劣化は崖ではなく坂ですが、その傾向はすべてのモデルに現れるため、トレードオフは選択肢ではなく必須です。"
    },
    {
      "id": "c",
      "text": "問題はコストだけです。トークンが増えれば料金は上がりますが、支払える限り、ウィンドウを埋めても回答品質には影響しません。",
      "correct": false,
      "feedback": "正しくありません。コストは確かに上がりますが、それは根本的な問題ではありません。トークンが増えるほど、コンテキストから情報を正確に想起するモデルの能力は落ちます——これは課金とは無関係な性能上の法則であり、薄まった注意はいくら払っても買い戻せません。"
    }
  ]
}
```

## エージェントに戻る: これが飾りではなく土台である理由

単発の質疑応答では、コンテキストロットに気づきさえしないかもしれません。ウィンドウは一度使われて捨てられ、トークン数もたいてい危険域には届きません。エージェントはこの問題を「たまに遭遇するもの」から「ターンごとに悪化していくもの」へ変えます。ループの中のデータは増える一方で減らず[^S1]、エージェントは自律的に多くのターンを走り続けうるからです[^S2]。冒頭のコードを見返してください——`push` されるばかりで何も返さないあの `messages` 配列こそが、この過程を具体化したものです。

現場のエンジニアリング経験も完全に一致します。Claude Code の公式ドキュメントは "Claude's context window fills up fast, and performance degrades as it fills."[^S4]（Claude のコンテキストウィンドウはすぐ埋まり、埋まるにつれて性能は劣化する）と述べ、コンテキストウィンドウを "the most important resource to manage."[^S4]（管理すべき最も重要な資源）と呼んでいます。同じドキュメントには、書き留めておく価値のある観察もあります: "A clean session with a better prompt almost always outperforms a long session with accumulated corrections."[^S4] ——「長く話した」ことは「うまく話した」ことを意味しません。積み上がった訂正も、逸れていった脱線も、すべてウィンドウに座ったまま次のラウンドの推論に参加し続けます。

そこでコンテキストエンジニアリングの正確な位置づけはこうなります。これはチューニング段階で足す仕上げの磨きではなく、エージェントの信頼性の土台です。本シリーズの「Agent Harness Fundamentals: Loops and Control」コースの 4 つの制御バルブが管理するのは「ループを暴走させない」こと。このコースが取り付けるのはもう一組の機構、「ウィンドウを腐らせない」ことです。両方そろって初めて、あなたのハーネスは長時間のタスクを任せられるものになります。

## このコースのロードマップ

長い時間軸のタスクに対して、Anthropic は 3 種類の技術を挙げています——"compaction, structured note-taking, and multi-agent architectures"（コンパクション、構造化されたノート取り、マルチエージェントアーキテクチャ）。狙いは、エージェントが "maintain coherence, context, and goal-directed behavior over sequences of actions"[^S1]（一連の行動を通じて一貫性、コンテキスト、目標志向の振る舞いを保つ）ことです。このコースはその道筋をたどります:

- **レッスン2** ではウィンドウを解剖します。システムプロンプト、ツール定義、例——それぞれどれだけの場所を取り、どう書けば無駄がないのか。
- **レッスン3** ではジャストインタイムの取得を扱います。材料を前もって全部詰め込むのではなく、軽量な識別子を渡して、必要になったらエージェント自身に調べに行かせます。
- **レッスン4** ではコンパクションとノートを扱います。ウィンドウが上限に近づいたときにどう要約して再起動するか、そして重要な情報をウィンドウの外にどう記録するか。
- **レッスン5** ではサブエージェントとコンテキスト分離を扱います。散らかる探索作業はきれいなウィンドウを持つサブエージェントに送り、蒸留された結論だけを引き取ります。
- **レッスン6** では自分のハーネスに戻り、これらの機構を 1 つずつ取り付けます。

最後に、程度についての注意を 1 つ。ここに挙げた機構はどれも複雑さを増やします。そして Anthropic のエンジニアリングガイダンスはこう言います: "you should consider adding complexity only when it demonstrably improves outcomes."[^S2]（複雑さを加えるのは、それが結果を明確に改善すると示せるときだけにすべきである）。ですからこの先のどのレッスンも、「どうやるか」の前に「いつやる価値があるか」を説明します——すべてのエージェントにサブエージェントが要るわけではありませんし、すべてのタスクがコンパクションに値するわけでもありません。

<!-- exercises -->
## 💻 演習

### レベル1: 6 つの実践を 2 つの引き出しに仕分ける

以下に 6 つの実践が挙げてあります。主にプロンプトエンジニアリング（「指示そのものをどう書き、どう構成するか」に焦点）に属するのはどれで、主にコンテキストエンジニアリング（「推論のラウンドごとにウィンドウにどのトークンを維持するか」に焦点）に属するのはどれでしょうか。それぞれを分類し、1 文ずつ根拠を書いてください。

1. システムプロンプトを「あなたはアシスタントです」から、責務と境界の具体的な記述に書き換える
2. ハーネスの各ターンで、3 ターン前のツール結果を 1 行の要約に置き換える
3. タスク指示に「入力はこう、出力はこう」という例を追加する
4. 500 ページの製品マニュアルに対して、モデルには目次とファイルパスだけを渡し、必要になったら調べに行かせる
5. 指示の中の「簡潔に」を「3 文以内で返答する」に変える
6. セッションが 40 ターン目に達したら、履歴全体を要約に圧縮し、それを使って新しいウィンドウで再起動する

<!-- rubric -->
- 判断の基準を明示している: この実践が変えるのは「ある指示テキストの品質」なのか、「ウィンドウ内のトークン集合の出入りと保持」なのか——感覚でラベルを貼っていない
- 分類が参考解答と一致している（1、3、5 がプロンプトエンジニアリング、2、4、6 がコンテキストエンジニアリング）、あるいは争点のある項目については、示した基準に基づく自己整合的な論証がある
- アテンションバジェットや有限の資源という考えを使って、マルチターンのエージェントの場面ではコンテキストエンジニアリングの側が選択肢ではない理由を説明できている

<!-- answer -->
基準: その実践が作用する対象を見ます。**ある指示テキストそのものの書き方**（言い回し、構造、例）を変えるもので、書き上げれば固定されるなら、プロンプトエンジニアリングです。**どの内容がウィンドウに入り、どんな形で留まり、いつ出ていくか**を決めるもので、走っているループの中で繰り返し実行される必要があるなら、コンテキストエンジニアリングです。

1 項目ずつ見ます:

- **1 → プロンプトエンジニアリング。** システムプロンプトのテキスト品質を変えるもので、一度書けば終わり。ウィンドウ内容の出入りには関わりません。
- **2 → コンテキストエンジニアリング。** ウィンドウ内の古いツール結果の保持と除去を決めています。原文を要約に差し替えるのは、毎ターン実行されるウィンドウ保守の操作です。
- **3 → プロンプトエンジニアリング。** 例を足すのは、意図をよりよく伝えるために指示の構成要素を整えることです。
- **4 → コンテキストエンジニアリング。** 「全文をウィンドウに」と「パスだけ渡して必要時に調べる」の選択は、まさにどのトークンをウィンドウに置くかを決めています。
- **5 → プロンプトエンジニアリング。** 曖昧な言い回しを検証可能な制約に変えるのは、古典的な指示の書き換えです。
- **6 → コンテキストエンジニアリング。** 要約して再起動する操作はウィンドウ全体のトークン集合を変えるもので、単一の指示の書き方とは無関係です。

マルチターンのエージェントの場面で 2 つ目のカテゴリが選択肢でない理由: ループの中のデータは増える一方で減らないのに、アテンションバジェットは有限で、新しいトークンはすべてその一部を消費します。コンテキストは限界収穫が逓減する有限の資源です[^S1]。指示がどれほどうまく書かれていても、ツール結果とメッセージ履歴でウィンドウがターンごとに埋まっていくのを止めることはできません。誰か（つまりあなたのハーネス）がトレードオフを引き受けるしかないのです。

<!-- hint -->
2 つの定義に戻ってください。一方は「指示をどう書き、どう構成するか」、もう一方は「推論時にウィンドウでどの最適なトークン集合を維持するか」です。各実践について問いましょう: これはテキストの言い回しを変えるのか、それともウィンドウ内容の出入りと保持を変えるのか。

<!-- hint -->
もう 1 つのリトマス試験: この実践は一度書けば終わりでしょうか、それともループの毎ターン（あるいは特定のターン）で実行しなければならない操作でしょうか。後者はほぼ間違いなくウィンドウの管理です。

### レベル2: ループに最小の観測メーターを取り付ける

実際のモデルを呼ばずに、本シリーズの「Agent Harness Fundamentals: Loops and Control」コースのループを 20 ターン走らせるさまをシミュレートする単体のスクリプトを書いてください。各ターンで、シミュレートしたアシスタントメッセージ（200 文字とする）とシミュレートしたツール結果（3000 文字とする）を `messages` に追加し、累計文字数をターンごとに出力します。次にスイッチを 1 つ足します: ツール結果は直近 5 ターン分だけ残す（それより古いものはリストから外す）ようにして、もう一度走らせてください。2 つの問いに答えます。2 つのモードで累計はそれぞれどんな傾向を示しますか。その差は何を語っていますか。

<!-- rubric -->
- スクリプトが単体で動き、累計をターンごとに出力している。「全部残す」モードは線形に増加し、「ループの中のデータは増える一方で減らない」ことを裏づけている
- 「ツール結果は直近 5 ターン分だけ残す」モードでは 6 ターン目以降に増加率がはっきり鈍り（ツール結果が頭打ちになり、アシスタントメッセージだけがゆっくり積み上がる）、その差がツール結果の切り詰めだけから来ていることを指摘できている
- 観察を本レッスンの考えに接続できている: 管理されない線形増加は有限のアテンションバジェットを削り続けるので、まず切り詰めるべきは「サイズが最大で、有用な期間が最短」の内容である

<!-- answer -->
参考実装:

```javascript
const ASSISTANT_LEN = 200;    // 各ターンのアシスタントメッセージのシミュレート長（文字）
const TOOL_LEN = 3000;        // 各ターンのツール結果のシミュレート長（文字）
const TURNS = 20;
const KEEP_RECENT_TOOL = 5;   // ツール結果は直近 5 ターン分だけ残す。全部残すなら null にする

let messages = [];
for (let turn = 1; turn <= TURNS; turn++) {
  messages.push({ role: "assistant", size: ASSISTANT_LEN });
  messages.push({ role: "tool", size: TOOL_LEN });

  if (KEEP_RECENT_TOOL !== null) {
    const toolIndexes = messages
      .map((m, i) => (m.role === "tool" ? i : -1))
      .filter((i) => i >= 0);
    const stale = new Set(toolIndexes.slice(0, -KEEP_RECENT_TOOL));
    messages = messages.filter((_, i) => !stale.has(i));
  }

  const total = messages.reduce((sum, m) => sum + m.size, 0);
  console.log(`turn=${String(turn).padStart(2)} total_chars=${total}`);
}
```

2 つのモードの傾向:

- **全部残す**（`KEEP_RECENT_TOOL = null`）: 毎ターン正味 3200 文字ずつ増える厳密な直線で、20 ターン目の累計は 64000。これが「データは増える一方で減らない」の姿です——介入がなければ増加は止まりません。
- **直近 5 ターン分だけ残す**: 最初の 5 ターンは「全部残す」と同一。6 ターン目以降は、新しいツール結果が 1 つ入るたびに最も古いものが 1 つ出ていって相殺し、毎ターンの正味増加はアシスタントメッセージの 200 文字だけになります。20 ターン目の累計は 200 × 20 + 3000 × 5 = 19000。曲線は 5 ターン目以降にはっきり平坦になります。

その差が語ること: 2 つの曲線の差はまるごとツール結果の切り詰めから生まれています——ウィンドウの中でサイズが最大で、有用な期間が最短の内容（古いファイル一覧、古い検索の生テキスト。たいてい数ターン後にはもう参照されません）です。管理しなければ線形増加が有限のアテンションバジェットを削り続けますし、コンテキストは限界収穫が逓減する有限の資源です[^S1]。この「ツール結果は直近 N ターン分だけ残す」スイッチこそ、レッスン4 で体系的に展開するコンパクション戦略の最も素朴な形です。

<!-- hint -->
実際の API を呼ぶ必要はありません。観測の対象は `messages` のサイズであって、モデルが出力する内容ではないからです。固定長のプレースホルダーメッセージで、ターンごとの追加をシミュレートしてください。

<!-- hint -->
「直近 5 ターン分だけ残す」は、6 ターン目以降は新しいツール結果が 1 つ入るたびに最も古いものが 1 つ出ていくことを意味します。計算してみましょう。そのとき、毎ターンの正味増加として残るのは何でしょうか。

<!-- /exercises -->

## まとめ

- コンテキストエンジニアリングはプロンプトエンジニアリングの自然な発展です。前者は「推論時にウィンドウでどの最適なトークン集合を維持するか」を、後者は「指示をどう書き、どう構成するか」を管理し、視点は一文からウィンドウ全体へ引き上げられます[^S1]。
- "Prompt engineering is dead as a standalone skill" はコミュニティのロードマップによる主観的な断定です[^S5]。本レッスンはより慎重な言い回しを採ります——置き換えではなく進化[^S1]。
- LLM は有限のアテンションバジェットを使ってコンテキストを解析し、新しいトークンはすべてその一部を消費します[^S1]——「ウィンドウに収まる」ことと「モデルがうまく使える」ことは別ものです。
- コンテキストロットは崖ではなく漸進的な性能の坂であり、モデルによって急だったり緩やかだったりしますが、その傾向はすべてのモデルに現れます[^S1]。だからエラーにはならず、ただ静かにエージェントを鈍らせます。
- コンテキストは限界収穫が逓減する有限の資源であり[^S1]、エージェントのループの中のデータは増える一方で減りません[^S1]。だからこそ Claude Code の公式ドキュメントはコンテキストウィンドウを "the most important resource to manage."[^S4] と呼んでいます。
- 長い時間軸のタスクのための 3 種類の技術——コンパクション、構造化されたノート取り、マルチエージェントアーキテクチャ[^S1]——はレッスン4 とレッスン5 の主軸に対応し、レッスン6 でそれらをあなたのハーネスに組み込みます。複雑さを持ち込む前に、それが本当に結果を改善するかを確認してください[^S2]。

[>> レッスン2: コンテキストの解剖: システムプロンプト、ツール、そして例](./02-anatomy-of-context.md)
