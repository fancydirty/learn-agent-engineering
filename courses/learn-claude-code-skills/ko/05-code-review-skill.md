# 레슨 5: 케이스 스터디: 코드 리뷰 스킬 만들기

> 학습 목표:
> - 여러 단계로 이루어진 워크플로를 구성하는 법 익히기
> - 체크리스트 패턴이 어떻게 적용되는지 이해하기
> - 보조 파일 사용에 익숙해지기
> - 실전에 바로 쓸 수 있는 복잡한 스킬 하나 만들기
>
> 전제: [<< 레슨 4](./04-testing-debugging.md) | 다음: [레슨 6 >>](./06-advanced-patterns.md)

## 코드 리뷰가 좋은 케이스 스터디인 이유

코드 리뷰는 교과서에 나올 법한 구조화된 워크플로입니다.[^S3]

- **단계가 고정되어 있다**: 컨벤션 확인, 문제 탐색, 개선안 제시
- **기준이 측정 가능하다**: 모든 점검 항목은 통과하거나 통과하지 못하거나 둘 중 하나다
- **끊임없이 반복된다**: 모든 PR에 리뷰가 필요하다
- **스킬에 잘 맞는다**: 팀의 리뷰 기준을 스킬에 적어 두면 모든 리뷰가 같은 잣대를 유지한다

이 케이스 스터디에서 다루는 것:
- 복잡한 워크플로를 명확한 단계로 쪼개는 법
- 체크리스트를 중심으로 지시문을 구성하는 법
- 여러 출력 차원을 한 번에 다루는 법

## 1단계: 리뷰 범위 정하기

무엇을 쓰기 전에, 이 스킬이 무엇을 점검할 책임을 지는지 먼저 정합니다.

**우리 코드 리뷰 스킬이 다루는 세 가지 차원:**

1. **컨벤션**: 네이밍, 서식, 주석
2. **잠재적 문제**: 에러 처리, 엣지 케이스, 보안 위험
3. **유지보수성**: 중복 코드, 함수 길이, 논리적 복잡도

**의도적으로 점검하지 않는 것:**
- 비즈니스 로직이 실제로 올바른지 (요구사항에 대한 진짜 지식이 필요합니다)
- 알고리즘 효율 (성능 테스트가 필요합니다)
- UI/UX 디자인 (코드 리뷰의 범위 밖입니다)

## 2단계: 디렉터리 구조 만들기

이번에는 보조 파일을 써서 리뷰 규칙을 정리합니다.[^S2]

```bash
mkdir -p ~/.claude/skills/code-review
mkdir -p ~/.claude/skills/code-review/checklists

touch ~/.claude/skills/code-review/SKILL.md
touch ~/.claude/skills/code-review/checklists/naming.md
touch ~/.claude/skills/code-review/checklists/error-handling.md
```

**파일을 나누는 이유:**
- SKILL.md는 짧게 유지되고 핵심 흐름만 담는다
- 상세한 점검 규칙은 별도 파일에 두고 필요할 때 로드한다[^S2]
- 팀이 메인 파일을 건드리지 않고 각 체크리스트를 독립적으로 관리할 수 있다

## 3단계: 메인 SKILL.md 작성하기

```markdown
---
name: code-review
description: 코드 변경을 네이밍 컨벤션, 에러 처리, 잠재적 버그, 유지보수성 문제 관점에서 리뷰한다. PR 리뷰나 일반적인 코드 품질 점검에 사용
---

# 코드 리뷰 어시스턴트

팀의 컨벤션과 확립된 관행에 비추어 코드 변경을 체계적으로 리뷰한다.

## 입력 형식

다음 중 무엇이든 받는다.

- Git diff 출력
- 완전한 소스 파일
- 코드 조각 (함수 또는 클래스)
- PR 링크 (먼저 도구로 PR 내용을 읽을 것)

## 리뷰 흐름

다음 순서로 리뷰를 진행한다.

### 1. 컨벤션 점검

`checklists/naming.md`를 참조해 각 항목을 차례로 확인한다.

- **변수명**: 의미가 분명하고, 팀 컨벤션(camelCase, snake_case 등)과 일관적인가
- **함수명**: 동사로 시작하고, 의도를 분명히 드러내는가
- **클래스명**: 명사이고, 단일 책임에 부합하는가
- **상수명**: 모두 대문자, 언더스코어 구분인가

**기준:** 모든 이름은 코드에 익숙하지 않은 사람에게도 그 용도를 알려 줘야 한다

### 2. 에러 처리 점검

`checklists/error-handling.md`를 참조해 다음을 확인한다.

- **예외 포착**: try/catch가 있는가, 그리고 올바른 예외 타입을 잡고 있는가
- **에러 반환값**: 함수가 에러를 올바르게 처리하고 전파하는가
- **엣지 케이스**: 빈 입력, null, undefined, 빈 배열이 처리되는가
- **리소스 정리**: 파일, 커넥션, 락이 제대로 해제되는가

**기준:** 실패할 수 있는 것에는 에러 처리가 필요하다

### 3. 잠재적 문제 점검

- **null/undefined 접근**: 존재하지 않는 프로퍼티나 메서드에 닿을 수 있는가
- **타입 안전성**: 암묵적 타입 변환의 위험은 없는가
- **동시성**: 경쟁 상태나 교착 상태의 위험은 없는가
- **보안 구멍**: SQL 인젝션, XSS, CSRF, 비밀정보 유출

**기준:** 런타임 에러나 보안 위험을 낳을 수 있는 코드는 지적한다

### 4. 유지보수성 점검

- **함수 길이**: 50줄을 넘으면 분리를 제안한다
- **중복**: 세 번 이상 나타나는 로직은 추출을 제안한다
- **중첩 깊이**: 3단계를 넘으면 리팩터링을 제안한다
- **주석 품질**: 복잡한 로직이 설명되어 있는가

**기준:** 다른 개발자가 이 코드를 쉽게 읽고 바꿀 수 있어야 한다

## 출력 형식

리뷰 결과는 다음 구조로 보고한다.

### ✅ 통과
- [점검 항목] - 기준을 충족함

### ⚠️ 살펴볼 것
- **위치**: `file:line`
- **문제**: 정확히 무엇이 잘못됐는지
- **영향**: 이것이 무엇으로 이어질 수 있는지
- **제안**: 어떻게 개선할지

### 🔴 반드시 고칠 것
- **위치**: `file:line`
- **문제**: 정확히 무엇이 잘못됐는지
- **위험**: 왜 이대로 내보낼 수 없는지
- **제안**: 구체적인 수정 방법

### 📊 종합 평가
- 코드 품질: 우수 / 무난 / 개선 필요
- 주요 문제: [가장 중요한 2~3건]
- 권장 우선순위: [무엇부터 고칠지]

## 참고 사항

- **맥락 부족**: 조각이 불완전하면 주변 코드가 더 필요할 수 있다고 말한다
- **프레임워크 관용구**: 문제처럼 보여도 프레임워크 특유의 패턴일 수 있다 — "확인 필요"로 표시한다
- **테스트 코드**: 테스트에서는 타당한 범위에서 기준을 완화한다 (예: 함수 길이)
- **아무것도 없을 때**: 모든 점검을 통과하면 "✅ 리뷰 통과, 뚜렷한 문제를 찾지 못했습니다"를 출력한다
```

## 4단계: 보조 파일 작성하기

**checklists/naming.md:**

```markdown
# 네이밍 컨벤션 체크리스트

## 변수명

**좋은 예:**
- `userCount`: 사용자 수임이 분명하다
- `isAuthenticated`: 불리언은 is/has/can으로 시작한다
- `maxRetryAttempts`: 의미와 단위를 함께 밝힌다

**나쁜 예:**
- `x`, `temp`, `data`: 너무 뭉뚱그려져 있다
- `flag`, `status`: 어떤 상태를 담는지 말해 주지 않는다
- `getUserInfo2`: 숫자 접미사는 보통 중복이 있다는 뜻이다

## 함수명

**좋은 예:**
- `calculateTotalPrice()`: 동사 더하기 명사, 동작과 그 대상을 모두 밝힌다
- `validateUserInput()`: 무엇을 하는지와 무엇에 대해 하는지를 말한다
- `fetchUserProfile()`: `fetch`가 비동기임을 알려 준다

**나쁜 예:**
- `process()`: 너무 뭉뚱그려져 있다, 무엇을 처리하는가
- `doStuff()`: 의도를 전혀 표현하지 못한다
- `handleData()`: `handle`도 `data`도 너무 넓다

## 클래스명

**좋은 예:**
- `UserRepository`: 책임을 밝히는 명사 (사용자 데이터 읽기와 쓰기)
- `PaymentProcessor`: 결제를 처리하는 것임이 분명하다
- `EmailValidator`: 이메일 검증이라는 책임을 밝힌다

**나쁜 예:**
- `Manager`, `Helper`, `Utility`: 접미사가 너무 뭉뚱그려져 아무 의미가 없다
- `DataClass`: 어떤 데이터인지 말해 주지 않는다
```

**checklists/error-handling.md:**

````markdown
# 에러 처리 체크리스트

## 반드시 점검해야 할 시나리오

### 1. 외부 의존성 호출
- API 요청 (네트워크 실패, 타임아웃, 4xx/5xx 응답)
- 데이터베이스 쿼리 (연결 실패, 쿼리 타임아웃, 제약 조건 위반)
- 파일 작업 (파일 없음, 권한 부족, 디스크 가득 참)

### 2. 사용자 입력
- 빈 입력, null, undefined
- 형식이 잘못된 입력
- 범위를 벗어난 값

### 3. 데이터 변환
- JSON 파싱 (형식이 잘못된 입력)
- 타입 변환 (문자열에서 숫자로 변환 실패)
- 날짜 파싱 (유효하지 않은 날짜 형식)

## 에러 처리 패턴

**잡아서 처리하기:**
```javascript
try {
  const data = await fetchUser(id);
  return processData(data);
} catch (error) {
  logger.error('Failed to fetch user', { id, error });
  return null; // 또는 커스텀 에러를 던진다
}
```

**호출 전에 확인하기:**
```javascript
if (!user) {
  throw new Error('User not found');
}
const profile = user.getProfile(); // 안전 — user는 확실히 null이 아니다
```

## 흔한 실수

**문제:** 빈 catch 블록
```javascript
try {
  riskyOperation();
} catch (e) {
  // 여기서 아무 일도 일어나지 않는다 — 에러가 삼켜진다
}
```

**수정:** 최소한 로그는 남긴다
```javascript
try {
  riskyOperation();
} catch (e) {
  logger.error('Operation failed', e);
  throw e; // 또는 에러 상태를 반환한다
}
```
````

## 5단계: 문제투성이 케이스로 테스트하기

문제를 여럿 심어 둔 스니펫을 준비합니다.

```javascript
function process(data) {
  var result = [];
  for (var i = 0; i < data.length; i++) {
    var item = data[i];
    if (item.type == "A") {
      var x = item.value * 2;
      result.push(x);
    } else if (item.type == "B") {
      var y = item.value / 2;
      result.push(y);
    } else {
      result.push(item.value);
    }
  }
  return result;
}
```

스킬을 호출합니다.

```
/code-review

[위 코드를 붙여넣기]
```

**출력에는 다음이 포함되어야 합니다.**

- ⚠️ 네이밍: `process`, `data`, `x`, `y`가 모두 너무 뭉뚱그려져 있다
- ⚠️ `const`/`let` 대신 `var`를 쓴다
- ⚠️ `===` 대신 `==`를 쓴다
- ⚠️ `data`가 null인지, 배열이 맞는지 전혀 확인하지 않는다
- ⚠️ `item.value`가 존재하는지 확인하지 않는다
- 제안: 이 함수는 더 작은 순수 함수들로 나눌 수 있다

## 6단계: 반복하기

**첫 실행에서 흔히 드러나는 것:**

- 놓친 것 (잡아내지 못한 실제 문제) → 점검 규칙을 추가한다
- 출력이 너무 김 → 중요한 것만 보고하도록 출력 형식을 조인다
- 오탐 (멀쩡한 코드를 문제로 표시) → "확인 필요" 분류를 추가한다

**계속 개선하기:**

1. 리뷰할 때마다 어떤 문제가 빠져나갔는지 적어 둔다
2. 체크리스트를 갱신한다
3. 다시 테스트한다
4. 한 달쯤 지나면 그 스킬은 진짜로 정확해집니다.[^S8]

```agentmentor-check
{
  "id": "skills-zh-05-checklist-structure",
  "label": "체크리스트 구성 방식 정하기",
  "prompt": "코드 리뷰 스킬을 쓰고 있는데 점검 규칙이 계속 늘어나 SKILL.md가 300줄을 넘었습니다. 어느 쪽이 더 나은 선택일까요?",
  "whyHere": "이 스킬의 리뷰 규칙이 SKILL.md가 아니라 checklists/로 빠져나가는 것을 방금 보았습니다. 이 지점에서 저지르기 쉬운 실수는 SKILL.md를 모든 것이 쌓이는 자리로 여기는 것입니다. 자기 스킬이 이 크기를 넘기기 전에, 언제 파일을 나눠야 하는지 못 박아 둘 만합니다.",
  "mode": "single",
  "choices": [
    {
      "id": "a",
      "text": "규칙과 흐름을 한 파일에 두기 위해 계속 SKILL.md에 써 나간다",
      "correct": false,
      "feedback": "아닙니다. 300줄은 너무 깁니다. Claude는 스킬을 로드할 때 SKILL.md 전체를 읽으므로, 길이는 호출할 때마다 컨텍스트 비용으로 돌아옵니다 — 게다가 규칙 하나를 바꾸려 할 때마다 그 전부를 스크롤해야 합니다. 상세 규칙을 checklists/로 옮기면 메인 파일이 훨씬 또렷해집니다."
    },
    {
      "id": "b",
      "text": "상세한 점검 규칙을 보조 파일로 옮기고, 흐름은 메인 파일에 남긴다",
      "correct": true,
      "feedback": "정답입니다. 이것이 실제로 작동하는 점진적 공개입니다. SKILL.md는 핵심 흐름만 담고('네이밍을 확인하려면 checklists/naming.md를 참조한다'), 상세 규칙은 각자의 파일에 놓입니다. Claude는 필요할 때 그 파일들을 읽고, 100줄 미만의 메인 파일은 관리하기가 훨씬 수월합니다."
    }
  ]
}
```

<!-- exercises -->
## 💻 연습

### 레벨 1: 자기만의 리뷰 스킬 만들기

잘 아는 영역을 하나 고르세요 — 프런트엔드 코드, API 설계, SQL 쿼리, 문서 — 그리고 그 영역의 리뷰 스킬을 만듭니다.

**요구사항:**
1. 리뷰 차원 최소 3개
2. 차원마다 구체적인 점검 항목 3~5개
3. 명확한 출력 형식 (통과, 살펴볼 것, 반드시 고칠 것)
4. 실제 사례 최소 2개로 테스트

<!-- rubric -->
- 차원이 잘 정의되어 있고 점검할 가치가 있음
- 모든 점검 항목이 행동으로 옮길 만큼 구체적 ("코드 품질 확인"이 아니라)
- 출력 형식만 봐도 심각도가 한눈에 드러남
- 실제 코드나 실제 문서로 테스트하고 결과를 기록함

<!-- answer -->
예시 답안 (API 설계 리뷰):

```markdown
---
name: api-design-review
description: API 설계를 RESTful 컨벤션, 파라미터 네이밍, 에러 처리, 문서 완결성 관점에서 리뷰한다. API 설계 리뷰 때 사용
---

# API 설계 리뷰

## 리뷰 차원

### 1. RESTful 컨벤션
- URL에 동사가 아니라 명사를 쓴다 (✓ `/users` ✗ `/getUsers`)
- HTTP 메서드가 올바르다 (GET 조회, POST 생성, PUT 수정, DELETE 삭제)
- 상태 코드가 타당하다 (200/201/400/404/500)

### 2. 파라미터 설계
- 파라미터명이 분명하고, 전체에 한 가지 스타일을 쓴다 (snake_case 또는 camelCase)
- 필수 파라미터와 선택 파라미터가 구분된다
- 검증 규칙이 문서화되어 있다

### 3. 에러 처리
- 일관된 에러 응답 형태가 하나 있다
- 에러 메시지에 행동으로 옮길 만큼의 맥락이 담긴다
- 클라이언트가 분기할 수 있도록 에러 코드가 있다

## 출력 형식

### ✅ 컨벤션 충족
- [점검 항목]

### ⚠️ 개선 제안
- **문제**: [무엇이 잘못됐는지]
- **제안**: [어떻게 개선할지]

### 🔴 컨벤션 위반
- **문제**: [무엇이 잘못됐는지]
- **영향**: [왜 심각한지]
- **수정**: [무엇을 바꿔야 하는지]
```

<!-- hint -->
일주일에 최소 세 번은 리뷰하는 대상을 고르세요 — 그렇지 않으면 스킬이 들인 품에 값할 만큼 쓰이지 않습니다

<!-- hint -->
첫 버전이 모든 것을 다룰 필요는 없습니다. 가장 흔한 문제 3가지를 쓰고, 일주일 써 본 다음 더 추가하세요

### 레벨 2: 사람의 리뷰와 스킬의 리뷰 비교하기

코드 한 덩어리를 골라 두 번 리뷰합니다.
1. 직접 손으로
2. code-review 스킬로

각각 무엇을 찾아냈는지 비교하고 다음을 적습니다.
- 내가 놓쳤는데 스킬이 잡아낸 문제
- 스킬이 놓쳤는데 내가 잡아낸 문제
- 스킬의 지적 중 오탐이었던 것 (문제로 표시했지만 실은 괜찮았던 것)

<!-- rubric -->
- 같은 코드가 두 리뷰 방식을 모두 거침
- 발견 사항을 나란히 놓고 비교한 기록이 있음
- 스킬이 강한 지점과 부족한 지점을 분석함
- 스킬에 가할 구체적인 변경을 제안함

<!-- answer -->
비교의 예:

**코드:** 50줄짜리 데이터 처리 함수

**사람의 리뷰가 찾은 것:**
- 불분명한 변수명 (2곳)
- 빠진 에러 처리 (1곳)
- 단순화할 수 있는 로직 (1곳)

**스킬의 리뷰가 찾은 것:**
- 불분명한 변수명 (3곳 — 내가 찾은 것보다 하나 더)
- 빠진 에러 처리 (2곳 — 내가 찾은 것보다 하나 더)
- 함수가 50줄을 넘음, 분리 제안
- `===` 대신 `==` 사용 (2곳, 둘 다 내가 놓친 것)

**오탐:**
- 스킬이 "중복 코드"라고 표시했지만, 그 두 블록은 비슷해 보일 뿐 — 목적이 다르므로 합쳐서는 안 됩니다

**가할 변경:**
- 중복 점검에 유사도 임계값을 넣어 닮았다는 이유만으로 표시하지 않게 한다
- 내가 손으로 적용한 "로직 단순화" 규칙을 스킬에 추가한다

<!-- hint -->
스킬은 기계적이고 측정 가능한 문제에 강합니다 — 네이밍, 서식, 명백히 빠진 에러 처리. 비즈니스 로직이 괜찮은지 판단하는 일은 여전히 사람이 필요합니다

<!-- /exercises -->

## 정리

- **코드 리뷰는 스킬에 자연스럽게 맞는다**: 단계가 고정되고, 기준이 측정 가능하며, 반복이 잦다
- **흐름을 체크리스트로 구성한다**: 컨벤션, 에러 처리, 잠재적 문제, 유지보수성
- **보조 파일이 스킬을 관리 가능하게 유지한다**: 메인 파일은 짧게, 상세 규칙은 따로
- **출력에 등급을 매기는 것이 중요하다**: 통과, 살펴볼 것, 반드시 고칠 것 — 리뷰어가 무엇부터 할지 알 수 있도록
- **계속 반복한다**: 리뷰할 때마다 놓친 점검을 추가하면 한 달 뒤에는 정확해진다

다음 레슨에서는 고급 패턴으로 넘어갑니다. 개인 스킬과 프로젝트 스킬, 버전 관리, 그리고 팀 협업입니다.

[레슨 6 >>](./06-advanced-patterns.md)
