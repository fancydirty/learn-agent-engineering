import type { ControlsConfig } from "streamdown";

export const streamdownControls: ControlsConfig = {
  mermaid: {
    copy: true,
    download: true,
    fullscreen: true,
    panZoom: false,
  },
};
